# 视频生成网站设计及实施任务

> **文档状态：** 可执行基线（2026-08-03）  
> **目标读者：** 产品、设计、前后端、测试与部署人员  
> **实施约定：** 执行任务时使用任务中的复选框跟踪；按任务顺序实施和验收，不并行修改同一模块。

## 1. 结论摘要

首版建设一个**单机 Linux 部署、无数据库、面向竖屏图文短视频**的网站。用户可以只提供文字，也可以上传图片并填写文字；视频风格可选择预设（如升学宴、生日宴），填写自定义描述，或提交已获授权的参考视频链接。系统分析素材与风格后自动加入创意分镜、字幕、转场、画面动效和合规 BGM，生成可预览的 MP4；用户可反复提出修改意见并生成新 revision，满意后下载和分享。

推荐方案：

- 前端：React + TypeScript + Vite + Ant Design；业务页面统一使用 Ant Design 组件和设计令牌，不并行引入第二套通用 UI 库。
- 后端：Express 5 + TypeScript；Express 是 Node.js 生态最广泛采用、概念少且易理解的 API 框架，前后端统一语言便于维护。
- 集成工具：部署目标是 Linux。Web 前端/后端语言保持不变，但第三方开源工具不限语言，可将 Python、Go、Rust、C/C++ 项目作为固定版本 CLI、受限子进程或独立容器接入；统一经 TypeScript adapter 调用，不把第三方内部模型泄漏到业务 API。
- 渲染：FFmpeg 为核心渲染器，Sharp 做图片预处理；参考视频解析/风格分析优先复用 yt-dlp、OpenCV、PySceneDetect、librosa、PaddleOCR/CLIP 等高质量免费开源能力，实施前核验许可证与 Star。
- 存储：应用服务器文件系统；每个项目使用 `manifest.json` 保存状态和版本，不引入数据库。
- 任务：进程内AnalysisQueue和RenderQueue默认各并发1，限制参考分析与FFmpeg资源，避免耗尽Linux服务器。
- AI：系统预置MiniMax供应商，默认将“提示词优化”绑定MiniMax-M3、“字幕/特效”绑定MiniMax-H3，但两项能力默认关闭。管理员可在网页系统设置新增/修改AI供应商、协议、请求地址、模型和API Key，并分别更换两项能力的绑定；所有密钥保存到服务器配置文件且只返回掩码。提示词优化结果仍须预览接受，字幕/特效仍须分镜确认。
- BGM：首版根据风格**自动选择并裁剪本地已授权曲库**。语言模型不能直接生成音频，不把“选曲”误称为“AI 作曲”；真正的音乐生成 API 放到后续版本。

## 2. 已知条件、假设与范围边界

### 2.1 已知条件

1. 当前目录只有需求与候选项目资料，尚无代码，也不是 Git 仓库。
2. 首版不使用数据库，部署到 Linux 单服务器，上传数据、风格画像和 revisions 保存到应用数据目录。
3. Web 主栈固定为 React + TypeScript + Vite + Ant Design，以及 Express 5 + TypeScript；集成工具语言不限。
4. AI配置由管理员在系统设置网页管理，保存到 `data/system/settings.json`。默认供应商是MiniMax，能力绑定为M3/H3；用户可改为其他受支持供应商/请求地址/模型/API Key。两个项目级AI开关默认关闭。
5. 参考视频链接仅用于提取主题、色调、节奏、字幕布局和动效强度等抽象风格，不复制原视频素材、音乐、台词、水印或可识别创作表达。用户必须确认拥有分析权限并遵守平台条款。
6. “免费”定义为可自托管的开源软件或无需付费即可运行的本地模型；云 API 的免费额度和条款会变化，不能承诺永久免费，因此不作为核心流程依赖。

### 2.2 首版默认产品参数

以下参数作为可直接开发的默认决策；拿到参考视频样片后只调整模板参数，不改整体架构：

| 项目 | 首版默认值 |
|---|---|
| 输出 | MP4，H.264 + AAC，1080×1920，30 fps，`yuv420p` |
| 时长 | 15–60 秒；按文字量和图片数自动计算 |
| 图片 | 0–20 张；JPG、PNG、WebP；单张不超过 15 MB；无图片但有文字时生成排版文字卡与程序化背景 |
| 文字 | 图片和文字至少提供一项；总计不超过 5,000 字符，每张图可有独立文案 |
| 风格来源 | 三选一：预设下拉、自定义描述、参考视频链接 |
| 预设风格 | `school_banquet`升学宴、`birthday`生日宴、`wedding`婚礼、`romantic`浪漫温馨、`humorous`轻松幽默、`tech`科技感 |
| 参考视频 | 支持allowlist平台的公开链接；默认最长5分钟、最大200MB、单视频；分析后展示风格画像供确认 |
| 反馈修订 | 每次反馈生成结构化修改方案，用户确认后创建新revision；次数不设产品上限，但受磁盘/任务限制 |
| 动效 | 缓慢推近、平移、轻微缩放；按模板组合，不使用高风险闪烁效果 |
| 转场 | 淡入淡出、滑动、缩放切换；默认 0.4–0.8 秒 |
| 字幕 | 底部安全区内，白字描边；按分镜时间轴生成 ASS 并烧录 |
| 音频 | 自动匹配本地已授权 BGM，循环/裁剪至视频长度，淡入淡出 |
| UI 组件库 | Ant Design React；导航、表单、开关、上传、步骤、卡片、表格、弹窗、通知、进度统一使用其组件 |
| 提示词优化AI | 独立Switch，默认未选中；默认绑定MiniMax-M3，可在系统设置换供应商/模型；提案接受前不得使用 |
| 字幕/特效AI | 独立Switch，默认未选中；默认绑定MiniMax-H3，可换供应商/模型；结果必须进入分镜确认 |
| 预览 | 提示词提案先做原文/优化文对比；完成渲染后网页播放器预览；首版不做浏览器内实时逐帧预渲染 |
| 修改 | 修改文字、顺序、风格或字幕后创建新修订并重新渲染 |
| 分享 | 生成不可预测的只读链接，支持复制链接和 Web Share API |
| 用户体系 | 首版无注册、账号和团队协作；编辑后台仅供单运营者/受信任内网使用 |
| 访问边界 | 生产反向代理必须用 Basic Auth 保护 SPA 与项目 API；仅 `/share/*`、`/api/v1/share/*` 及最小化的 `/health` 匿名，health 只返回 `{"status":"ok"}` |
| 部署 | 单实例、单机磁盘；不承诺多实例共享状态和水平扩容 |

### 2.3 首版不做

- 文生视频、数字人、口型同步、声音克隆、AI 图片生成。
- 自动发布到抖音/小红书等平台；“分享”仅指系统内公开链接和浏览器系统分享。
- 浏览器时间线剪辑器、逐帧特效编辑、多人协作。
- AI 原创作曲；只使用来源和商用授权明确的本地曲库。
- 数据库、对象存储、Redis、分布式队列、GPU 集群。
- 未经授权下载、绕过登录/DRM/风控、去水印、保存或重新发布参考视频；授权参考仅临时下载低清分析副本，提取抽象风格后按保留策略删除。
- 对参考作品做像素级复刻，或复制其原始画面、音乐、台词、人物肖像和受保护模板。

## 3. 用户与核心流程

### 3.1 目标用户与核心价值

目标用户是只有简单提示词、文字或少量图片，不熟悉剪辑，也不知道最终视频应该采用什么视觉表达的内容创作者和活动运营人员。

**用户痛点：** 用户能描述“升学宴”“生日祝福”等目标，却难以把描述转换成完整的镜头节奏、配色、BGM、字幕样式、特效和转场，也难以一次说清成片要求。

**系统亮点：** 用户只需提供素材/文字和视频风格，系统即主动完成创意理解并生成可解释的风格画像与分镜，自动匹配合规BGM、字幕、特效和转场；生成后通过“预览—反馈—修改计划—新revision”循环持续调整，直到用户明确标记满意。AI能力只在用户主动选择时辅助，默认开源规则流程也必须完成上述价值闭环。

### 3.2 主流程

**可选前置配置：** 只有需要 MiniMax 时，管理员进入“系统设置”，填写 API Key、确认 M3/H3 模型并测试连接。没有 Key 时两个 AI `Switch` 显示不可用，但项目的规则生成流程保持可用。

1. **创建项目：** 输入标题。
2. **提供内容：** 上传0–20张图片并填写逐图/总文案，或只填写文字；二者至少有一项。
3. **选择风格来源：** 预设下拉（升学宴、生日宴等）、自定义描述、参考视频链接三选一。参考链接需勾选“我有权分析此内容”。
4. **确认风格画像：** 参考链接解析后展示主题、主色、明暗/饱和度、镜头节奏、运动强度、字幕布局、BPM/音乐情绪及置信度；用户编辑并确认后才可继续。
5. **选择可选AI：** 用两个独立Switch选择M3提示词优化和H3字幕/特效。M3提案须预览并接受；H3结果进入分镜确认。
6. **系统创意方案：** 系统解决“用户只有简单文字、不知道成片应该是什么样”的核心痛点，综合素材和已确认风格自动补全创意主题、分镜、动态文字卡、BGM、字幕、特效和转场；无图片时使用程序化背景和动态排版。
7. **确认并渲染：** 用户编辑并确认分镜后提交异步任务；系统冻结全部输入和工具版本，生成MP4。
8. **预览与反馈：** 用户播放成片并填写修改意见，也可调整节奏、字幕、配色、BGM、转场、图片顺序和时长。
9. **反馈计划与新revision：** 系统把反馈转换成可预览的结构化修改计划；用户接受后更新草稿、重新确认并生成新revision。重复8–9直到满意，历史版本可对比和回退。
10. **最终交付：** 标记满意版本为final，下载MP4并开启可撤销分享链接。

### 3.3 异常流程

- 参考链接不受支持、需要登录、平台解析变化或下载失败：不尝试绕过限制，提示用户改用自定义风格描述；测试环境可使用项目内合法fixture验证分析器。
- 参考视频超过5分钟/200MB、含播放列表或重定向到非allowlist域：分析前拒绝。
- 风格分析某一工具失败：返回已成功提取的字段与置信度，允许用户手工补全；核心生成不因CLIP/OCR/节拍分析缺失而阻塞。
- 自由文本反馈无法可靠映射：不猜测，展示结构化控件让用户选择具体调整项后再生成修改计划。
- 不合法图片：上传阶段立即拒绝并说明格式、尺寸或容量问题。
- AI供应商/绑定未配置、模型不可用、超时或返回格式错误：提示词保留原文，字幕/特效退回规则；不自动换供应商/模型或接受提案。
- FFmpeg 失败：任务标记为 `failed`，保存脱敏后的错误摘要；用户可原参数重试。
- 服务重启：按 7.3 状态表恢复；`queued` 保持、`running` 重新排队、`cancelling` 结束为 `cancelled`。
- 磁盘空间不足：提交渲染前拒绝新任务并提示清理项目；不在渲染中途静默删除数据。

## 4. 方案比较与选型

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| React + Ant Design + Express 5 + FFmpeg | 前后端统一 TypeScript；Express 生态成熟、概念少；Ant Design 覆盖管理后台组件；FFmpeg 可控 | 仍需自行实现模板和任务状态 | **首版采用** |
| React + NestJS + FFmpeg | 模块/依赖注入规范完整 | 对当前单体 MVP 装饰器和抽象偏多，不符合简洁目标 | 项目复杂化后再评估 |
| React + Express + Remotion/HTML 渲染 | 前后端统一 TypeScript，画面组件化 | Chromium 渲染资源占用高；音视频细节仍依赖 FFmpeg | 仅作为后续模板引擎实验 |
| 集成MoneyPrinterTurbo/Pixelle/OpenMontage等 | 现成分镜/流水线丰富 | 整体依赖复杂 | **只复用审计通过的独立模块/算子，不整体接管Web系统** |

### 4.1 开源优先、6000 Star门槛与最小功能策略

先拆成最小功能，再选择工具。候选默认须在实施时复核 **GitHub Star ≥ 6,000**、未归档、近12个月有维护、许可证允许当前分发方式、Linux可自动化部署、无不可接受高危漏洞。Star只是初筛，不替代代码/许可证审计；低于6000只允许在没有高质量替代且负责人书面批准时采用。第三方语言不限，Python/Go/Rust/C++工具通过版本锁定的CLI或独立容器接入，Web主栈仍是React/Express TypeScript。

本项目自写代码只负责产品流程、数据契约、权限/安全、工具编排、确认门禁和无法由现成工具覆盖的薄适配层；视频下载、分析、镜头检测、OCR、节拍、转码、字幕烧录和尽可能多的分镜/模板算子必须复用审计通过的开源工具。若某单元最终自研，`docs/oss-adoption.md`必须列出已评估候选、失败证据和最小自研范围。

| 最小功能 | 高质量免费候选 | 计划用法 | 禁止/降级 |
|---|---|---|---|
| 参考链接元数据与授权范围内的临时低清下载 | `yt-dlp/yt-dlp`（GitHubStar约182k） | Linux CLI：先`--dump-single-json`，再按需`--no-playlist`下载受限分析副本 | 不传用户Cookie、不绕登录/DRM；失败改用自定义风格 |
| 抖音等平台备用解析 | `putyy/res-downloader`（约18.9k）、`MediaCrawler`（约59.7k） | 仅在Linux无头模式、许可证和安全审计通过时作为平台adapter；默认仍yt-dlp | 不运行桌面抓包GUI，不采集评论/账号数据 |
| 媒体探测、抽帧、转码、镜头差异、最终渲染 | FFmpeg/ffprobe（高Star、行业标准） | 直接采用CLI，贯穿分析与渲染 | 禁止shell拼接和任意filter输入 |
| 色调、亮度、饱和度、运动量、画面构图 | OpenCV（高Star） | Python分析容器读取采样帧，输出固定JSON schema | 失败时退回FFmpeg统计与手工编辑 |
| 镜头边界与节奏 | PySceneDetect（实施时确认≥6k） | Python CLI/库输出镜头时间点、平均镜长、切换频率 | 不把整套UI引入Web |
| 音频BPM、onset、能量/情绪特征 | librosa（实施时确认≥6k） | Python分析容器输出BPM、能量曲线和标签；不保留原音频 | 失败时根据镜头节奏和预设选择BGM |
| 参考视频字幕区域与密度 | PaddleOCR（GitHubStar约86.9k） | 可选Python容器分析采样帧，只输出区域/密度/字号级别 | 不复制参考字幕正文；CPU成本高时允许关闭 |
| 主题语义标签 | OpenAI CLIP或OpenCLIP（高Star、免费本地模型） | 可选本地推理，对抽帧从预定义安全标签集打分 | 不生成/复制画面；无模型时由用户确认主题 |
| 图文到分镜/自动视频流程 | MoneyPrinterTurbo（约101k）、OpenMontage（约44.8k）、Pixelle-Video（约26.4k）、hyperframes（约39.3k） | 审计后优先复用独立的分镜、模板或时间线模块；以adapter转换为本项目schema | 不整体启动其账号/数据库/Agent系统，不同时引入重复流水线 |
| 自动剪辑/反馈修改参考 | `video-use`（约18.6k）、NarratoAI（约10.6k）、autoclip（约6.3k） | 参考或复用独立剪辑算子；每个算子必须有确定输入输出和无模型降级 | 不允许Agent直接执行未审查命令 |
| 字幕格式与烧录 | Subtitle Edit（约13.7k）行为测试 + FFmpeg/libass | 复用格式兼容测试思路，运行时由ASS生成器和FFmpeg完成 | 不部署C#桌面应用 |
| 图片规范化 | Sharp/libvips（高Star） | Node库直接处理EXIF、色彩和尺寸 | 不扩展无关AI修图 |

`flycut-caption`（约1.7k）、`videoflow`（极低Star）、`short-video-factory`（约5.1k）低于默认门槛，不再作为v0.1首选依赖；只有审计证明不可替代并书面例外时才能采用。

### 4.2 免费API与成本边界

- v0.1核心链路只依赖可自托管开源工具，不依赖付费视频生成API。
- 可以为外部视频生成API保留provider接口，但只有在许可证/服务条款允许、无需付费或有可验证长期免费方案、用户明确启用时接入；“注册赠送额度”不视为永久免费。
- 自托管软件免费不代表运行无成本：CPU/GPU、磁盘和带宽仍由部署者承担。CLIP/PaddleOCR等重工具为可选分析能力，资源不足时降级而非阻塞。
- 所有采用/拒绝决定写入 `docs/oss-adoption.md` 和 `THIRD_PARTY_NOTICES.md`，记录Star快照日期、精确版本/commit、许可证、复用模块、Linux部署方式、资源需求、风险和替代方案。

## 5. 产品界面设计

### 5.1 页面

1. **项目列表 `/`**
   - 创建项目入口。
   - 项目卡片展示封面、标题、更新时间、最新渲染状态。
   - 支持打开和删除；删除需二次确认。
2. **项目编辑 `/projects/:projectId`**
   - 步骤条：素材 → 风格来源 → 风格画像/创意方案 → 生成 → 预览反馈 → 最终交付。
   - 素材区：支持只填文字、只传图片或二者结合；图片拖拽、缩略图、排序、删除、逐图文案。
   - 风格来源区：使用 `Radio.Group` 三选一；预设模式用 `Select`（升学宴、生日宴、婚礼等），自定义模式用 `Input.TextArea`，参考模式用 `Input`填写URL、权限确认`Checkbox`和“分析风格”按钮。
   - 风格画像区：使用 `Descriptions`、色卡、节奏图、`Tag`和`Form`展示/编辑主题、色调、节奏、字幕/BGM/特效建议、置信度和工具来源；必须确认。
   - AI设置区：两个独立Switch选择M3/H3；M3提案与H3分镜仍走各自确认门禁。
   - 创意方案区：展示系统主动补全的创意意图、镜头、文字卡、BGM、字幕、特效和转场，并解释其与素材/风格的对应关系。
   - 生成区：使用 `Progress`、`Alert` 和 `Button` 展示分析/渲染阶段、警告和取消操作。
3. **预览反馈页 `/projects/:projectId/revisions/:revisionId`**
   - 9:16播放器、revision对比/回退、错误警告、下载和分享。
   - 修改意见区同时提供自由文本与结构化控件：更快/更慢、字幕、色调、BGM情绪/音量、特效强度、转场、镜头顺序/时长。
   - 系统生成修改计划并展示受影响镜头/字段；用户接受后创建新草稿和revision，拒绝则不改变当前版本。
   - “设为满意版本”将revision标记为final；final仍可撤销后继续修改。
4. **分享页 `/share/:token`**
   - 只读播放器和下载按钮，不暴露服务器文件路径及编辑接口。
5. **系统设置 `/settings`**
   - 顶部/侧边 `Menu` 提供“系统设置”，仅受信任管理员可进入。
   - “AI供应商”使用Ant Design `Table`+`Drawer/Form`管理多个profile：供应商名称、协议（MiniMax/OpenAI-compatible）、HTTPS请求地址、API Key、可用模型列表、启用状态。
   - “能力绑定”分别为提示词优化和字幕/特效选择供应商profile及模型；默认绑定MiniMax-M3/MiniMax-H3，项目页Switch动态显示当前供应商/模型。
   - profile GET只返回configured及Key掩码，编辑时Key输入初始为空；留空保持，提供测试连接、替换、禁用和二次确认删除。被项目引用的profile不能删除，只能先换绑。
   - `Input.Password` 设置 `autoComplete="new-password"`；Key不进入URL、日志、浏览器存储、Query缓存或错误上报。
6. **能力提示区**
   - 分别显示“M3 提示词优化”和“H3 字幕/特效”的可用/未配置/最近降级状态；可用不代表默认启用。
   - 页面明确说明 MiniMax 是特殊需求辅助功能，两个 `Switch` 未选中时不会向 MiniMax 发送任何内容。

### 5.2 交互原则

- 首屏直接说明三步：“上传图文 → 选择风格 → 生成视频”。
- 图片和文字至少提供一项；只有文字时必须预览程序化背景/文字卡方案，不能因无图片阻塞。
- 风格来源三选一；参考链接分析完成且画像确认前不能生成。任何来源变化都会使分镜确认失效。
- 系统必须主动给出创意方案，而不是要求用户预先描述每个镜头；每个自动选择应显示简短理由并允许编辑。
- 每轮反馈先形成修改计划再应用，不能直接覆盖已完成revision；循环直到用户主动标记满意。
- 上传、提示词提案、生成方案、确认分镜、渲染是独立动作，避免一次失败后全部重来。
- 任何 M3 提案必须由明确的接受操作改变状态；默认、关闭、离开页面、超时和 API 成功都不能推定用户同意。
- M3 与 H3 开关互不联动：可只优化提示词、只生成字幕/特效、两者都用或都不用。改变有效提示词、H3 开关或风格后，现有 storyboard 标记为 stale，必须重新生成并确认后才能渲染。
- 状态不只用颜色表达，始终配文字和图标。
- 移动端可完成上传和查看；复杂排序在桌面端体验优先。

## 6. 系统架构

```text
Browser
  │ HTTP/JSON + multipart + video range requests
  ▼
React + Ant Design SPA ───► Express 5 API（TypeScript）
                              ├─ ProjectService
                              ├─ SystemSettingsService ─► data/system/settings.json
                              ├─ UploadService ─────────► Sharp
                              ├─ ReferenceStyleService
                              │      └─ Linux ToolRunner ─► yt-dlp + FFmpeg + Python分析容器
                              │                              (OpenCV/PySceneDetect/librosa/OCR/CLIP)
                              ├─ PromptService ─────────► AIProviderRegistry(prompt_optimizer，可选)
                              │      └─ ProposalConsentGate（必须显式接受）
                              ├─ StoryboardService ─────► RulePlanner + 经审计的开源分镜/模板算子
                              │      └──────────────────► AIProviderRegistry(caption_effects，可选)
                              ├─ FeedbackService ───────► 结构化修改算子 + revision lineage
                              ├─ AnalysisQueue / RenderQueue（受限并发）
                              │     └─ RenderPipeline ──► FFmpeg + ASS
                              ├─ ShareService
                              └─ FileProjectRepository
                                      └─ ./data/projects/<project_id>/
```

### 6.1 组件职责

| 组件 | 单一职责 | 主要依赖 |
|---|---|---|
| `FileProjectRepository` | 原子读写项目 manifest，列出和删除项目 | Node.js `fs/promises` |
| `SystemSettingsRepository` | 原子读写 `data/system/settings.json`，设置 0600 权限并屏蔽 Key | Node.js 文件系统 |
| `SystemSettingsService` | 提供不可序列化的内部凭据快照与公开脱敏DTO；校验、保存、清除和测试配置 | Zod、Repository |
| `InternalProviderCredentials` | 仅供AIProviderRegistry读取指定profile完整Key，禁止进入响应/日志/错误 | 私有服务类型 |
| `SafeProviderHttpClient` | 解析并固定公网A/AAAA，禁重定向，防DNS rebinding/SSRF | undici Agent、node:dns、IP分类库 |
| `PublicProviderProfile` | 仅含configured、mask、protocol、base、models、enabled和version | 可序列化DTO |
| `AIProviderRegistry` | 根据能力绑定选择MiniMax/OpenAI-compatible adapter与模型并验证响应 | SystemSettingsService、SafeProviderHttpClient |
| `UploadService` | 流式接收图片或文字，规范化并保存 | Busboy、file-type、Sharp |
| `LinuxToolRunner` | 以固定命令、超时、资源限制和JSON schema调用多语言CLI/容器 | spawn、Docker/Podman、allowlist |
| `ReferenceStyleService` | 校验授权URL，临时获取参考视频并合并主题/色调/节奏/字幕/音频特征 | yt-dlp、FFmpeg、OpenCV等adapters |
| `StyleProfileService` | 管理preset/custom/reference三类风格及画像确认 | Repository、Zod |
| `RulePlanner` | 根据素材和确认风格主动生成创意意图、分镜、字幕、特效和BGM分类 | 高星开源算子、风格模板 |
| `FeedbackService` | 将结构化控件/可识别文本反馈转成修改计划，应用为新草稿并连接revision父子关系 | 修改算子、Repository |
| `PromptService` | 调用prompt_optimizer绑定（默认MiniMax-M3）生成候选并保存决定状态 | AIProviderRegistry、Repository |
| `ProposalConsentGate` | 阻止未显式接受的优化文本进入storyboard/render_input | PromptService |
| `StoryboardService` | 默认RulePlanner；开关开启时调用caption_effects绑定（默认MiniMax-H3）替换字幕/特效 | AIProviderRegistry、Planner |
| `RenderPipeline` | 将已确认方案渲染为 MP4 | FFmpeg、Sharp、ASS |
| `AnalysisQueue` | 串行/低并发执行参考视频下载和分析，收集阶段进度与降级结果 | LinuxToolRunner、Repository |
| `RenderQueue` | 排队、取消、恢复和串行执行渲染 | Node.js Promise/AbortController、Repository |
| `ShareService` | 生成/撤销分享令牌并解析只读资源 | secrets、Repository |
| React API client | 封装 HTTP、错误和轮询 | fetch、TanStack Query |
| UI shell | 统一导航、表单、反馈、主题和响应式布局 | Ant Design `ConfigProvider`、`App`、`Layout`、`Menu` |
| Editor state | 管理页面草稿，不复制后端持久化职责 | TanStack Query/React 本地状态 |

### 6.2 目录结构

```text
video-developer/
├── package.json                    # npm workspaces + root scripts
├── package-lock.json
├── tsconfig.base.json
├── README.md
├── THIRD_PARTY_NOTICES.md
├── docs/oss-adoption.md
├── .gitignore
├── docker-compose.yml
├── data/
│   ├── .gitkeep
│   └── system/settings.json        # 运行时生成，0600，不提交 Git
├── assets/
│   ├── fonts/NotoSansSC-Regular.otf
│   ├── backgrounds/
│   └── music/
│       ├── LICENSES.md
│       ├── celebratory/
│       ├── birthday/
│       ├── wedding/
│       ├── humorous/
│       ├── romantic/
│       └── tech/
├── frontend/
│   ├── package.json
│   ├── vite.config.ts
│   ├── src/
│   │   ├── app/router.tsx
│   │   ├── app/theme.ts
│   │   ├── app/AppShell.tsx
│   │   ├── api/client.ts
│   │   ├── api/types.ts
│   │   ├── components/
│   │   ├── features/projects/
│   │   ├── features/editor/
│   │   ├── features/render/
│   │   ├── features/settings/
│   │   ├── features/share/
│   │   └── styles/
│   └── tests/
├── backend/
│   ├── package.json
│   ├── tsconfig.json
│   ├── src/
│   │   ├── server.ts
│   │   ├── app.ts
│   │   ├── config.ts
│   │   ├── api/
│   │   ├── domain/schemas.ts
│   │   ├── repositories/file-projects.ts
│   │   ├── repositories/system-settings.ts
│   │   ├── services/uploads.ts
│   │   ├── services/system-settings.ts
│   │   ├── services/reference-styles.ts
│   │   ├── services/style-profiles.ts
│   │   ├── services/prompts.ts
│   │   ├── services/storyboards.ts
│   │   ├── services/feedback.ts
│   │   ├── services/shares.ts
│   │   ├── integrations/providers/minimax.ts
│   │   ├── integrations/providers/openai-compatible.ts
│   │   ├── integrations/ai-provider-registry.ts
│   │   ├── integrations/safe-provider-http-client.ts
│   │   ├── integrations/oss/
│   │   ├── tools/runner.ts
│   │   ├── tools/reference-analyzer.ts
│   │   ├── tools/schemas.ts
│   │   ├── render/plan.ts
│   │   ├── render/subtitles.ts
│   │   ├── render/pipeline.ts
│   │   └── jobs/queue.ts
│   └── tests/
├── analysis-tools/
│   ├── Dockerfile
│   ├── requirements.lock
│   └── analyze_reference.py
└── scripts/
    ├── check-ffmpeg.ts
    └── smoke-test.ts
```

## 7. 数据设计（文件系统）

### 7.1 磁盘布局

#### 7.1.1 系统设置文件

`data/system/settings.json` 是网站运行时配置，不使用数据库或AI密钥环境变量：

```json
{
  "schema_version": 2,
  "version": 4,
  "ai_providers": [
    {
      "id": "minimax-default",
      "name": "MiniMax",
      "protocol": "minimax",
      "base_url": "https://api.minimax.io/v1",
      "api_key": "用户在网页填写的完整Key",
      "models": ["MiniMax-M3", "MiniMax-H3"],
      "enabled": true
    }
  ],
  "ai_bindings": {
    "prompt_optimizer": {"provider_id": "minimax-default", "model": "MiniMax-M3"},
    "caption_effects": {"provider_id": "minimax-default", "model": "MiniMax-H3"}
  },
  "updated_at": "ISO-8601 UTC"
}
```

- 首次启动创建不含Key的MiniMax默认profile；管理员可新增MiniMax或OpenAI-compatible供应商profile，并分别绑定两项AI能力。普通项目用户只能选择开关，不能读取密钥。
- 文件父目录0700、文件0600，由非root服务用户拥有。Key按用户要求以磁盘明文保存，因此必须依赖HTTPS、Basic Auth、文件/备份权限和日志脱敏；没有独立密钥时不宣称加密。
- 写入使用Zod、设置锁、临时文件+fsync+原子替换和version CAS。
- `getInternalProviderCredentials(profileId)`只向AIProviderRegistry返回不可序列化的完整凭据；`getPublicProviderProfiles()`仅返回configured、掩码、protocol、base、models、enabled和version。两种类型不可复用。
- GET永不返回完整Key；PUT中Key为空表示保持，提供值表示替换。删除/禁用前检查能力绑定和项目引用，存在引用时返回冲突。
- protocol首版支持`minimax`和`openai_compatible`；每种协议由独立adapter构造认证头、路径和请求schema，禁止让用户输入任意header或模板代码。
- Base URL必须HTTPS、无userinfo/query/fragment、端口443或省略、规范化path。MiniMax协议校验官方host；OpenAI-compatible允许管理员确认的公网host。所有host在保存和连接前解析全部A/AAAA并拒绝非全球单播，undici固定已验证IP并保留SNI，禁用全部重定向，防SSRF/DNS rebinding。
- 模型名限制1–128位安全字符；测试连接只返回可用模型/状态/耗时，不返回供应商原始错误中的密钥或请求体。
- 备份默认排除设置文件；显式包含时输出必须0600并警告含多个供应商密钥。

#### 7.1.2 项目数据

```text
data/projects/<project_id>/
├── manifest.json
├── uploads/<asset_id>/original.<ext>
├── uploads/<asset_id>/normalized.jpg
├── references/<reference_id>/metadata.json
├── references/<reference_id>/profile.json
├── references/<reference_id>/samples/       # 已授权的少量抽帧，按保留策略清理
├── styles/selection.json                    # preset/custom/reference三选一及确认摘要
├── prompts/<proposal_id>.json
├── feedback/<feedback_id>.json              # proposed/accepted/rejected/applied
├── storyboard/draft.json             # 当前可编辑草稿，含 version/updated_at
├── revisions/<revision_id>/storyboard.json  # 供审核展示的分镜副本
├── revisions/<revision_id>/render_input.json # 渲染唯一权威输入：分镜+设置+不可变素材摘要
├── revisions/<revision_id>/subtitles.ass
├── revisions/<revision_id>/render.log
├── revisions/<revision_id>/output.mp4
└── tmp/                         # 成功或失败后清理
```

- `project_id`、`asset_id`、`revision_id` 使用 UUIDv4，由服务端生成，不接受客户端路径。
- `storyboard/draft.json` 是唯一当前草稿；`storyboard:generate` 与 `PUT /storyboard` 都必须提交 `expected_version`（项目尚无草稿时为 `null`），在同一项目锁内做乐观并发检查，成功后版本加 1、计算 storyboard digest，并清空 `confirmed_version/confirmed_input_digest/confirmed_at`。版本不匹配返回 `409 STORYBOARD_VERSION_CONFLICT`。
- “待渲染确认载荷”由有效文字/提示词来源、完整storyboard、已确认StyleProfile及来源摘要、字幕/BGM/动效/AI开关、按顺序素材ID/文案/normalized SHA-256、父revision/已接受反馈计划组成；标题和分享状态不纳入。使用UTF-8、键排序、数组保序、无多余空白的canonical JSON计算SHA-256。
- `POST /storyboard:confirm` 必须提交当前 storyboard version 和 canonical input digest；后端在项目锁内从当前项目状态重新构造载荷并匹配后，才保存 `confirmed_version`、`confirmed_input_digest` 和时间。任何会改变确认载荷的操作——文字/提示词、风格来源或画像、字幕/BGM/动效/H3、素材上传/删除/排序/文案、反馈计划应用、分镜生成/编辑——都必须原子清空确认；只改标题或分享状态不清空。
- `POST /renders` 必须重新构造当前确认载荷，并校验 storyboard version 和 input digest 与服务端确认记录完全一致，否则返回 `409 STORYBOARD_NOT_CONFIRMED` 或 `STORYBOARD_CONFIRMATION_STALE`；该门禁适用于规则与 H3 分镜，不能由客户端绕过。
- 通过确认门禁后，`POST /renders`冻结实际文字/已接受提示词提案、StyleProfile、creative_intent、storyboard（每字段标记rule或provider/model）、ProjectSettings、素材摘要、反馈来源、字体/BGM和全部工具/provider版本到`render_input.json`。worker与retry只读该不可变文件，后续设置/工具升级不改变历史输出。
- normalized 素材文件创建后不可原地覆盖；从项目草稿删除素材时，若任一 revision 的 `render_input.json` 仍引用它则保留物理文件，只从当前 assets 列表移除，项目整体删除时才回收。worker 读取前验证 SHA-256，不一致则以 `ASSET_INTEGRITY_ERROR` 失败。
- JSON 写入采用“同目录临时文件 + `fsync` + 原子替换”，避免进程中断产生半个文件。
- 每个项目使用进程内异步锁串行化 manifest、草稿、进度、取消和分享更新；manifest 的 `version` 作为 CAS 版本，防止并发更新丢失。
- `manifest.json` 是项目元数据和 revision 状态的唯一事实来源；派生文件可重新生成。
- 服务端启动时扫描 manifests 恢复索引，不维护额外全局数据库文件。

### 7.2 核心模型

```json
{
  "schema_version": 1,
  "version": 3,
  "id": "uuid",
  "title": "旅行回忆",
  "created_at": "ISO-8601 UTC",
  "updated_at": "ISO-8601 UTC",
  "assets": [
    {"id": "uuid", "filename": "a.jpg", "mime_type": "image/jpeg", "width": 1440, "height": 1920, "caption": "第一天到达海边", "order": 0}
  ],
  "text_content": "祝贺孩子金榜题名",
  "style_selection": {
    "mode": "preset",
    "preset_id": "school_banquet",
    "custom_text": null,
    "reference_id": null,
    "profile_version": 1,
    "confirmed_profile_digest": "sha256:<hex>"
  },
  "settings": {"style": "romantic", "subtitles_enabled": true, "music_enabled": true, "effects_enabled": true, "prompt_optimization_enabled": false, "caption_effects_ai_enabled": false},
  "prompt": {"original_text": "记录一次温暖的海边旅行", "active_source": "original", "accepted_proposal_id": null},
  "latest_revision_id": "uuid",
  "draft_storyboard": {"version": 2, "updated_at": "ISO-8601 UTC", "path": "storyboard/draft.json", "digest": "sha256:<hex>", "confirmed_version": 2, "confirmed_input_digest": "sha256:<hex>", "confirmed_at": "ISO-8601 UTC"},
  "share": {"enabled": true, "revision_id": "uuid", "token_digest": "sha256:<hex>", "created_at": "ISO-8601 UTC"},
  "revisions": [
    {"id": "uuid", "parent_revision_id": null, "feedback_id": null, "status": "completed", "is_final": false, "progress": 100, "stage": "completed", "render_input_digest": "sha256:<hex>", "warning_codes": [], "error_code": null, "created_at": "ISO-8601 UTC"}
  ]
}
```

`references/<reference_id>/profile.json`：

```json
{
  "version": 1,
  "source": "reference_url",
  "theme_tags": ["celebration", "family", "youth"],
  "palette": ["#D9B45B", "#9B1C31", "#F6E7C1"],
  "brightness": 0.68,
  "saturation": 0.61,
  "contrast": 0.55,
  "average_shot_seconds": 2.4,
  "pace": "medium_fast",
  "motion_intensity": 0.42,
  "caption": {"region": "bottom", "density": "medium", "size": "large"},
  "music": {"bpm": 112, "energy": "uplifting", "tags": ["celebration"]},
  "confidence": {"theme": 0.72, "palette": 0.91, "pace": 0.88},
  "tool_versions": {"yt_dlp": "pinned", "ffmpeg": "pinned", "opencv": "pinned"},
  "confirmed_digest": null
}
```

- 风格画像只保存抽象特征和少量授权抽帧，不保存参考视频音乐/台词正文。用户编辑后通过确认API生成digest。
- preset和custom也规范化为同一StyleProfile，确保RulePlanner不关心来源。

`feedback/<feedback_id>.json`：

```json
{
  "id": "uuid",
  "source_revision_id": "uuid",
  "status": "proposed",
  "raw_comment": "字幕再大一点，节奏慢一些",
  "changes": [
    {"op": "set_caption_scale", "value": 1.15},
    {"op": "set_pace", "value": "slower"}
  ],
  "unsupported_text": [],
  "created_at": "ISO-8601 UTC"
}
```

- 反馈计划只允许白名单操作；状态为`proposed → accepted/rejected`，accepted应用后创建新草稿并记录`applied_revision_id`。
- 无法可靠解释的自由文本进入`unsupported_text`，必须由用户用结构化控件澄清，系统不得猜测。

`prompts/<proposal_id>.json`：

```json
{
  "id": "uuid",
  "model": "MiniMax-M3",
  "original_text": "记录一次温暖的海边旅行",
  "proposed_text": "以温暖细腻的叙事记录海边旅行……",
  "proposed_digest": "sha256:<hex>",
  "status": "proposed",
  "accepted_text": null,
  "created_at": "ISO-8601 UTC",
  "decided_at": null
}
```

- 状态只允许 `proposed → accepted` 或 `proposed → rejected`，不可逆；再次优化创建新 proposal。
- `accepted` 必须来自独立确认 API，并提交 `proposed_digest` 防止确认错版本；“编辑后接受”把用户最终确认文本写入 `accepted_text`。
- `active_source=optimized` 只在 proposal 已 accepted 时合法；生成 storyboard 和 `render_input.json` 必须保存实际提示词文本、来源、proposal ID、模型名和摘要。未接受提案只能留作预览审计，不能被系统自动选用。
- 用户修改原始提示词后，后端必须清除旧 `accepted_proposal_id` 并把 `active_source` 切回 `original`；旧 proposal 保留审计但不能复用。关闭 M3 开关只阻止新请求，不撤销用户已经接受并拥有的文本；用户可显式“恢复原文”。

`storyboard.json`：

```json
{
  "version": 1,
  "duration_seconds": 18.0,
  "music_category": "acoustic/warm",
  "scenes": [
    {"asset_id": "uuid", "start": 0.0, "duration": 4.5, "subtitle": "第一天，我们抵达海边", "motion": "zoom_in", "transition": "fade"}
  ]
}
```

### 7.3 状态机

`StyleAnalysisJob.status`：`queued → downloading → probing → sampling → analyzing → merging → awaiting_confirmation`，任一运行阶段可到`failed/cancelling/cancelled`。部分工具失败但核心字段可用时仍进入`awaiting_confirmation`并带warnings；只有用户确认画像后reference状态为`confirmed`。

`FeedbackPlan.status`：`proposed → accepted → applied`或`proposed → rejected`；source revision删除前禁止删除其计划，应用时必须校验source revision和当前draft version未过期。

`Revision.status` 只取终态/调度态，`Revision.stage` 只表示运行中的进度阶段：

| 当前 status | 允许操作/事件 | 下一 status | stage 规则 |
|---|---|---|---|
| `queued` | worker 领取 | `running` | `planning` |
| `queued` | 用户取消 | `cancelled` | `cancelled` |
| `running` | 阶段推进 | `running` | `planning → preparing → rendering → finalizing` |
| `running` | 用户取消 | `cancelling` | 保持当前阶段 |
| `cancelling` | 子进程退出/重启恢复 | `cancelled` | `cancelled` |
| `running` | probe 成功 | `completed` | `completed` |
| `running` | 任一阶段异常 | `failed` | 失败时所在阶段 |

- `draft` 是项目编辑状态，不是 revision status；revision 创建后初始即为 `queued`。
- `completed`、`failed`、`cancelled` 为不可逆终态。重试永远创建新revision。`is_final`是独立交付标记，同项目最多一个；设置新final时原final自动撤销但文件保留。
- 服务重启时：遗留 `queued` 保持不变；`running` 回到 `queued` 并清零瞬时进度；`cancelling` 变为 `cancelled`；终态不变。
- 状态只能由后端按表迁移。每次迁移都在项目锁内原子更新 manifest；前端不得直接提交 `status`、`stage`、输出路径或分享令牌。

### 7.4 数据保留

- 参考视频原始分析副本只放项目`tmp/reference-*`，风格画像完成或失败后立即删除；默认只保留profile、metadata和最多12张低清抽帧，项目删除时一并删除。
- 用户可手工删除参考抽帧而保留画像；任何公开分享都不包含参考资料。
- 首版默认项目和revision永久保留，直到用户主动删除。
- 删除项目时先将目录原子移动到 `data/trash/`，再异步删除，以减少半删除状态。
- 提供磁盘可用空间指标和管理员清理命令；自动过期策略待引入用户体系后再决定。

## 8. API 设计

统一前缀 `/api/v1`，成功返回 JSON；错误格式固定为：

```json
{"error":{"code":"INVALID_IMAGE","message":"图片格式不受支持","details":{}}}
```

| 方法与路径 | 用途 | 关键响应 |
|---|---|---|
| `GET /health` | 存活检查 | `{"status":"ok"}` |
| `GET /api/v1/capabilities` | FFmpeg、分析工具、OSS、两项AI能力 | 返回工具版本/可用性及能力绑定的provider/model，不返回密钥 |
| `GET /api/v1/system-settings/ai-providers` | 读取供应商profiles与能力绑定 | PublicProviderProfile数组；无完整Key |
| `POST /api/v1/system-settings/ai-providers` | 新增供应商、协议、base、Key、模型 | `201` + 脱敏profile |
| `PUT /api/v1/system-settings/ai-providers/{pid}` | 修改/替换Key、地址、模型、启用状态 | version CAS + 脱敏profile |
| `DELETE /api/v1/system-settings/ai-providers/{pid}` | 删除未绑定/未引用profile | `204`或409 |
| `POST /api/v1/system-settings/ai-providers/{pid}:test` | 测试供应商连接/模型 | 脱敏状态、模型、耗时 |
| `PUT /api/v1/system-settings/ai-bindings` | 分别绑定prompt_optimizer/caption_effects | 更新后的公开绑定 |
| `GET /api/v1/projects` | 项目列表 | 项目摘要数组 |
| `POST /api/v1/projects` | 创建项目 | `201` + project |
| `GET /api/v1/projects/{id}` | 项目详情 | project |
| `PATCH /api/v1/projects/{id}` | 改标题、设置、素材文案/顺序 | 更新后的 project |
| `DELETE /api/v1/projects/{id}` | 删除项目 | `204` |
| `GET /api/v1/styles/presets` | 列出升学宴、生日宴等版本化预设 | preset摘要和默认StyleProfile |
| `POST /api/v1/projects/{id}/style-selection` | 保存preset/custom/reference三选一 | selection；改变后清除分镜确认 |
| `POST /api/v1/projects/{id}/references:analyze` | 提交allowlist链接、授权确认并创建分析任务 | `202` + analysis job/reference ID |
| `GET /api/v1/projects/{id}/references/{refId}` | 查询解析进度、警告和风格画像 | metadata + profile + confidence |
| `POST /api/v1/projects/{id}/references/{refId}:confirm` | 编辑并确认抽象风格画像 | confirmed profile digest |
| `DELETE /api/v1/projects/{id}/references/{refId}` | 删除参考metadata/profile/samples | `204` |
| `POST /api/v1/projects/{id}/assets` | multipart 上传图片 | `201` + asset |
| `DELETE /api/v1/projects/{id}/assets/{assetId}` | 删除素材 | `204` |
| `POST /api/v1/projects/{id}/prompt-proposals` | 用户开启功能后调用 M3 生成候选，不改变有效提示词 | `201` + proposed proposal |
| `POST /api/v1/projects/{id}/prompt-proposals/{pid}:accept` | 提交 proposal digest 和最终确认文本，显式接受 | accepted proposal + active prompt |
| `POST /api/v1/projects/{id}/prompt-proposals/{pid}:reject` | 明确拒绝提案，继续使用原提示词 | rejected proposal |
| `POST /api/v1/projects/{id}/storyboard:generate` | 携带 `expected_version`；默认规则生成，H3开关开启时只替换字幕/特效 | 新 version + warnings + content source；冲突返回 409 |
| `PUT /api/v1/projects/{id}/storyboard` | 携带当前 version 保存草稿并清除旧确认 | 新 version/digest；版本冲突返回 409 |
| `POST /api/v1/projects/{id}/storyboard:confirm` | 提交当前 version/input digest，服务端从提示词+设置+有序素材+分镜复算后确认 | confirmed version/input digest/time |
| `POST /api/v1/projects/{id}/renders` | 校验当前分镜已确认，原子冻结草稿、创建 revision 并排队 | `202` + revision；未确认/已过期返回 409 |
| `GET /api/v1/projects/{id}/revisions/{rid}` | 查询进度或结果 | revision |
| `POST /api/v1/projects/{id}/revisions/{rid}/feedback:plan` | 将自由文本+结构化控件转成白名单修改计划 | proposed plan + unsupported_text |
| `POST /api/v1/projects/{id}/feedback/{fid}:accept` | 接受并应用修改计划到新草稿 | updated draft + parent revision |
| `POST /api/v1/projects/{id}/feedback/{fid}:reject` | 拒绝计划，不改变项目 | rejected plan |
| `POST /api/v1/projects/{id}/revisions/{rid}:finalize` | 将满意版本标记为final | revision；同项目仅一个final |
| `DELETE /api/v1/projects/{id}/revisions/{rid}/finalize` | 撤销final以继续修改 | `204` |
| `POST /api/v1/projects/{id}/revisions/{rid}:cancel` | 取消任务 | revision |
| `POST /api/v1/projects/{id}/revisions/{rid}:retry` | 在锁内逐字节复制源 `render_input.json` 并创建新任务，不读取当前项目草稿/设置 | `202` + new revision |
| `GET /api/v1/projects/{id}/revisions/{rid}/video` | 播放/下载，支持 Range | `video/mp4` |
| `POST /api/v1/projects/{id}/revisions/{rid}/share` | 为指定成功 revision 开启/轮换分享 | 一次性返回 URL |
| `DELETE /api/v1/projects/{id}/share` | 撤销项目当前分享 | `204` |
| `GET /api/v1/share/{token}` | 分享页元数据 | 令牌绑定 revision 的只读标题和视频 URL |
| `GET /api/v1/share/{token}/video` | 分享令牌所绑定的视频，支持 Range | `video/mp4` |

一个项目同一时刻只有一个活动分享链接，且链接永远固定到创建时指定的 `completed` revision；生成新 revision 不会改变旧链接内容。再次分享同一或其他 revision 会轮换 token 并使旧链接立即失效。token 由 32 字节随机值构成，manifest 保存 `SHA-256(token)`；随机 token 自身具有 256 位熵，无需另存盐，比较使用常量时间函数。

关键错误码补充：`INVALID_AI_PROVIDER`、`AI_PROVIDER_IN_USE`、`AI_PROVIDER_NOT_CONFIGURED`、`AI_PROVIDER_CONNECTION_FAILED`、`INVALID_REFERENCE_URL`、`REFERENCE_AUTHORIZATION_REQUIRED`、`REFERENCE_UNSUPPORTED`、`REFERENCE_LIMIT_EXCEEDED`、`STYLE_ANALYSIS_FAILED`、`STYLE_PROFILE_NOT_CONFIRMED`、`FEEDBACK_UNSUPPORTED`、`FEEDBACK_PLAN_STALE`。其余沿用项目、素材、分镜、渲染和分享错误码。

## 9. 风格分析、创意生成、字幕、特效与 BGM

### 9.1 风格来源与参考视频分析

1. **预设模式：** 从版本化StyleProfile库选择；升学宴强调庄重喜庆、金红配色、成长叙事和中等节奏，生日宴强调温暖活泼、祝福字幕和轻快节奏。用户仍可编辑画像。
2. **自定义模式：** 将主题、受众、色调、节奏、情绪、字幕和特效偏好规范化为StyleProfile；缺失字段由规则默认值补全并标记`inferred=true`。
3. **参考模式：**
   - URL只接受配置的公开平台host和单视频；先用yt-dlp读取metadata，验证时长/大小/播放列表/协议，再临时获取最低满足分析的分辨率。
   - FFmpeg抽帧/音频；OpenCV提取色调/明暗/饱和度/运动量；PySceneDetect计算镜头节奏；librosa提取BPM/能量；PaddleOCR只统计字幕区域/密度；CLIP从预定义主题标签集评分。
   - 分析器输出独立JSON，ReferenceStyleService按字段置信度合并。任何工具失败只降低对应字段置信度，不伪造结果。
   - 页面展示画像、证据抽帧、工具来源和置信度；用户编辑并确认后生成digest。系统只模仿抽象风格，不复制画面/音乐/台词。
4. 任何风格来源变化或画像编辑都会使旧分镜确认失效。

#### 9.1.1 参考链接安全与合规

- 只允许`https`和配置的抖音/YouTube等平台host，不接受IP、localhost、私网DNS、userinfo、query中的凭据或任意重定向目标；沿用SafeProviderHttpClient的DNS固定/非公网拒绝原则。
- yt-dlp以固定版本、无shell、无playlist、无cookie、最大5分钟/200MB/720p、超时和受限临时目录运行；关闭插件/配置文件自动加载，不执行postprocessor脚本。
- 分析任务使用低权限Linux用户/容器，限制CPU、内存、进程数、文件数、网络出口和总时长；stdout/stderr截断并脱敏。
- 用户必须勾选授权声明；系统不绕过登录、DRM、地区限制或平台防护。原始文件分析后立即删除。

### 9.2 自动创意与确定性规则

1. 先生成`creative_intent`：一句主题、开场/发展/高潮/收尾结构、情绪曲线及每项自动选择理由，让用户知道系统理解了什么。
2. 有图片时每张至少一个候选镜头并按文案/视觉顺序重排；无图片时按句子生成2–8个动态文字卡，使用已授权纹理、渐变、粒子/几何背景和Ken Burns类运动。
3. 镜头默认时长`clamp(字符数/6,2.5,6.0)`，再根据StyleProfile的pace/average_shot_seconds校准；总时长15–60秒。
4. 总文案按标点切句；相邻镜头避免相同motion；字幕每行约18个中文字符、最多两行，以像素宽度最终校验。
5. BGM从StyleProfile的情绪/BPM/energy映射到已授权曲库，按目标节奏裁剪；H3无权选择文件。
6. 特效强度根据主题和场景控制，升学宴/生日宴避免频闪和过度故障效果；所有motion/transition来自白名单。
7. 系统必须生成完整默认方案，用户无需先知道镜头语言；同时保留字幕、节奏、BGM、特效、配色和顺序的可编辑入口。

### 9.2.1 反馈修改规则

- 结构化控件直接映射白名单操作；自由文本只识别可证明的有限意图词典，或在提示词优化开关开启时调用当前`prompt_optimizer`绑定生成候选修改指令并再次征得用户同意。
- 修改计划必须列出`before/after`、受影响镜头和是否需要重新生成字幕/特效/BGM；应用前用户确认。
- 每次应用创建新草稿并以`parent_revision_id`连接来源，不改写历史MP4；可回退任一revision并从其继续分支。
- 只有用户点击“设为满意版本”才结束循环并标记final。

### 9.3 风格模板与时间线

| 风格 | 色彩/字幕 | 动效与转场 | BGM 分类 |
|---|---|---|---|
| 升学宴 | 金/红/暖白，庄重喜庆 | 成长回顾→录取/祝福高潮，稳健推近与淡化 | celebratory/uplifting |
| 生日宴 | 暖橙/粉/彩色，活泼温馨 | 年龄/回忆/祝福文字卡，轻快缩放和滑动 | upbeat/warm |
| 婚礼 | 米白/香槟金/柔粉 | 慢推近、交叉淡化、誓言重点字幕 | romantic/acoustic |
| 轻松幽默 | 暖黄强调、较粗字幕 | 快速轻缩放、滑动；避免频闪 | upbeat/light |
| 浪漫温馨 | 暖粉/米白、柔和阴影 | 慢推近、交叉淡化 | acoustic/warm |
| 科技感 | 青蓝、细描边 | 平移、轻微故障切换；控制强度 | electronic/minimal |

时间线契约：`Scene.duration` 表示该镜头从开始到结束的完整屏幕时间；除最后一镜外，结尾与下一镜重叠转场。轻松幽默、浪漫温馨、科技感的默认转场时长分别为 0.4、0.8、0.5 秒，且不得超过相邻任一镜头时长的 25%。第 1 镜只做 0.4 秒画面淡入，最后一镜 `transition=none` 并做 0.6 秒淡出。镜头开始偏移按 `next_start = current_start + current_duration - transition_duration` 计算，`Storyboard.duration_seconds = 最后一镜 start + duration`；规则规划器以该公式反算并保证最终 15–60 秒，RenderPlan 单测逐帧验证偏移，ffprobe 允许误差不超过 0.5 秒。

BGM契约：预设和参考画像把`music.tags/energy/bpm`映射到授权曲库目录与曲目metadata；升学宴默认celebratory，生日宴upbeat/warm，婚礼romantic/acoustic。候选先按标签和BPM距离排序，再以项目/风格digest稳定选取。选曲仅在创建revision时冻结到`render_input.json`；新增曲库不影响历史版本，文件/许可证摘要不一致则失败。

### 9.4 AI辅助能力边界

#### 9.4.1 提示词优化（默认绑定MiniMax-M3）

- 默认绑定MiniMax-M3；管理员可在系统设置切换到其他已配置供应商/模型，提案预览必须显示实际供应商和模型。
- 只有用户开启开关并点击优化才调用`prompt_optimizer`绑定；输入仅含当前提示词、已确认StyleProfile和必要文字，不上传图片二进制。
- 输出schema只允许候选文本和修改说明，不得生成字幕、BGM、路径或命令。
- API 成功只产生 `proposed` 提案。页面必须展示原文/提案差异；只有用户点击“接受”或“编辑后接受”，后端确认 proposal digest 后才更新 active prompt。
- 拒绝、关闭弹窗、离开页面、网络超时或关闭开关均保持原提示词；系统不得自动接受、默认勾选接受或用提案覆盖原文。

#### 9.4.2 字幕/特效生成（默认绑定MiniMax-H3）

- 默认绑定MiniMax-H3；管理员可独立切换`caption_effects`供应商/模型，与提示词优化开关互不关联。
- 只有用户开启字幕/特效AI并生成方案时调用；输入采用当前有效提示词、素材顺序和已确认StyleProfile。
- 输出schema只允许每镜字幕和白名单motion/transition，不得改变图片、时长上限、BGM文件、路径或命令。
- 结果必须经过Zod、白名单和时间线校验，并标记实际供应商/模型后进入StoryboardEditor确认。

#### 9.4.3 供应商配置、降级与隐私

- AIProviderRegistry从`data/system/settings.json`读取能力绑定；内部凭据只给adapter，GET/capabilities/日志只用PublicProviderProfile。设置保存后热更新。
- `/capabilities`分别报告两项绑定的供应商、模型和可用状态；未配置/禁用时关闭对应项目Switch，不静默换绑。
- MiniMax与OpenAI-compatible adapter各自使用固定认证/请求构造，响应统一转为能力schema；15秒超时、最多重试1次，授权失败不重试。
- 提示词优化失败保留原文；字幕/特效失败退回RulePlanner。两个开关关闭时不得调用任何AI供应商。
- 日志不记录任何供应商Key、完整提示词/字幕、请求体或原始响应；只记录provider ID、model、ID、耗时和脱敏错误码。
- 每个供应商在系统设置中独立测试；连接成功不自动开启项目功能。

### 9.5 FFmpeg 渲染步骤

1. 有图片时用Sharp纠正EXIF/色彩；无图片时根据StyleProfile生成1080×1920程序化背景和文字卡素材。
2. 图片镜头使用模糊填充背景+完整前景或经用户确认的裁切；文字镜头使用渐变、纹理、粒子/几何动效和动态排版。
3. 每个镜头用 `zoompan`/`scale`/`crop` 生成中间无音频片段。
4. 按 storyboard 转场合并镜头；转场不可用时降级为硬切，不执行任意外部脚本。
5. 生成 ASS 字幕，使用项目内置中文字体并限制在平台安全区。
6. 使用 `render_input.json` 中冻结的 BGM 和字体，验证文件及许可证条目摘要；BGM 裁剪/循环、响度归一化并设置 1 秒淡入淡出，禁止运行时重新选曲。
7. 合成音视频和字幕，输出 H.264/AAC MP4；运行 `ffprobe` 验证编码、尺寸、时长和可播放性。
8. 原子移动为 `output.mp4`，清理 revision 临时文件，状态变为 `completed`。

## 10. 非功能需求

### 10.1 性能与容量

- 目标环境：4 vCPU、8 GB RAM、可用磁盘不少于 20 GB。
- 30 秒、10 张 1080p 图片的视频，在目标服务器上 3 分钟内完成作为首版性能目标，不作为跨硬件绝对承诺。
- 上传使用流式写盘，不把整个文件读入内存。
- 渲染队列默认并发1；风格分析队列默认并发1。Python/Go/Rust工具运行在Linux受限容器/用户中，总CPU/内存/进程/磁盘受配额控制。
- 单个参考视频默认≤5分钟/200MB/720p分析副本；分析目标3分钟内完成，重模型关闭时应更快。
- 视频响应支持HTTP Range。

### 10.2 安全与隐私

- 根据文件魔数和 Sharp 解码结果验证类型，不信任扩展名和客户端 MIME。
- 对像素总数设上限，防止解压炸弹；移除 EXIF 元数据。
- 文件名只用于展示，磁盘路径全部使用服务端 UUID；拒绝路径穿越和符号链接。
- 参考URL与AI供应商URL使用不同schema和网络策略；前者仅允许平台host，后者允许管理员确认的公网HTTPS provider。两者均执行DNS/IP/重定向防SSRF。
- 所有第三方CLI使用绝对可执行路径、固定参数模板、无shell、独立临时目录和JSON输出schema；工具版本及容器digest冻结到revision。
- 参考视频授权声明、分析时间和来源URL摘要写入审计；公开日志不记录完整带query URL。
- 限制请求体、上传数量、文字长度和同时任务数。
- 生产环境限定 CORS 来源并使用 HTTPS；反向代理以 Basic Auth 保护编辑 SPA 和项目 API，仅 `/share/*`、`/api/v1/share/*` 允许匿名持 token 访问，另匿名 `/health` 只返回固定存活状态且不泄露版本、路径或能力。首版不允许把未保护的项目 API 暴露到公网。
- Basic Auth 凭据只以部署 secret/htpasswd 文件注入，不进入仓库、镜像、浏览器持久存储或项目文件；MiniMax Key 按用户要求只存 `data/system/settings.json`，该文件权限必须为 0600、禁止提交 Git和默认备份。前后端不记录凭据、Key、完整提示词或字幕正文。
- 系统设置路由与页面必须受 Basic Auth 保护并启用 CSRF/Origin 校验；生产只允许 HTTPS。GET/错误/capabilities 永不返回完整 Key，PUT 请求体不得进入访问日志或错误上报。
- MiniMax 两开关默认关闭；关闭时后端不得发起供应商请求。M3 proposal 必须以服务端状态和摘要校验显式同意，不能仅依赖前端按钮状态。
- 分享 token 使用 32 字节安全随机数，manifest 仅保存 SHA-256 摘要并以常量时间比较；撤销或轮换后立即失效。
- 明示用户必须拥有上传素材及音乐的使用权。

### 10.3 可观测性

- JSON 结构化日志字段：`request_id`、`project_id`、`revision_id`、`stage`、`duration_ms`、`error_code`。
- `/health` 只判断进程；`/api/v1/capabilities` 从系统设置的脱敏快照报告 FFmpeg、字体、曲库、已采用开源组件及 M3/H3 两项能力。
- 系统设置变更日志只记录操作者、时间、配置版本和“Key已设置/替换/清除”，不记录 Key、API Base 查询参数或请求体。
- 每个 revision 保存 FFmpeg stderr 到 `render.log`，API 只返回安全摘要。
- 记录上传数、队列长度、各阶段耗时、成功率和磁盘余量；首版先输出日志，后续接监控系统。

## 11. 测试与验收策略

### 11.1 测试层级

- 后端单元测试：Zod、文件仓库、provider profile/绑定、URL安全、StyleProfile合并、创意规则、反馈白名单、M3同意门禁、H3降级、状态机、ASS。
- 工具契约测试：对yt-dlp/FFmpeg/OpenCV/PySceneDetect/librosa/PaddleOCR/CLIP使用固定fixtures验证JSON schema、超时、资源限制、部分失败降级和版本记录。
- 后端集成测试：Supertest覆盖风格/分析/反馈/provider/渲染API；真实FFmpeg生成图片型和纯文字型MP4；外部平台与AI供应商使用受控mock。
- 前端组件测试：三种风格模式、预设下拉、自定义输入、参考URL/授权、画像编辑确认、AI供应商管理、两个Switch、创意方案、反馈计划和final标记。
- E2E：分别覆盖预设升学宴、生日宴、自定义、mock参考链接、纯文字和图文输入；循环两次反馈后final、下载和分享；AI默认关闭及自定义供应商可选路径。
- 人工视觉验收：升学宴、生日宴、自定义、参考画像和纯文字至少各一条，检查创意合理性、字幕安全区、节奏、BGM、特效和移动端播放。

### 11.2 MVP 完成定义

以下条件全部满足才算首版完成：

1. 用户可只填文字、只传图片或二者结合；至少一项非空。纯文字输入可生成包含程序化背景、动态文字卡、BGM、字幕和特效的视频。
2. 风格来源三选一：预设下拉至少含升学宴/生日宴/婚礼等，自定义描述可编辑，参考视频链接可异步分析。
3. 参考链接必须经授权确认、平台/大小/时长/安全校验；输出主题、色调、节奏、字幕布局、BPM/音乐情绪和置信度，用户确认画像后才能生成。
4. 系统必须针对简单描述主动生成`creative_intent`、完整分镜、BGM、字幕、特效和转场，并解释选择理由，而不是把创意责任退回用户。
5. M3/H3两个Ant Design Switch默认未选中且独立；提示词提案须接受，字幕/特效须分镜确认；AI关闭/不可用时开源规则流程完整可用。
6. 管理员可在网页新增/修改/测试/禁用AI供应商profile（供应商、协议、请求地址、模型、API Key），并分别绑定两项能力；默认MiniMax-M3/H3。任何GET/日志不回显Key。
7. 用户可在预览后提交自由文本+结构化修改意见；系统生成可确认修改计划，每次应用创建新revision并保留父子关系，至少连续完成两轮反馈仍可回退历史。
8. 用户可将满意revision标记final、下载和分享；同项目仅一个final，可撤销继续修改。
9. 输出为1080×1920 H.264/AAC MP4，包含确认后的风格、BGM、字幕、动效和转场。
10. yt-dlp/FFmpeg/OpenCV等工具在Linux受限执行，原始参考副本分析后删除；部分分析工具失败可降级并显示置信度/警告。
11. 所有首选开源候选实施时核验Star≥6000、许可、维护和安全；低于门槛必须书面例外。核心流程不依赖付费或不稳定免费额度API。
12. 页面持续显示分析/渲染状态，刷新和服务重启可恢复；非法URL/文件、超限、磁盘不足、工具/AI失败有稳定错误码且不泄密。
13. Ant Design是唯一主要UI库；Web主栈保持React/Vite/Express TypeScript，Python/Go/Rust仅作为Linux受控工具/容器。
14. `docs/oss-adoption.md`和第三方通知记录Star快照、版本/commit、许可证、最小复用模块、Linux方式、资源与替代方案。
15. Vitest/Supertest、工具契约、真实FFmpeg、前端组件和Playwright E2E全部通过。
16. README可让开发者启动Web与分析工具，在AI关闭状态完成“文字/图片→风格→创意视频→两轮反馈→final下载分享”。

---

# 实施计划

> **For agentic workers:** 实施前使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`，逐任务执行；每个步骤使用 `- [ ]` 跟踪。

**目标：** 交付Linux单服务器上的视频创作网站：文字/图片+预设/自定义/参考风格→系统自动创意视频→多轮反馈→final下载分享。  
**架构：** React/Ant Design调用Express TypeScript；Web服务通过受控adapters调用不限语言的高质量开源CLI/容器，分析参考风格并用FFmpeg渲染。AI供应商/地址/模型/Key可在网页配置，两项能力默认绑定MiniMax-M3/H3且可替换。  
**技术栈：** Node.js 22、Express 5、TypeScript、Zod、React/Vite/Ant Design、Sharp、FFmpeg/ffprobe、yt-dlp、Python分析容器（OpenCV/PySceneDetect/librosa，可选PaddleOCR/CLIP）、Vitest/Supertest/Playwright、Docker Compose。

## 12. 实施任务

### Task 1：初始化 npm 工作区、Express 和 Ant Design 基线

**文件：** 创建 `.gitignore`、`package.json`、`package-lock.json`、`tsconfig.base.json`、`eslint.config.js`、`README.md`、`THIRD_PARTY_NOTICES.md`、`docs/oss-adoption.md`、`frontend/package.json`、`frontend/vitest.config.ts`、`frontend/src/test/setup.ts`、`backend/package.json`、`backend/tsconfig.json`、`backend/vitest.config.ts`、`backend/src/app.ts`、`backend/src/server.ts`、`backend/src/config.ts`、`backend/tests/`、`data/.gitkeep`、`assets/fonts/LICENSES.md`、`assets/music/LICENSES.md`、`assets/backgrounds/LICENSES.md`及六类风格目录的`.gitkeep`。

- [ ] 确认本机安装 Node.js 22、npm、FFmpeg 7 和 Git；运行 `node -v && npm -v && ffmpeg -version && ffprobe -version`，四项均成功。
- [ ] `.gitignore`忽略node_modules、构建/模型缓存、data/projects/trash/system settings和参考临时文件；不创建任何AI密钥环境变量示例。
- [ ] 创建npm workspaces和共享TypeScript配置；所有依赖使用 `--save-exact`，提交唯一根lockfile。根开发依赖精确安装TypeScript、tsx、Vitest、ESLint及TypeScript插件、Prettier；后端安装Supertest及类型、Express类型；前端安装Testing Library、user-event、jest-dom和jsdom。
- [ ] 前端精确安装React、Vite、Ant Design、icons、TanStack Query；后端精确安装Express 5、Zod、Sharp、Busboy、file-type、undici及安全IP分类库。创建前后端Vitest配置、测试setup、ESLint配置和最小健康检查测试，禁止npm exec临时下载未锁定包。
- [ ] 前端在 `ConfigProvider` 中设置中文locale和主题token；后端创建最小 `app.ts`/`server.ts`，实现 `GET /health`。配置根脚本 `dev/build/typecheck/lint/test:backend/test:frontend`，每个命令只使用lockfile依赖。
- [ ] 创建 `assets/music/{celebratory,birthday,wedding,humorous,romantic,tech}`、`assets/backgrounds/`、`assets/fonts/`和许可证记录模板；建立OSS审计模板与第三方通知。
- [ ] README写明准确命令；运行 `npm ls --all` 确认无缺失/外部临时依赖，CI使用 `npm ci` 可从lockfile离线复现已缓存安装。
- [ ] 运行 `npm run typecheck && npm run build && npm run test:backend -- --run && npm run test:frontend -- --run`，预期全部退出码 0。
- [ ] 提交：`chore: initialize express and ant design workspace`。

### Task 2：领域模型、高星开源审计与多语言工具契约

**文件：** 创建 `backend/src/domain/schemas.ts`、`backend/src/tools/schemas.ts`、`scripts/audit-oss.ts`、`scripts/check-tools.ts`、`analysis-tools/requirements.lock`、`analysis-tools/analyze_reference.py`、`analysis-tools/tests/`；修改 `docs/oss-adoption.md`、`THIRD_PARTY_NOTICES.md`。

- [ ] 用Zod定义Project、ContentInput、StyleSelection、StyleProfile、StyleAnalysisJob、FeedbackPlan、AIProviderProfile、Storyboard、Revision；Python输出schema必须有相同版本和contract fixtures。
- [ ] 审计yt-dlp、FFmpeg、OpenCV、PySceneDetect、librosa、PaddleOCR、CLIP/OpenCLIP、MoneyPrinterTurbo、OpenMontage、Pixelle、hyperframes、video-use、NarratoAI、autoclip、Sharp和Ant Design；记录Star快照日期、是否≥6000、许可证、维护、安全、Linux方式、资源和复用模块。
- [ ] `audit-oss`默认拒绝Star<6000、归档、许可证不兼容或高危未解决项目；低Star例外必须含`exception_owner/reason/no_alternative/expiry`。flycut-caption、videoflow、short-video-factory不得默认进入依赖。
- [ ] Python分析依赖精确锁定且带hash；`analyze_reference.py`从stdin/文件读取固定job JSON，只向stdout输出版本化JSON，日志走stderr，不接受命令字符串。
- [ ] `check-tools.ts`验证Linux可执行文件/容器digest、版本范围、许可证清单和JSON schema；缺少可选OCR/CLIP只报告degraded，缺少yt-dlp/FFmpeg/OpenCV核心则capability不可用。
- [ ] 运行 `npm exec -- tsx scripts/audit-oss.ts --check --min-stars 6000 docs/oss-adoption.md && npm exec -- tsx scripts/check-tools.ts --contract-only && npm run test:backend -- --run backend/tests/domain backend/tests/tools`，预期通过。
- [ ] 提交：`feat: define domains and audited tool contracts`。

### Task 3：文件仓库、多AI供应商设置与安全HTTP客户端

**文件：** 创建 `backend/src/repositories/file-projects.ts`、`backend/src/repositories/system-settings.ts`、`backend/src/services/system-settings.ts`、`backend/src/integrations/safe-provider-http-client.ts`、`backend/src/integrations/ai-provider-registry.ts`、`backend/src/integrations/providers/minimax.ts`、`openai-compatible.ts`、`backend/src/api/system-settings.ts`、`backend/src/api/capabilities.ts`；测试对应repository/provider/API文件。

- [ ] 实现项目/设置原子仓库：Promise mutex、version CAS、临时文件+sync+rename；首次创建schema v2、0700目录/0600文件和不含Key的MiniMax默认profile/能力绑定。
- [ ] 实现provider profile CRUD、连接测试和能力绑定API；Key留空保持、替换后清空输入、GET只返回掩码。绑定/引用中的profile不能删除。
- [ ] 定义不可混用的`InternalProviderCredentials`和`PublicProviderProfile`；类型/运行时测试保证Key不能JSON序列化、记录或进入Express response。
- [ ] MiniMax和OpenAI-compatible adapters只允许固定协议字段，不接受任意header/模板代码；响应统一为prompt_optimizer/caption_effects schema。
- [ ] SafeProviderHttpClient校验HTTPS、无userinfo/query/fragment、模型/path字符；解析全部A/AAAA、拒绝非全球单播、固定IP/SNI、禁3xx、限制超时/响应体，覆盖DNS rebinding测试。
- [ ] capabilities返回高星工具版本/可用性与两项AI绑定的provider/model；设置变更热更新，无需重启。
- [ ] 运行 `npm run test:backend -- --run backend/tests/repositories backend/tests/integrations backend/tests/api/system-settings.test.ts backend/tests/api/capabilities.test.ts`，预期全部通过且无Key泄漏。
- [ ] 提交：`feat: add provider registry and secure file settings`。

### Task 4：实现项目 CRUD API

**文件：** 创建 `backend/src/api/projects.ts`、`backend/src/api/errors.ts`；修改 `backend/src/app.ts`；测试 `backend/tests/api/test_projects.ts`。

- [ ] API测试覆盖文字-only、图片-only、图文、全空拒绝；preset/custom/reference三种StyleSelection互斥；切换风格/画像、素材、反馈会清空确认，标题不清空。
- [ ] Express Router+Zod实现项目CRUD和style-selection；预设ID必须来自版本化库，自定义描述限制长度，reference必须指向已分析记录。
- [ ] PATCH只接受业务白名单字段；客户端不能直接设置accepted proposal、confirmed profile、final、工具来源或模型来源。
- [ ] 删除采用移入 trash；存在活动渲染时返回 `409 RENDER_ALREADY_ACTIVE`。
- [ ] 运行 `npm run test:backend -- --run backend/tests/api/test_projects.ts`，预期全通过。
- [ ] 提交：`feat: add project management API`。

### Task 5：实现文字/图片素材输入与规范化

**文件：** 创建 `backend/src/services/uploads.ts`、`backend/src/api/assets.ts`；测试 `backend/tests/services/test_uploads.ts`、`backend/tests/api/test_assets.ts`，素材 `backend/tests/fixtures/images/`。

- [ ] 测试JPG/PNG/WebP、伪文件/超限/像素炸弹/路径穿越，以及0图片+文字成功、图片+空文字成功、两者全空失败。
- [ ] Busboy流式写盘并用file-type+Sharp双检；文字按Unicode长度/控制字符/HTML清理规则校验，但保留用户原始纯文本语义。
- [ ] Sharp纠正EXIF、转sRGB、去元数据；无图片时生成器不创建伪上传，而在RenderPlan中创建text_card场景。
- [ ] 上传、删除、排序、文字或逐图文案变化均清空风格/分镜相关确认；历史revision引用文件保持不可变。
- [ ] 运行 `npm run test:backend -- --run backend/tests/services/test_uploads.ts backend/tests/api/test_assets.ts`，预期全通过。
- [ ] 提交：`feat: add validated image uploads`。

### Task 6：实现预设/自定义/参考视频风格分析

**文件：** 创建 `backend/src/tools/runner.ts`、`backend/src/tools/reference-analyzer.ts`、`backend/src/services/reference-styles.ts`、`backend/src/services/style-profiles.ts`、`backend/src/api/styles.ts`、`backend/src/api/references.ts`、`backend/src/jobs/analysis-queue.ts`；测试对应tools/services/api/jobs文件。

- [ ] 建立升学宴、生日宴、婚礼、浪漫、幽默、科技的版本化StyleProfile fixtures，测试preset/custom/reference三种来源统一输出schema。
- [ ] 实现参考URL校验和授权门禁：HTTPS、平台allowlist、单视频、≤5分钟/200MB、无Cookie/登录/DRM；所有参数数组调用，无shell。
- [ ] LinuxToolRunner以受限用户/容器执行固定版本yt-dlp/FFmpeg和Python分析器，限制CPU/内存/PID/文件/网络/超时，截断脱敏stderr，始终清理原视频。
- [ ] 合并OpenCV/PySceneDetect/librosa/PaddleOCR/CLIP输出；核心字段按置信度选取，OCR/CLIP等可选失败生成warning而不阻塞。
- [ ] 实现分析队列、进度/取消/重启恢复、profile编辑/确认API；画像未确认时Storyboard生成返回`STYLE_PROFILE_NOT_CONFIRMED`。
- [ ] 契约测试覆盖抖音/YouTube mock metadata、平台变更、playlist/超限/恶意URL、工具超时/畸形JSON/部分失败、临时文件零残留。
- [ ] 运行 `npm run test:backend -- --run backend/tests/tools backend/tests/services/reference-styles.test.ts backend/tests/api/references.test.ts backend/tests/jobs/analysis-queue.test.ts`，预期全通过。
- [ ] 提交：`feat: analyze and confirm video style profiles`。

### Task 7：接入可配置AI供应商的提示词优化与字幕/特效

**文件：** 创建 `backend/src/services/prompts.ts`、`backend/src/services/ai-content.ts`、`backend/src/api/prompts.ts`、`backend/src/integrations/ai-schemas.ts`；修改AIProviderRegistry；测试provider mocks、prompts、proposal API和ai-content。

- [ ] mock测试默认MiniMax-M3/H3绑定及切换到OpenAI-compatible profile/model后热生效；覆盖禁用、无Key、401/429/500、超时、非法schema和Key清除。
- [ ] 两个项目Switch默认关闭且独立；关闭时零AI请求。项目响应展示当前绑定的provider/model但不含地址敏感部分和Key。
- [ ] PromptService调用`prompt_optimizer`绑定，只产生proposed提案；accept校验digest，reject/关闭/超时不生效，修改原文使旧接受失效。
- [ ] AIContentService调用caption_effects绑定，只返回字幕和白名单motion/transition候选并标记provider/model；失败返回可供Task8 RulePlanner使用的降级信号，不直接修改BGM/图片/时长或尚未创建的storyboard。
- [ ] AIProviderRegistry只能读取InternalProviderCredentials并通过SafeProviderHttpClient；公开DTO/日志/Express response不存在apiKey字段。
- [ ] 运行 `npm run test:backend -- --run backend/tests/integrations/providers backend/tests/services/prompts.test.ts backend/tests/api/prompt-proposals.test.ts backend/tests/services/storyboard-ai.test.ts`，预期通过。
- [ ] 提交：`feat: add configurable AI capability bindings`。

### Task 8：实现自动创意分镜、反馈计划与RenderPlan

**文件：** 创建 `backend/src/services/storyboards.ts`、`backend/src/services/feedback.ts`、`backend/src/render/templates.ts`、`backend/src/render/plan.ts`、`backend/src/render/subtitles.ts`、`backend/src/api/storyboards.ts`、`backend/src/api/feedback.ts`；测试对应文件。

- [ ] 参数化测试六种预设、自定义、参考画像、图文/纯文字；断言生成creative_intent、完整四段叙事、BGM标签、字幕、特效理由和15–60秒时间线。
- [ ] 通过adapters复用审计通过的MoneyPrinterTurbo/OpenMontage/Pixelle/hyperframes独立算子；至少集成一个适配当前schema的高星分镜/模板算子。若全部因许可证/运行条件失败，先提交ADR和spike证据，再允许最小规则实现。
- [ ] 无图片时生成text_card和程序化背景场景；有图片时重排/裁切策略可解释且不丢素材引用。
- [ ] 生成时调用Task7 AIContentService（开关开启）并只合并字幕/白名单特效；关闭或失败时使用RulePlanner，输出记录实际provider/model或rule来源。
- [ ] 实现storyboard生成/保存/确认：必须存在已确认StyleProfile；任何素材/风格/AI/反馈变化使确认失效。
- [ ] 实现FeedbackService白名单操作、unsupported_text、before/after预览、accept/reject/apply；应用创建新草稿并记录parent revision，不改历史输出。
- [ ] RenderPlan验证确认摘要、StyleProfile、provider/model、工具版本、图片/BGM/字体摘要；生成安全ASS，用户/AI文本不进入filter表达式。
- [ ] 运行 `npm run test:backend -- --run backend/tests/services/storyboards.test.ts backend/tests/services/feedback.test.ts backend/tests/api/storyboards.test.ts backend/tests/api/feedback.test.ts backend/tests/render`，预期通过。
- [ ] 提交：`feat: generate creative plans and revision feedback`。

### Task 9：实现 FFmpeg 渲染管线

**文件：** 创建 `backend/src/render/pipeline.ts`、`backend/src/render/probe.ts`、`backend/src/render/text-cards.ts`、`scripts/check-assets.ts`；测试 `backend/tests/render/`；生产字体、背景和六类BGM放入已登记assets目录。

- [ ] 集成测试同时渲染纯文字升学宴、图文生日宴和参考画像视频；断言程序化背景、动态文字、字幕、BGM、转场、取消和错误清理。
- [ ] 使用spawn shell:false和AbortController调用FFmpeg；所有filter由白名单构建器生成。
- [ ] 实现图片contain/裁切、text_card渐变/纹理/几何动效、镜头motion/transition、ASS、BGM BPM匹配/循环/淡化和H.264/AAC编码。
- [ ] 测试 fixture 使用脚本生成的纯色图片和正弦波音频，不冒充生产曲库；生产环境引入 Noto Sans SC（SIL OFL）及每种风格至少 1 首经负责人确认可商用/再分发的 BGM，并按 Task 1 字段登记许可证和 SHA-256。
- [ ] 实现 `scripts/check-assets.ts`，运行 `npm exec -- tsx scripts/check-assets.ts --font assets/fonts/NotoSansSC-Regular.otf --music-root assets/music --min-tracks-per-style 1 --verify-digests`；预期六类风格均通过且无未登记文件。
- [ ] 音乐选择只在创建 revision 时按 9.3 的分类→目录映射、规范化路径排序和 SHA-256 取模执行，并把曲目/字体路径、文件摘要和许可证条目摘要冻结到 `render_input.json`；worker 禁止重新选曲，没有许可证记录或摘要不匹配时拒绝渲染。
- [ ] 解析 FFmpeg `-progress` 输出，将阶段进度限制在 0–99；只有 probe 验证成功后写 100。
- [ ] 用 ffprobe 断言输出：1080×1920、30 fps、H.264、有音频时 AAC、时长误差不超过 0.5 秒、`yuv420p`。
- [ ] 运行 `npm run test:backend -- --run backend/tests/render/test_pipeline.ts`，预期全通过。
- [ ] 提交：`feat: render vertical videos with ffmpeg`。

### Task 10：实现任务队列、恢复、取消与重试

**文件：** 创建 `backend/src/jobs/queue.ts`、`backend/src/api/renders.ts`；修改 `backend/src/app.ts`；测试 `backend/tests/jobs/test_queue.ts`、`backend/tests/api/test_renders.ts`。

- [ ] 测试分析/渲染队列互不覆盖、同类型并发1、取消/恢复；render拒绝未确认StyleProfile/storyboard/AI提案或过期反馈；排队后工具版本、provider/model、画像和资产均被冻结；retry逐字节复制。
- [ ] 实现统一JobSupervisor管理AnalysisQueue和RenderQueue；SIGTERM停止接单，用AbortController终止Node子进程，并让LinuxToolRunner终止整个进程组/容器。
- [ ] POST /renders锁内重构确认载荷，冻结StyleProfile、creative_intent、AI provider/model、反馈来源、工具/container版本、资产/BGM/字体摘要到render_input；worker不读取当前状态、不重新调用AI/分析工具。
- [ ] retry 在项目锁内逐字节复制源 revision 的 `render_input.json` 到新 revision，记录相同输入摘要后入队；当前草稿、设置和素材元数据不参与重试。
- [ ] 服务启动时按状态表恢复：`queued` 保持、`running` 回到 `queued`、`cancelling` 变 `cancelled`、终态不变。
- [ ] 提交前按 `输入规范化文件总大小 × 2 + 预估未压缩帧缓存上限 2 GB + 目标输出 500 MB` 计算所需空间；`max(该估值, 5 GB)` 加配置磁盘保留量后仍不足则返回 `INSUFFICIENT_STORAGE`。
- [ ] 实现提交、状态、取消和重试 API；保证同项目最多一个活动任务。
- [ ] 运行 `npm run test:backend -- --run backend/tests/jobs/test_queue.ts backend/tests/api/test_renders.ts`，预期全通过。
- [ ] 提交：`feat: add recoverable render queue`。

### Task 11：实现视频播放、下载和分享

**文件：** 创建 `backend/src/services/shares.ts`、`backend/src/api/media.ts`、`backend/src/api/shares.ts`；测试 `backend/tests/api/test_media.ts`、`backend/tests/api/test_shares.ts`。

- [ ] 测试Range、下载、分享轮换/撤销、revision lineage列表、设为final/切换final/撤销final；只有completed可final和分享。
- [ ] 视频端点只由项目/revision元数据解析路径；返回正确Content-Type/Length/Accept-Ranges/Content-Disposition。
- [ ] 实现revision对比摘要（输入/风格/反馈变化，不读取视频二进制）和final API；新final自动取消旧标记但不删除文件。
- [ ] 创建 32 字节安全随机 token，只保存 `SHA-256` 摘要并用 `crypto.timingSafeEqual` 比较；manifest 同时记录绑定的 `revision_id` 与创建时间。
- [ ] 分享 API 只返回标题、视频 URL 和下载能力，不返回项目编辑信息。
- [ ] 运行 `npm run test:backend -- --run backend/tests/api/test_media.ts backend/tests/api/test_shares.ts`，预期全通过。
- [ ] 提交：`feat: add video delivery and revocable sharing`。

### Task 12：实现 Ant Design 应用壳、系统设置和素材管理

**文件：** 创建 `frontend/src/app/theme.ts`、`frontend/src/app/AppShell.tsx`、`frontend/src/app/router.tsx`、`frontend/src/api/client.ts`、`frontend/src/api/types.ts`、`frontend/src/features/settings/SystemSettingsPage.tsx`、`frontend/src/features/projects/ProjectListPage.tsx`、`frontend/src/features/editor/AssetStep.tsx`；测试对应 `SystemSettingsPage.test.tsx`、`ProjectListPage.test.tsx`、`AssetStep.test.tsx`。

- [ ] 测试Ant Design导航、项目CRUD、文字-only/图片-only输入，以及AI供应商profile新增/编辑/掩码/测试/禁用/删除冲突和两项能力绑定；DOM/URL/缓存不含Key。
- [ ] AppShell菜单包含项目和系统设置；系统设置用Table+Drawer/Form管理supplier/protocol/base/Key/models/enabled，用Select绑定两项能力。默认显示MiniMax M3/H3，可新增OpenAI-compatible。
- [ ] API client统一错误/request ID和共享schema类型；Key字段标记sensitive，禁止进入TanStack Query缓存和全局错误边界。
- [ ] 素材编辑用Upload.Dragger和Input.TextArea，明确“图片可选、文字可选、至少一项”；提供缩略图、逐图文案和键盘排序。
- [ ] 运行 `npm run test:frontend -- --run frontend/src/features/settings frontend/src/features/projects frontend/src/features/editor/AssetStep.test.tsx && npm --prefix frontend run typecheck && npm --prefix frontend run build`，预期全通过。
- [ ] 提交：`feat: add ant design shell settings and asset UI`。

### Task 13：实现风格来源、画像、创意分镜和生成界面

**文件：** 创建 `StyleSourceStep.tsx`、`ReferenceAnalysisPanel.tsx`、`StyleProfileEditor.tsx`、`PromptOptimizer.tsx`、`StoryboardEditor.tsx`、`RenderStep.tsx`及对应测试。

- [ ] 测试三种风格模式互斥：预设Select含升学宴/生日宴，自定义Textarea，参考URL+授权Checkbox；切换来源清空旧确认。
- [ ] 参考面板展示分析阶段、取消/重试、部分失败warning、证据抽帧、主题/色卡/节奏/BPM/字幕区域/置信度；编辑并确认画像。
- [ ] 两个AI Switch显示当前绑定provider/model，默认未选中且独立；未配置时链接系统设置。提示词提案和字幕/特效维持确认门禁。
- [ ] StoryboardEditor先展示creative_intent和自动选择理由，再展示text_card/图片镜头、BGM、字幕、动效、转场；用户可编辑后确认。
- [ ] RenderStep仅在内容非空、StyleProfile与storyboard均确认时启用；展示冻结的风格来源、工具和AI来源。
- [ ] 运行 `npm run test:frontend -- --run frontend/src/features/editor frontend/src/features/render && npm --prefix frontend run typecheck && npm --prefix frontend run build`，预期通过。
- [ ] 提交：`feat: add style analysis and creative storyboard UI`。

### Task 14：实现预览反馈循环、final、下载和分享

**文件：** 创建 `ResultPage.tsx`、`RevisionCompare.tsx`、`FeedbackPanel.tsx`、`FeedbackPlanModal.tsx`、`SharePage.tsx`、`ShareActions.tsx`及对应测试。

- [ ] 测试播放器、revision树/对比/回退、自由文本和结构化反馈、unsupported澄清、计划接受/拒绝、两轮新revision、final切换/撤销、下载分享。
- [ ] FeedbackPanel提供节奏、字幕、配色、BGM、特效、转场、顺序/时长控件和备注；计划Modal显示before/after与受影响镜头，接受前不改项目。
- [ ] ResultPage显示9:16视频、创意意图、风格/AI/工具来源、warnings和父子版本；“设为满意版本”清晰区别于普通completed。
- [ ] 分享只指向指定completed/final revision，Web Share不可用时复制；撤销后旧token404，分享页不暴露参考资料/反馈原文。
- [ ] 运行 `npm run test:frontend -- --run frontend/src/features/render frontend/src/features/feedback frontend/src/features/share && npm --prefix frontend run build`，预期通过。
- [ ] 提交：`feat: add iterative feedback and final delivery UI`。

### Task 15：端到端测试与无障碍/兼容性检查

**文件：** 创建 `frontend/e2e/video-flow.spec.ts`、`frontend/playwright.config.ts`、`frontend/vitest.config.ts`、`frontend/src/test/setup.ts`、`scripts/smoke-test.ts`；修改 `frontend/package.json` 测试脚本。

- [ ] E2E A：纯文字+升学宴，AI关闭，系统生成创意/BGM/字幕/特效，完成两轮反馈并final下载分享。
- [ ] E2E B：图片+生日宴；验证素材重排、风格理由和BGM/特效。
- [ ] E2E C：mock抖音参考URL→分析画像→用户编辑确认→生成；断言原始参考文件删除、分享不含参考资料。
- [ ] E2E D：新增OpenAI-compatible供应商profile并重新绑定两项能力；Key仅掩码，Switch独立，提案/分镜确认正确；再切回MiniMax默认。
- [ ] 异常：未授权/恶意/超限URL、yt-dlp平台变化、分析部分失败、unsupported反馈、provider失败、确认过期、渲染失败、重启恢复和磁盘不足。
- [ ] `scripts/smoke-test.ts` 通过 API 完成最短流程，并用 ffprobe 校验下载文件。
- [ ] 在 Chrome、Safari 和移动视口验证上传、播放、下载；运行 axe 检查关键页面无严重无障碍错误。
- [ ] 运行 `npm run test:backend -- --run && npm run test:frontend -- --run && npm run typecheck && npm run build && npm --prefix frontend run e2e`，预期全部退出码 0。
- [ ] 提交：`test: cover complete video generation workflow`。

### Task 16：容器化、运维和数据保护

**文件：** 创建 `backend/Dockerfile`、`frontend/Dockerfile`、`analysis-tools/Dockerfile`、`deploy/nginx.conf`、`deploy/entrypoint.sh`、`deploy/tool-policy.json`、`docker-compose.yml`、`scripts/proxy-smoke-test.sh`、`scripts/tool-smoke-test.sh`、`scripts/backup_data.sh`；完善README。

- [ ] Linux镜像固定Node22、FFmpeg、yt-dlp和Python lock；默认分析工具含OpenCV/PySceneDetect/librosa，PaddleOCR/CLIP放`analysis-heavy`可选profile。记录二进制/镜像digest。
- [ ] Web主服务仍为Express；多语言工具只由LinuxToolRunner调用。使用非root用户、`prlimit/timeout`、只读根、no-new-privileges、临时卷配额和平台/provider出口策略；不挂Docker socket。
- [ ] Nginx托管Ant Design并代理Express，Basic Auth保护编辑/设置/API，只匿名health/share并保留Range。
- [ ] compose定义frontend-proxy、backend和可选analysis-heavy，密钥仅在settings数据卷；不声明AI环境变量。htpasswd通过Docker secret挂载，缺失/空时拒绝启动；Express继续Origin/CSRF校验。上传320MB、参考200MB、API/下载超时按设计设置。
- [ ] 备份默认包含项目manifest、uploads、StyleProfile、feedback plans、proposals和outputs，排除tmp原始参考与`settings.json`；`--include-secrets`才包含设置且输出0600警告。
- [ ] README说明Web主栈、Linux多语言工具、供应商profiles/能力绑定、配置文件权限、高星审计、参考合规、备份恢复和故障处理。
- [ ] 运行 `bash scripts/proxy-smoke-test.sh http://localhost`：未认证 `/` 和系统设置返回401，带凭据后200；`/health`匿名200；匿名分享API返回业务404而非401；生成视频后Range请求返回206；伪造Origin访问设置PUT被Express拒绝。
- [ ] `tool-smoke-test`验证yt-dlp mock、分析schema、资源终止和tmp零残留；`grep -REi "(API_KEY|TOKEN)=" docker-compose.yml */Dockerfile`无匹配；通过设置API配置测试provider后完成smoke视频。
- [ ] 提交：`chore: add production deployment and operations guide`。

### Task 17：发布验收

**文件：** 创建 `docs/release-checklist.md`、`docs/samples/README.md`、`scripts/release-check.ts`、`scripts/restart-recovery-test.ts`、`artifacts/.gitkeep`。

- [ ] `release-check`生成：纯文字升学宴、图文生日宴、婚礼、自定义风格、mock参考风格、AI可选样片；记录StyleProfile、creative_intent、ffprobe、工具版本、耗时和大小。
- [ ] 产品人工确认系统在简单输入下主动生成合理BGM/字幕/特效，且两轮反馈后变化符合计划；参考模式只提取抽象风格、不复制内容。
- [ ] `release-check --verify-licenses`验证音乐、背景、字体、测试素材和参考fixture的来源、许可、日期和摘要。
- [ ] 在目标规格服务器运行 `npm exec -- tsx scripts/release-check.ts --repeat 10 --assert-success-rate 1.0 --assert-no-active-jobs`，预期 10 次全部成功、无崩溃和无遗留活动状态，证据写入 `artifacts/release/summary.json`。
- [ ] 运行 `npm exec -- tsx scripts/restart-recovery-test.ts`，预期 queued/running/cancelling 按状态表恢复；运行 `bash scripts/backup_data.sh create artifacts/backup.tar.zst && bash scripts/backup_data.sh restore-test artifacts/backup.tar.zst`，预期恢复副本校验和一致。
- [ ] `audit-oss --min-stars 6000`验证所有默认候选；例外字段完整；许可证/版本/container digest/工具JSON契约均通过。
- [ ] 在系统设置创建MiniMax与mock OpenAI-compatible profiles，分别换绑并测试两项能力，GET只掩码；清除Key后开源规则流程仍完成。
- [ ] 运行 `npm run test:backend -- --run && npm run test:frontend -- --run && npm run typecheck && npm run build && npm --prefix frontend run e2e`，预期全部退出码0；验证系统设置脱敏/权限、磁盘不足、分享撤销、项目删除和日志脱敏证据均写入release checklist。
- [ ] 对照“11.2 MVP 完成定义”逐项签字并提交：`git add docs/release-checklist.md docs/samples artifacts/release/summary.json artifacts/release/*.probe.json && git commit -m "docs: add v0.1.0 release evidence"`；生成的 MP4 样片保留在服务器但不提交 Git。
- [ ] 全部通过后执行 `git tag -a v0.1.0 -m "video generator v0.1.0"`；运行 `git status --short` 预期无输出，`git tag --points-at HEAD` 预期包含 `v0.1.0`。

## 13. 推荐实施顺序与里程碑

| 里程碑 | 包含任务 | 可演示结果 |
|---|---|---|
| M1：基础与素材 | Task 1–5 | Web主栈、供应商设置、文字/图片输入和高星工具契约 |
| M2：风格与创意 | Task 6–8 | 预设/自定义/参考画像、可选AI、自动创意分镜与反馈计划 |
| M3：视频产出 | Task 9–11 | 纯文字/图文渲染、队列恢复、final、下载分享 |
| M4：完整网站 | Task 12–14 | 系统设置、风格分析、创意预览和多轮反馈闭环 |
| M5：可发布 | Task 15–17 | E2E、Linux工具隔离、授权和发布证据 |

依赖关系：M1→M2→M3→M4→M5。Task 12可在Task3–5 API稳定后开始；Task13等待Task6风格分析、Task7能力绑定和Task8分镜契约；Task14等待Task8反馈及Task11 final/share。工具版本或schema变化必须先更新contract fixtures和OpenAPI类型。

## 14. 后续演进（不进入 v0.1.0）

1. **v0.2：** TTS 旁白、字幕按音频强制对齐、更多模板、项目复制。
2. **v0.3：** PostgreSQL + 对象存储 + Redis/BullMQ，多用户、登录和配额。
3. **v0.4：** 接入经审核的 AI 作曲/文生视频 API；采用 provider adapter，不改变项目/修订领域模型。
4. **v0.5：** 可视化时间线；字幕编辑继续复用 v0.1 审计通过的最小组件，若当时未采用 flycut-caption 则重新审计其新版本。
5. **更后期：** 社媒 OAuth/发布；需单独评估平台协议、审核机制、账号安全和合规，不复用当前公开分享 token 作为平台凭证。

## 15. 参考视频与创意质量校准

参考链接解析属于正式功能，但平台随时可能变更，验收必须同时准备：授权且可解析的公开链接、相同内容的合法本地fixture、不可解析/超限/需登录反例。系统验收关注抽象画像与成片适配，而非像素级复刻：

- 主题、主色、明暗、饱和度和节奏是否与参考的高层特征一致；
- BGM标签/BPM、字幕区域和特效强度是否合理但没有复制原音乐/文字；
- 系统对升学宴、生日宴和简单文字能否主动给出完整创意，不要求用户懂镜头语言；
- 用户反馈是否在下一revision准确体现，未提及内容不应无故大改；
- 平台解析失败时是否顺畅降级到自定义描述或合法文件fixture。
