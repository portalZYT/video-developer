# 视频生成网站设计及实施任务（v0.2）

> **文档状态：** 可执行基线（2026-08-05 重构）
> **目标读者：** 产品、设计、前后端、测试与部署人员
> **v0.2 主要变化（相对 v0.1）：** AI 能力从 2 个固定 slot 升级为 `CapabilityRegistry`（4 个内置能力：planner / prompt_optimizer / caption_effects / feedback_parser）；新增 `StorageBackend` 抽象（默认 `LocalFileSystemAdapter`，可选 `AliyunOSSAdapter`，附 `MemoryStorageAdapter` 测试替身）；新增 `ProjectRepository` DB seam；移除分享功能（`is_final` 标记保留为"用户满意版本"）；新增资深剪辑师视角的编辑器契约（§9C）与资深创意师视角的 CreativePlan 规范（§9B）。
> **实施约定：** 执行任务时使用任务中的复选框跟踪；按任务顺序实施和验收，不并行修改同一模块。

## 1. 结论摘要

首版建设一个**单机 Linux 部署、无数据库、面向竖屏图文短视频**的网站。用户可以只提供文字，也可以上传图片并填写文字；视频风格可选择预设（如升学宴、生日宴），填写自定义描述，或提交已获授权的参考视频链接。系统分析素材与风格后自动加入创意分镜、字幕、转场、画面动效和合规 BGM，生成可预览的 MP4；用户可反复提出修改意见并生成新 revision，满意后下载（首版**不做分享链接**，仅保留本地下载与"满意版本"标记）。

推荐方案：

- **AI 能力注册表：** 系统内置 `CapabilityRegistry`，v0.1 锁定四个能力 ID——`planner`（视频策划师/创意设计师，默认绑定 MiniMax-M3，输出结构化 `CreativePlan`）、`prompt_optimizer`（提示词优化，默认绑定 MiniMax-M3，须预览/接受门禁）、`caption_effects`（字幕/特效，默认绑定 MiniMax-H3，须进入分镜确认）、`feedback_parser`（自由文本反馈解析成白名单修改操作，默认绑定 MiniMax-M3）。每个能力有独立的 provider / 模型绑定、独立的项目级 Switch、独立的 gate / fallback；管理员在系统设置页按"能力场景"绑定供应商和模型。**所有能力 v0.1 默认关闭**。
- **前端：** React + TypeScript + Vite + Ant Design；业务页面统一使用 Ant Design 组件和设计令牌，不并行引入第二套通用 UI 库。
- **后端：** Express 5 + TypeScript；Express 是 Node.js 生态最广泛采用、概念少且易理解的 API 框架，前后端统一语言便于维护。
- **集成工具：** 部署目标是 Linux。Web 前端/后端语言保持不变，但第三方开源工具不限语言，可将 Python、Go、Rust、C/C++ 项目作为固定版本 CLI、受限子进程或独立容器接入；统一经 TypeScript adapter 调用，不把第三方内部模型泄漏到业务 API。
- **渲染：** FFmpeg 为核心渲染器，Sharp 做图片预处理；参考视频解析/风格分析优先复用 yt-dlp、OpenCV、PySceneDetect、librosa、PaddleOCR/CLIP 等高质量免费开源能力，实施前核验许可证与 Star。
- **存储：** 通过 `StorageBackend` 接口抽象，默认 `LocalFileSystemAdapter`（0700/0600 文件权限），可选 `AliyunOSSAdapter`（阿里云 OSS，STS 临时凭证优先、静态 AccessKey 兜底，需管理员二次确认）；每个项目使用 `manifest.json` 保存状态和版本，**不引入数据库**，但 `ProjectRepository` 接口留 DB seam（v0.3 接入 `DbProjectRepository`）。
- **任务：** 进程内 `AnalysisQueue` 和 `RenderQueue` 默认各并发 1，限制参考分析与 FFmpeg 资源，避免耗尽 Linux 服务器。
- **BGM：** 首版根据风格**自动选择并裁剪本地已授权曲库**。语言模型不能直接生成音频，不把"选曲"误称为"AI 作曲"；真正的音乐生成 API 放到后续版本。

## 2. 已知条件、假设与范围边界

### 2.1 已知条件

1. 当前目录只有需求与候选项目资料，尚无代码，也不是 Git 仓库。
2. 首版不使用数据库，部署到 Linux 单服务器，上传数据、风格画像和 revisions 保存到应用数据目录；`ProjectRepository` 接口已抽象，DB 实现为占位（`DbProjectRepository`），v0.3 接入。
3. Web 主栈固定为 React + TypeScript + Vite + Ant Design，以及 Express 5 + TypeScript；集成工具语言不限。
4. AI 配置由管理员在系统设置网页管理，保存到 `data/system/settings.json`（schema_version=3）。v0.1 锁定四个内置 capability ID；管理员可改绑定（供应商 + 模型）、测试连接、切换 `enabled_by_default`，**但 v0.1 不允许新增 capability**（新增 capability 需代码新增 adapter）。四个项目级 capability Switch 默认关闭。
5. 存储后端由管理员在系统设置选择（local 默认 / aliyun_oss 可选）；OSS 配置（endpoint、bucket、region、prefix、credentials_mode、AK 或 STS role、signed URL TTL）保存在 `settings.json` 的 `storage` 段；切换 backend **不做自动迁移**，需运行 `scripts/storage-migrate.ts` 显式导入导出。
6. 参考视频链接仅用于提取主题、色调、节奏、字幕布局和动效强度等抽象风格，不复制原视频素材、音乐、台词、水印或可识别创作表达。用户必须确认拥有分析权限并遵守平台条款。
7. "免费"定义为可自托管的开源软件或无需付费即可运行的本地模型；云 API 的免费额度和条款会变化，不能承诺永久免费，因此不作为核心流程依赖。
8. **首版不做分享功能。**`is_final` 标记保留（"用户满意版本"），但与任何外部分享无关。

### 2.2 首版默认产品参数

以下参数作为可直接开发的默认决策；拿到参考视频样片后只调整模板参数，不改整体架构：

| 项目 | 首版默认值 |
|---|---|
| 输出 | MP4，H.264 + AAC，1080×1920，30 fps，`yuv420p` |
| 时长 | 15–60 秒；按文字量和图片数自动计算 |
| 图片 | 0–20 张；JPG、PNG、WebP；单张不超过 15 MB；无图片但有文字时生成排版文字卡与程序化背景 |
| 文字 | 图片和文字至少提供一项；总计不超过 5,000 字符，每张图可有独立文案 |
| 风格来源 | 三选一：预设下拉、自定义描述、参考视频链接 |
| 预设风格 | `school_banquet`升学宴、`birthday`生日宴、`wedding`婚礼、`romantic`浪漫温馨、`humorous`轻松幽默、`tech`科技感 |
| 参考视频 | 支持 allowlist 平台的公开链接；默认最长 5 分钟、最大 200 MB、单视频；分析后展示风格画像供确认 |
| 反馈修订 | 每次反馈生成结构化修改方案，用户确认后创建新 revision；次数不设产品上限，但受磁盘/任务限制 |
| 动效白名单 | `kenburns_in`/`kenburns_out`/`kenburns_pan`/`push_in`/`push_out`/`pan_left`/`pan_right`/`particles_drift`/`geometric_rotate`/`static` |
| 转场白名单 | `fade`/`slide_left`/`slide_right`/`zoom_cut`/`none` |
| 转场时长 | 升学宴/生日宴/婚礼 = 0.6–0.8s；轻松幽默/科技感 = 0.4–0.5s；浪漫温馨 = 0.7–0.9s；最大不超过相邻镜头时长 25% |
| 字幕 | 底部安全区内（1080×1920 下边距 12%），白色 + 黑色 2 px 描边，1–2 行，中文 ≤18 字/行、英文 ≤36 字/行 |
| 音频 | 自动匹配本地已授权 BGM，循环/裁剪至视频长度，淡入淡出各 1.0s，响度归一化 -16 LUFS |
| UI 组件库 | Ant Design React；导航、表单、开关、上传、步骤、卡片、表格、弹窗、通知、进度统一使用其组件 |
| **planner 能力** | 独立 Switch，默认未选中；默认绑定 MiniMax-M3，可在系统设置换供应商/模型；输出结构化 `CreativePlan`（含 `creative_intent` / `narrative_arc` 4 段 / `emotional_curve` / `bookend` / `bgm_rationale` / 2–3 个 `directions` / `scene_plan`），**两步门禁**：方向选择 + 方案确认 |
| **prompt_optimizer 能力** | 独立 Switch，默认未选中；默认绑定 MiniMax-M3；输出候选提示词，用户预览并接受才生效 |
| **caption_effects 能力** | 独立 Switch，默认未选中；默认绑定 MiniMax-H3；输出字幕/动效候选，必须进入分镜确认 |
| **feedback_parser 能力** | 独立 Switch，默认未选中；默认绑定 MiniMax-M3；将自由文本反馈解析为白名单修改操作；结果必须用户接受 |
| 预览 | planner 先输出 `creative_intent` 与可选方向，再生成 `scene_plan`；用户确认后 `StoryboardService` 接受为草稿；完成渲染后网页播放器预览；首版不做浏览器内实时逐帧预渲染 |
| 修改 | 修改文字、顺序、风格或字幕后创建新修订并重新渲染 |
| 用户体系 | 首版无注册、账号和团队协作；编辑后台仅供单运营者/受信任内网使用 |
| 访问边界 | 生产反向代理必须用 Basic Auth 保护 SPA 与项目 API；匿名访问仅放 `/health`（仅返回 `{"status":"ok"}`），不暴露 `/share/*`、不暴露项目 API |
| 存储后端 | 默认 `LocalFileSystemAdapter`（0700/0600 权限）；`AliyunOSSAdapter` 作为可选后端（管理员在系统设置切换） |
| OSS 凭证 | 优先 STS 临时凭证（≥15 分钟过期自动续签）；静态 AccessKey 兜底，需管理员二次确认 + warning；signed URL TTL 默认 3600s（范围 60–86400s） |
| 部署 | 单实例、单机磁盘；不承诺多实例共享状态和水平扩容 |

### 2.3 首版不做

- 文生视频、数字人、口型同步、声音克隆、AI 图片生成。
- **视频分享链接与社媒 OAuth/发布**（包括 Web Share API 调用、share token、第三方平台对接）。
- 浏览器时间线剪辑器、逐帧特效编辑、多人协作。
- AI 原创作曲；只使用来源和商用授权明确的本地曲库。
- 数据库、Redis、分布式队列、GPU 集群（DB 接口已抽象，v0.3 接入）。
- 对象存储强制依赖（OSS 为可选；v0.1 默认本地）。
- 未经授权下载、绕过登录/DRM/风控、去水印、保存或重新发布参考视频；授权参考仅临时下载低清分析副本，提取抽象风格后按保留策略删除。
- 对参考作品做像素级复刻，或复制其原始画面、音乐、台词、人物肖像和受保护模板。
- 在 v0.1 通过配置新增 capability（必须代码新增 adapter）。

## 3. 用户与核心流程

### 3.1 目标用户与核心价值

目标用户是只有简单提示词、文字或少量图片，不熟悉剪辑，也不知道最终视频应该采用什么视觉表达的内容创作者和活动运营人员。

**用户痛点：** 用户能描述"升学宴""生日祝福"等目标，却难以把描述转换成完整的镜头节奏、配色、BGM、字幕样式、特效和转场，也难以一次说清成片要求。

**系统亮点：** 用户只需提供素材/文字和视频风格，系统即主动完成创意理解并生成可解释的 `CreativePlan`（含 `creative_intent` / 4 段叙事弧 / 情绪曲线 / BGM 理由 / 2–3 个创意方向 / `scene_plan`），自动匹配合规 BGM、字幕、特效和转场；生成后通过"预览—反馈—修改计划—新 revision"循环持续调整，直到用户明确标记满意。AI 能力（planner / prompt_optimizer / caption_effects / feedback_parser）只在用户主动选择时辅助，默认开源规则流程也必须完成上述价值闭环。

### 3.2 主流程

**可选前置配置：** 只有需要 AI 能力时，管理员进入"系统设置"，填写 API Key、确认四个能力的供应商/模型绑定并测试连接。没有 Key 时四个 capability Switch 显示不可用，但项目的规则生成流程保持可用。存储后端默认 local；切换到 OSS 需填写 endpoint / bucket / credentials。

1. **创建项目：** 输入标题。
2. **提供内容：** 上传 0–20 张图片并填写逐图/总文案，或只填写文字；二者至少有一项。
3. **选择风格来源：** 预设下拉（升学宴、生日宴等）、自定义描述、参考视频链接三选一。参考链接需勾选"我有权分析此内容"。
4. **确认风格画像：** 参考链接解析后展示主题、主色、明暗/饱和度、镜头节奏、运动强度、字幕布局、BPM/音乐情绪及置信度；用户编辑并确认后才可继续。
5. **选择启用的 AI capability 组合：** 项目页显示四个独立 Switch（planner / prompt_optimizer / caption_effects / feedback_parser）；未启用则对应阶段使用开源规则实现；每个 Switch 显示当前系统级绑定（provider/model）的脱敏快照。
6. **系统创意方案：** 系统调用启用的 `planner` capability，输出结构化 `CreativePlan`：先 `creative_intent`（主题 + 4 段叙事弧 opening/development/climax/resolution + mood）+ 情绪曲线 + BGM 理由 + 2–3 个可选创意方向；用户选 1 方向后展开完整 `scene_plan`（每镜素材/字幕/动效/转场/对应情绪点 + bookend 首尾呼应 + 整体理由）。若 `planner` 关闭或失败，使用 `RulePlanner` 生成等价结构化默认方案。系统为每项自动选择给出理由并允许编辑。
7. **确认并渲染：** 用户编辑并确认 `scene_plan` 后提交异步任务；系统冻结全部输入、确认的 `CreativePlan`、工具版本与 capability 绑定，生成 MP4。
8. **预览与反馈：** 用户播放成片并填写修改意见，也可调整节奏、字幕、配色、BGM、转场、图片顺序和时长。若 `feedback_parser` 开启，系统同时把自由文本解析成白名单修改操作并给出置信度；否则使用有限意图词典。
9. **反馈计划与新 revision：** 系统把反馈（结构化控件 + 已解析的自由文本）转成可预览的结构化修改计划；用户接受后更新草稿、重新确认并生成新 revision。重复 8–9 直到满意，历史版本可对比和回退。
10. **最终交付：** 标记满意版本为 `final`，下载 MP4。**首版不开启任何分享链接。**

### 3.3 异常流程

- 参考链接不受支持、需要登录、平台解析变化或下载失败：不尝试绕过限制，提示用户改用自定义风格描述；测试环境可使用项目内合法 fixture 验证分析器。
- 参考视频超过 5 分钟/200 MB、含播放列表或重定向到非 allowlist 域：分析前拒绝。
- 风格分析某一工具失败：返回已成功提取的字段与置信度，允许用户手工补全；核心生成不因 CLIP/OCR/节拍分析缺失而阻塞。
- 自由文本反馈无法可靠映射：不猜测；若 `feedback_parser` 关闭，进入 `unsupported_text`；若开启则展示每个操作的置信度，由用户确认。
- 不合法图片：上传阶段立即拒绝并说明格式、尺寸或容量问题。
- **planner 输出 Zod 校验失败或 capability 不可用：** 降级到 `RulePlanner`，生成等价 `CreativePlan`，UI 显示 warning 但允许继续。
- **CreativePlan 输入变化（素材/文字/风格/AI 开关/feedback 应用）：** 自动标记 `stale`，禁止渲染，必须重新生成并确认。
- AI 供应商/绑定未配置、模型不可用、超时或返回格式错误：对应 capability 降级到 fallback；不自动换供应商/模型或接受提案。
- 存储后端不可用（local 写失败 / OSS 403/404/Network）：返回 `STORAGE_BACKEND_UNAVAILABLE`，任务不进入 `running`。
- OSS 凭证过期（STS）：自动续签一次，仍失败则 `OSS_PERMISSION_DENIED`。
- FFmpeg 失败：任务标记为 `failed`，保存脱敏后的错误摘要；用户可原参数重试。
- 服务重启：按 §7.4 状态表恢复；`queued` 保持、`running` 重新排队、`cancelling` 结束为 `cancelled`。
- 磁盘空间不足：提交渲染前拒绝新任务并提示清理项目；不在渲染中途静默删除数据。

## 4. 方案比较与选型

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| React + Ant Design + Express 5 + FFmpeg + Capability Registry + Storage Backend 抽象 | 前后端统一 TypeScript；Express 生态成熟、概念少；Ant Design 覆盖管理后台组件；FFmpeg 可控；能力与存储可插拔 | 仍需自行实现模板和任务状态 | **首版采用** |
| React + NestJS + FFmpeg | 模块/依赖注入规范完整 | 对当前单体 MVP 装饰器和抽象偏多，不符合简洁目标 | 项目复杂化后再评估 |
| React + Express + Remotion/HTML 渲染 | 前后端统一 TypeScript，画面组件化 | Chromium 渲染资源占用高；音视频细节仍依赖 FFmpeg | 仅作为后续模板引擎实验 |
| 集成 MoneyPrinterTurbo/Pixelle/OpenMontage/hyperframes 等 | 现成分镜/流水线丰富 | 整体依赖复杂 | **只复用审计通过的独立模块/算子，不整体接管 Web 系统** |
| 接入阿里云 OSS / 其他对象存储 | 大文件归档、跨实例共享、签名 URL 直传 | 引入凭据管理与 SSRF 防护复杂度 | **首版作为可选后端；通过 `AliyunOSSAdapter` 与 `StorageBackend` 接口接入；local 仍为默认** |

### 4.1 开源优先、6000 Star 门槛与最小功能策略

先拆成最小功能，再选择工具。候选默认须在实施时复核 **GitHub Star ≥ 6,000**、未归档、近 12 个月有维护、许可证允许当前分发方式、Linux 可自动化部署、无不可接受高危漏洞。Star 只是初筛，不替代代码/许可证审计；低于 6000 只允许在没有高质量替代且负责人书面批准时采用。第三方语言不限，Python/Go/Rust/C++ 工具通过版本锁定的 CLI 或独立容器接入，Web 主栈仍是 React/Express TypeScript。

本项目自写代码只负责产品流程、数据契约、权限/安全、工具编排、确认门禁和无法由现成工具覆盖的薄适配层；视频下载、分析、镜头检测、OCR、节拍、转码、字幕烧录和尽可能多的分镜/模板算子必须复用审计通过的开源工具。若某单元最终自研，`docs/oss-adoption.md` 必须列出已评估候选、失败证据和最小自研范围。

| 最小功能 | 高质量免费候选 | 计划用法 | 禁止/降级 |
|---|---|---|---|
| 参考链接元数据与授权范围内的临时低清下载 | `yt-dlp/yt-dlp`（约 182k） | Linux CLI：先 `--dump-single-json`，再按需 `--no-playlist` 下载受限分析副本 | 不传用户 Cookie、不绕登录/DRM；失败改用自定义风格 |
| 抖音等平台备用解析 | `putyy/res-downloader`（约 18.9k）、`MediaCrawler`（约 59.7k） | 仅在 Linux 无头模式、许可证和安全审计通过时作为平台 adapter；默认仍 yt-dlp | 不运行桌面抓包 GUI，不采集评论/账号数据 |
| 媒体探测、抽帧、转码、镜头差异、最终渲染 | FFmpeg/ffprobe（高 Star、行业标准） | 直接采用 CLI，贯穿分析与渲染 | 禁止 shell 拼接和任意 filter 输入 |
| 色调、亮度、饱和度、运动量、画面构图 | OpenCV（高 Star） | Python 分析容器读取采样帧，输出固定 JSON schema | 失败时退回 FFmpeg 统计与手工编辑 |
| 镜头边界与节奏 | PySceneDetect（实施时确认 ≥6k） | Python CLI/库输出镜头时间点、平均镜长、切换频率 | 不把整套 UI 引入 Web |
| 音频 BPM、onset、能量/情绪特征 | librosa（实施时确认 ≥6k） | Python 分析容器输出 BPM、能量曲线和标签；不保留原音频 | 失败时根据镜头节奏和预设选择 BGM |
| 参考视频字幕区域与密度 | PaddleOCR（约 86.9k） | 可选 Python 容器分析采样帧，只输出区域/密度/字号级别 | 不复制参考字幕正文；CPU 成本高时允许关闭 |
| 主题语义标签 | OpenAI CLIP 或 OpenCLIP（高 Star、免费本地模型） | 可选本地推理，对抽帧从预定义安全标签集打分 | 不生成/复制画面；无模型时由用户确认主题 |
| 图文到分镜/自动视频流程 | MoneyPrinterTurbo（约 101k）、OpenMontage（约 44.8k）、Pixelle-Video（约 26.4k）、hyperframes（约 39.3k） | 审计后优先复用独立的分镜、模板或时间线模块；以 adapter 转换为本项目 schema | 不整体启动其账号/数据库/Agent 系统，不同时引入重复流水线 |
| 自动剪辑/反馈修改参考 | `video-use`（约 18.6k）、NarratoAI（约 10.6k）、autoclip（约 6.3k） | 参考或复用独立剪辑算子；每个算子必须有确定输入输出和无模型降级 | 不允许 Agent 直接执行未审查命令 |
| 字幕格式与烧录 | Subtitle Edit（约 13.7k）行为测试 + FFmpeg/libass | 复用格式兼容测试思路，运行时由 ASS 生成器和 FFmpeg 完成 | 不部署 C# 桌面应用 |
| 图片规范化 | Sharp/libvips（高 Star） | Node 库直接处理 EXIF、色彩和尺寸 | 不扩展无关 AI 修图 |
| **对象存储/大文件归档** | **阿里云 OSS（官方 SDK 高 Star）** | **`AliyunOSSAdapter` 包 `StorageBackend`；默认不开；管理员在系统设置填写 endpoint/bucket/STS 或 AK；走 SSRF 防护、signed URL TTL、加密本地缓存** | **不引入除阿里云 OSS 之外的云存储；local 始终可用且为默认** |

`flycut-caption`（约 1.7k）、`videoflow`（极低 Star）、`short-video-factory`（约 5.1k）低于默认门槛，不再作为 v0.1 首选依赖；只有审计证明不可替代并书面例外时才能采用。

### 4.2 免费 API 与成本边界

- v0.1 核心链路只依赖可自托管开源工具，不依赖付费视频生成 API。
- 可以为外部视频生成 API 保留 provider 接口，但只有在许可证/服务条款允许、无需付费或有可验证长期免费方案、用户明确启用时接入；"注册赠送额度"不视为永久免费。
- 自托管软件免费不代表运行无成本：CPU/GPU、磁盘和带宽仍由部署者承担。CLIP/PaddleOCR 等重工具为可选分析能力，资源不足时降级而非阻塞。
- AI 能力以注册表方式管理：v0.1 锁定四个 capability ID（planner / prompt_optimizer / caption_effects / feedback_parser），管理员只能改绑定、测试连接、切换 `enabled_by_default`，不能新增 capability。新增 capability 需代码新增 adapter 与 Zod schema。
- 所有 AI 调用都通过 capability adapter；adapter 只接受协议允许的字段，不接受任意 header / 模板代码 / 自由路径。
- **OSS 凭证：** 优先 STS 临时凭证（自动续签）；静态 AccessKey 作为兜底，需管理员二次确认 + warning；不把长期 AK 写入代码、镜像或日志。`SafeProviderHttpClient` 与 OSS 出口走不同的 SSRF/DNS 防护策略（OSS 白名单放宽到阿里云公网 endpoint）。
- 所有采用/拒绝决定写入 `docs/oss-adoption.md` 和 `THIRD_PARTY_NOTICES.md`，记录 Star 快照日期、精确版本/commit、许可证、复用模块、Linux 部署方式、资源需求、风险和替代方案。

## 5. 产品界面设计

### 5.1 页面

1. **项目列表 `/`**
   - 创建项目入口。
   - 项目卡片展示封面、标题、更新时间、最新渲染状态。
   - 支持打开和删除；删除需二次确认。
2. **项目编辑 `/projects/:projectId`**
   - 步骤条：**素材 → 风格来源 → 风格画像 → 创意方案（planner） → AI 设置（其余 capability） → 生成 → 预览反馈 → 最终交付**。
   - 素材区：支持只填文字、只传图片或二者结合；图片拖拽、缩略图、排序、删除、逐图文案。
   - 风格来源区：使用 `Radio.Group` 三选一；预设模式用 `Select`（升学宴、生日宴、婚礼等），自定义模式用 `Input.TextArea`，参考模式用 `Input` 填写 URL、权限确认 `Checkbox` 和"分析风格"按钮。
   - 风格画像区：使用 `Descriptions`、色卡、节奏图、`Tag` 和 `Form` 展示/编辑主题、色调、节奏、字幕/BGM/特效建议、置信度和工具来源；必须确认。
   - **创意方案（planner）区：** 展示 `creative_intent`（主题 / 4 段叙事弧 / mood）+ 情绪曲线图 + BGM 理由 + **2–3 个可选方向卡**（每个方向有标题 / 概述 / 视觉关键词 / 差异化点）；用户选择方向后展开完整 `scene_plan`（每镜含 beat / start / duration / 字幕 / motion / transition / 情绪点 + bookend 首尾呼应）+ 自动选择理由；任意编辑可标记 `user_modified`。
   - **AI 设置区：** 四个独立 Switch（planner / prompt_optimizer / caption_effects / feedback_parser），每个 Switch 旁显示当前系统级绑定（provider/model）的脱敏快照与未配置/降级状态；未配置时链接跳转到系统设置。
   - 生成区：使用 `Progress`、`Alert` 和 `Button` 展示分析/渲染阶段、警告和取消操作。
3. **预览反馈页 `/projects/:projectId/revisions/:revisionId`**
   - 9:16 播放器、revision 对比/回退、错误警告、下载按钮。
   - 修改意见区同时提供自由文本与结构化控件：更快/更慢、字幕、色调、BGM 情绪/音量、特效强度、转场、镜头顺序/时长。
   - **若 feedback_parser 启用：** 反馈面板显示解析出的白名单操作列表与每个操作的置信度；用户接受后应用到新草稿；不接受的文本进入 `unsupported_text`。
   - 系统生成修改计划并展示受影响镜头/字段；用户接受后创建新草稿和 revision，拒绝则不改变当前版本。
   - "设为满意版本"将 revision 标记为 `final`；`final` 仍可撤销后继续修改。
4. ~~分享页 `/share/:token`~~ **已移除**（v0.2 起不做分享功能）。
5. **系统设置 `/settings`**
   - 顶部/侧边 `Menu` 提供"系统设置"，仅受信任管理员可进入。
   - **"AI 供应商"** 使用 Ant Design `Table`+`Drawer/Form` 管理多个 profile：供应商名称、协议（MiniMax / OpenAI-compatible）、HTTPS 请求地址、API Key、可用模型列表、启用状态。
   - **"能力绑定"** 用 Ant Design `Table`+`Form` 管理四个 capability 行：`capability_id`（来自 `CapabilityRegistry` 内置枚举，只读）、启用的供应商 profile、启用的模型、`enabled_by_default`、场景说明。默认绑定 planner / prompt_optimizer / feedback_parser → MiniMax-M3，caption_effects → MiniMax-H3，全部 `enabled_by_default=false`。`Input.Password` 设置 `autoComplete="new-password"`；Key 不进入 URL、日志、浏览器存储、Query 缓存或错误上报。
   - **"存储后端"** 区：Radio.Group 选择 `local`（默认）或 `aliyun_oss`；选择 OSS 时显示 endpoint、bucket、region、prefix、credentials_mode（`static_access_key` / `sts_role`）、对应字段、signed URL TTL。提交时 Key/AccessKeySecret 走 `Input.Password`；选择 `static_access_key` 时强制二次确认 + warning。
   - profile GET 只返回 configured 及 Key 掩码；编辑时 Key 输入初始为空；留空保持，提供测试连接、替换、禁用和二次确认删除。被 capability 引用或项目 override 引用的 profile 不能删除，只能先换绑。
6. **能力提示区**
   - 分别显示四个 capability 的"可用/未配置/最近降级"状态；可用不代表默认启用。
   - 页面明确说明 MiniMax 是特殊需求辅助功能，四个 Switch 未选中时不会向 MiniMax 发送任何内容。

### 5.2 交互原则

- 首屏直接说明三步："上传图文 → 选择风格 → 生成视频"。
- 图片和文字至少提供一项；只有文字时必须预览程序化背景/文字卡方案，不能因无图片阻塞。
- 风格来源三选一；参考链接分析完成且画像确认前不能生成。任何来源变化都会使分镜确认失效。
- 系统必须主动给出创意方案，而不是要求用户预先描述每个镜头；每个自动选择应显示简短理由并允许编辑。
- planner 输出先 `creative_intent` + 2–3 方向，用户选方向后展开 `scene_plan`；每一步都可回退到上一步修改。
- 每轮反馈先形成修改计划再应用，不能直接覆盖已完成 revision；循环直到用户主动标记满意。
- 上传、提示词提案、生成方案、确认分镜、渲染是独立动作，避免一次失败后全部重来。
- 任何 planner / prompt_optimizer / feedback_parser 提案必须由明确的接受操作改变状态；默认、关闭、离开页面、超时和 API 成功都不能推定用户同意。
- 四个 capability Switch 互不联动：可只开 planner、只优化提示词、只生成字幕/特效、只开 feedback_parser 或都不用。改变有效提示词、planner 开关、feedback_parser 应用或风格后，现有 `CreativePlan` 与 storyboard 标记为 `stale`，必须重新生成并确认后才能渲染。
- 状态不只用颜色表达，始终配文字和图标。
- 移动端可完成上传和查看；复杂排序在桌面端体验优先。

## 6. 系统架构

```text
Browser
  │ HTTP/JSON + multipart + video range requests
  ▼
React + Ant Design SPA ───► Express 5 API（TypeScript）
                              ├─ ProjectService
                              ├─ SystemSettingsService ─► data/system/settings.json
                              ├─ StorageBackend (interface)
                              │     ├─ LocalFileSystemAdapter (default, 0700/0600)
                              │     ├─ AliyunOSSAdapter (optional, STS preferred)
                              │     └─ MemoryStorageAdapter (tests only)
                              ├─ ProjectRepository (interface)
                              │     ├─ LocalProjectRepository
                              │     └─ DbProjectRepository (v0.3 placeholder)
                              ├─ CapabilityRegistry
                              │     ├─ planner          → AIProviderRegistry → SafeProviderHttpClient
                              │     ├─ prompt_optimizer → AIProviderRegistry → SafeProviderHttpClient
                              │     ├─ caption_effects  → AIProviderRegistry → SafeProviderHttpClient
                              │     └─ feedback_parser  → AIProviderRegistry → SafeProviderHttpClient
                              ├─ UploadService ────────► StorageBackend (via Sharp)
                              ├─ ReferenceStyleService
                              │     └─ LinuxToolRunner ─► yt-dlp + FFmpeg + Python 分析容器
                              │                            (OpenCV/PySceneDetect/librosa/OCR/CLIP)
                              ├─ PlannerService ──────► CapabilityRegistry(planner) | RulePlanner
                              ├─ PromptService ───────► CapabilityRegistry(prompt_optimizer)
                              │     └─ ProposalConsentGate（必须显式接受）
                              ├─ StoryboardService ───► PlannerService 输出 + RulePlanner
                              │     └──────────────────► CapabilityRegistry(caption_effects)
                              ├─ FeedbackService ─────► CapabilityRegistry(feedback_parser)
                              │     └─ 结构化修改算子 + revision lineage
                              ├─ AnalysisQueue / RenderQueue（受限并发）
                              │     └─ RenderPipeline ──► FFmpeg + ASS + Editor 契约校验
                              └─ FileProjectRepository ─► ProjectRepository (interface)
                                      └─ StorageBackend (local | oss)
```

### 6.1 组件职责

| 组件 | 单一职责 | 主要依赖 |
|---|---|---|
| `StorageBackend` (interface) | putObject/getObjectStream/headObject/deleteObject/exists/list/copy/signedUrl/ping；URI 前缀 `local://` 或 `oss://` | Node.js stream、fs/promises、官方 OSS SDK |
| `LocalFileSystemAdapter` | 把 `local://` 映射到配置根目录；0700/0600；HMAC-signed proxy URL | Node.js `fs/promises` |
| `AliyunOSSAdapter` | 把 `oss://` 映射到 bucket/key；STS 优先，AK 兜底；presigned URL；与 SafeProviderHttpClient 类似的 SSRF/DNS 防护但白名单放宽到阿里云公网 endpoint | 官方 OSS SDK |
| `MemoryStorageAdapter` | 测试用，进程内 Map 实现 `StorageBackend` | Node.js |
| `ProjectRepository` (interface) | `create / getById / list / update(CAS) / delete / withProjectLock` | Zod |
| `LocalProjectRepository` | 当前实现；manifest CAS + 临时文件 + fsync + rename | `StorageBackend`、Node.js `fs/promises` |
| `DbProjectRepository` (v0.3 占位) | 抛出 `Error("DB not enabled in v0.1")`；接口保留供 v0.3 接入 | — |
| `CapabilityRegistry` | 锁定四个 capability ID；按 ID 返回 `descriptor + adapter + binding + gate` | Zod、`AIProviderRegistry` |
| `CapabilityAdapter` (per id) | 一类 capability 一个 adapter；接收 `inputSchema` 校验后的输入，调用 `SafeProviderHttpClient`，按 `outputSchema` 校验响应，按 `fallback` 处理失败 | `AIProviderRegistry`、`SafeProviderHttpClient`、Zod |
| `SystemSettingsRepository` | 原子读写 `data/system/settings.json`，设置 0600 权限并屏蔽 Key | Node.js 文件系统 |
| `SystemSettingsService` | 提供不可序列化的内部凭据快照与公开脱敏 DTO；校验、保存、清除和测试配置 | Zod、Repository |
| `InternalProviderCredentials` | 仅供 `AIProviderRegistry` 读取指定 profile 完整 Key，禁止进入响应/日志/错误 | 私有服务类型 |
| `InternalOSSCredentials` | 仅供 `AliyunOSSAdapter` 读取完整 AccessKey/Secret 或 STS token | 私有服务类型 |
| `PublicProviderProfile` | 仅含 configured、mask、protocol、base、models、enabled 和 version | 可序列化 DTO |
| `PublicStorageConfig` | 仅含 backend 类型、endpoint/bucket/region/prefix/credentials_mode/enabled，不含任何密钥 | 可序列化 DTO |
| `SafeProviderHttpClient` | 解析并固定公网 A/AAAA，禁重定向，防 DNS rebinding/SSRF | undici Agent、node:dns、IP 分类库 |
| `AIProviderRegistry` | 根据 capability 绑定选择 MiniMax / OpenAI-compatible adapter 与模型并验证响应 | `SystemSettingsService`、`SafeProviderHttpClient` |
| `UploadService` | 流式接收图片或文字，规范化并保存到 `StorageBackend` | Busboy、file-type、Sharp |
| `LinuxToolRunner` | 以固定命令、超时、资源限制和 JSON schema 调用多语言 CLI/容器 | spawn、Docker/Podman、allowlist |
| `ReferenceStyleService` | 校验授权 URL，临时获取参考视频并合并主题/色调/节奏/字幕/音频特征 | yt-dlp、FFmpeg、OpenCV 等 adapters |
| `StyleProfileService` | 管理 preset/custom/reference 三类风格及画像确认 | Repository、Zod |
| `PlannerService` | 调用 `CapabilityRegistry(planner)`；失败/关闭时回落到 `RulePlanner`；输出结构化 `CreativePlan`（含 `creative_intent` / 4 段叙事弧 / 情绪曲线 / bookend / 2–3 方向 / `scene_plan`） | `CapabilityRegistry`、`RulePlanner`、Zod |
| `RulePlanner` | 规则生成的结构化 `CreativePlan`；planner 关闭或失败时的 fallback；与 AI planner **产出 schema parity** | 风格模板、本地算法 |
| `FeedbackService` | 调用 `CapabilityRegistry(feedback_parser)`；将结构化控件/可识别文本反馈转成修改计划；输出 `unsupported_text`；应用为新草稿并连接 revision 父子关系 | `CapabilityRegistry`、修改算子、Repository |
| `PromptService` | 调用 `CapabilityRegistry(prompt_optimizer)`；只产生 `proposed`；`ProposalConsentGate` 阻止未显式接受的优化文本进入 storyboard/render_input | `CapabilityRegistry`、Repository |
| `ProposalConsentGate` | 接受必须提交 proposal digest；不接受、关闭、超时均保持原文本 | Repository |
| `StoryboardService` | 接收确认的 `CreativePlan`；默认 `RulePlanner` 叠加，caption_effects 开启时仅替换字幕/动效 | `PlannerService`、`CapabilityRegistry(caption_effects)` |
| `EditorSpecValidator` | 校验 `scene_plan` / storyboard 满足 §9C 规范（时间线、字幕、BGM、安全区、动效白名单、转场白名单）；不通过则返回结构化错误并阻止渲染 | Zod refinements |
| `RenderPipeline` | 将已确认方案渲染为 MP4；ASS 字幕按 §9C 规格烧录；BGM 响度归一化 -16 LUFS、淡入淡出各 1.0s | FFmpeg、Sharp、ASS、`StorageBackend` |
| `AnalysisQueue` | 串行/低并发执行参考视频下载和分析，收集阶段进度与降级结果 | `LinuxToolRunner`、Repository |
| `RenderQueue` | 排队、取消、恢复和串行执行渲染 | Node.js Promise/AbortController、Repository |
| React API client | 封装 HTTP、错误和轮询 | fetch、TanStack Query |
| UI shell | 统一导航、表单、反馈、主题和响应式布局 | Ant Design `ConfigProvider`、`App`、`Layout`、`Menu` |
| Editor state | 管理页面草稿，不复制后端持久化职责 | TanStack Query/React 本地状态 |

### 6.2 目录结构

```text
video-developer/
├── package.json                    # npm workspaces + root scripts
├── package-lock.json
├── tsconfig.base.json
├── README.md
├── THIRD_PARTY_NOTICES.md
├── docs/
│   ├── oss-adoption.md
│   └── capability-scenarios.md
├── .gitignore
├── docker-compose.yml
├── data/
│   ├── .gitkeep
│   └── system/settings.json        # 运行时生成，0600，不提交 Git
├── assets/
│   ├── fonts/NotoSansSC-Regular.otf
│   ├── backgrounds/
│   └── music/
│       ├── LICENSES.md
│       ├── celebratory/
│       ├── birthday/
│       ├── wedding/
│       ├── humorous/
│       ├── romantic/
│       └── tech/
├── frontend/
│   ├── package.json
│   ├── vite.config.ts
│   ├── src/
│   │   ├── app/router.tsx
│   │   ├── app/theme.ts
│   │   ├── app/AppShell.tsx
│   │   ├── api/client.ts
│   │   ├── api/types.ts
│   │   ├── components/
│   │   ├── features/projects/
│   │   ├── features/editor/
│   │   │   ├── AssetStep.tsx
│   │   │   ├── StyleSourceStep.tsx
│   │   │   ├── StyleProfileEditor.tsx
│   │   │   ├── CreativePlanStep.tsx        # 新增：planner 输出 + 方向选择
│   │   │   ├── AISettingsStep.tsx
│   │   │   └── StoryboardEditor.tsx
│   │   ├── features/capabilities/          # 新增：能力设置 UI
│   │   ├── features/render/
│   │   ├── features/settings/
│   │   ├── features/feedback/
│   │   └── styles/
│   └── tests/
├── backend/
│   ├── package.json
│   ├── tsconfig.json
│   ├── src/
│   │   ├── server.ts
│   │   ├── app.ts
│   │   ├── config.ts
│   │   ├── api/
│   │   │   ├── projects.ts
│   │   │   ├── assets.ts
│   │   │   ├── styles.ts
│   │   │   ├── references.ts
│   │   │   ├── creative-plan.ts            # 新增
│   │   │   ├── storyboards.ts
│   │   │   ├── prompt-proposals.ts
│   │   │   ├── feedback.ts
│   │   │   ├── renders.ts
│   │   │   ├── media.ts
│   │   │   ├── capabilities.ts
│   │   │   └── system-settings.ts
│   │   ├── domain/schemas.ts
│   │   ├── storage/                        # 新增
│   │   │   ├── backend.ts
│   │   │   ├── local.ts
│   │   │   ├── aliyun-oss.ts
│   │   │   └── memory.ts
│   │   ├── repositories/
│   │   │   ├── projects.ts                 # interface
│   │   │   ├── file-projects.ts            # LocalProjectRepository
│   │   │   └── db-projects.ts              # DbProjectRepository placeholder
│   │   ├── integrations/
│   │   │   ├── capabilities/               # 新增
│   │   │   │   ├── registry.ts
│   │   │   │   ├── planner.ts
│   │   │   │   ├── prompt-optimizer.ts
│   │   │   │   ├── caption-effects.ts
│   │   │   │   └── feedback-parser.ts
│   │   │   ├── providers/minimax.ts
│   │   │   ├── providers/openai-compatible.ts
│   │   │   ├── ai-provider-registry.ts
│   │   │   └── safe-provider-http-client.ts
│   │   ├── services/
│   │   │   ├── system-settings.ts
│   │   │   ├── uploads.ts
│   │   │   ├── reference-styles.ts
│   │   │   ├── style-profiles.ts
│   │   │   ├── planner.ts                  # PlannerService
│   │   │   ├── rule-planner.ts             # 规则 planner，与 AI planner schema parity
│   │   │   ├── storyboards.ts
│   │   │   ├── prompts.ts
│   │   │   ├── feedback.ts
│   │   │   └── media.ts
│   │   ├── render/
│   │   │   ├── plan.ts
│   │   │   ├── subtitles.ts
│   │   │   ├── text-cards.ts
│   │   │   ├── editor-spec.ts              # 新增：§9C 校验
│   │   │   └── pipeline.ts
│   │   ├── jobs/
│   │   │   ├── analysis-queue.ts
│   │   │   └── render-queue.ts
│   │   └── tools/
│   │       ├── runner.ts
│   │       ├── reference-analyzer.ts
│   │       └── schemas.ts
│   └── tests/
├── analysis-tools/
│   ├── Dockerfile
│   ├── requirements.lock
│   └── analyze_reference.py
└── scripts/
    ├── audit-oss.ts
    ├── check-tools.ts
    ├── check-assets.ts
    ├── check-ffmpeg.ts
    ├── smoke-test.ts
    └── storage-migrate.ts                 # 新增
```

## 7. 数据设计（文件系统）

### 7.1 磁盘布局

#### 7.1.1 系统设置文件（`schema_version: 3`）

`data/system/settings.json` 是网站运行时配置，不使用数据库或 AI 密钥环境变量：

```json
{
  "schema_version": 3,
  "version": 1,
  "ai_providers": [
    {
      "id": "minimax-default",
      "name": "MiniMax",
      "protocol": "minimax",
      "base_url": "https://api.minimax.io/v1",
      "api_key": "用户在网页填写的完整 Key",
      "models": ["MiniMax-M3", "MiniMax-H3"],
      "enabled": true
    }
  ],
  "capability_bindings": {
    "planner":          { "provider_id": "minimax-default", "model": "MiniMax-M3", "enabled_by_default": false },
    "prompt_optimizer": { "provider_id": "minimax-default", "model": "MiniMax-M3", "enabled_by_default": false },
    "caption_effects":  { "provider_id": "minimax-default", "model": "MiniMax-H3", "enabled_by_default": false },
    "feedback_parser":  { "provider_id": "minimax-default", "model": "MiniMax-M3", "enabled_by_default": false }
  },
  "storage": {
    "backend": "local",
    "aliyun_oss": {
      "endpoint": null,
      "bucket": null,
      "region": null,
      "prefix": "video-developer/",
      "credentials_mode": "static_access_key",
      "static_access_key_id": null,
      "static_access_key_secret": null,
      "sts_role_arn": null,
      "sts_session_duration_seconds": 3600,
      "signed_url_ttl_seconds": 3600
    }
  },
  "updated_at": "ISO-8601 UTC"
}
```

- 首次启动创建 `schema_version=3`、0700 目录/0600 文件、不含 Key 的 MiniMax 默认 profile；能力绑定使用上面默认值；storage 默认 local。
- 管理员可新增 MiniMax 或 OpenAI-compatible 供应商 profile；可在能力绑定表里换 provider/model；可切换 storage backend 与 OSS 配置。普通项目用户只能选择 capability Switch，不能读取任何密钥。
- 文件父目录 0700、文件 0600，由非 root 服务用户拥有。Key/AccessKeySecret 按用户要求以磁盘明文保存，因此必须依赖 HTTPS、Basic Auth、文件/备份权限和日志脱敏；没有独立密钥时不宣称加密。
- 写入使用 Zod、设置锁、临时文件 + fsync + 原子替换和 version CAS。
- `getInternalProviderCredentials(profileId)` 只向 `AIProviderRegistry` 返回不可序列化的完整凭据；`getPublicProviderProfiles()` 仅返回 configured、掩码、protocol、base、models、enabled 和 version。两种类型不可复用。
- `getInternalOSSCredentials()` 只向 `AliyunOSSAdapter` 返回不可序列化的完整 AK/Secret 或 STS token；`getPublicStorageConfig()` 仅返回 backend、endpoint、bucket、region、prefix、credentials_mode、signed_url_ttl_seconds。两种类型不可复用。
- GET 永不返回完整 Key/AccessKeySecret；PUT 中 Key/AccessKeySecret 为空表示保持，提供值表示替换。删除/禁用前检查能力绑定和项目 override 引用，存在引用时返回冲突。
- protocol 首版支持 `minimax` 和 `openai_compatible`；每种协议由独立 adapter 构造认证头、路径和请求 schema，禁止让用户输入任意 header 或模板代码。
- Base URL 必须 HTTPS、无 userinfo/query/fragment、端口 443 或省略、规范化 path。MiniMax 协议校验官方 host；OpenAI-compatible 允许管理员确认的公网 host。所有 host 在保存和连接前解析全部 A/AAAA 并拒绝非全球单播，undici 固定已验证 IP 并保留 SNI，禁用全部重定向，防 SSRF/DNS rebinding。
- 模型名限制 1–128 位安全字符；测试连接只返回可用模型/状态/耗时，不返回供应商原始错误中的密钥或请求体。
- 备份默认排除设置文件；显式包含时输出必须 0600 并警告含多个供应商密钥与 OSS AccessKey。
- storage backend 切换**不做自动迁移**；v0.1 提供 `scripts/storage-migrate.ts` 显式导入导出，详见 §7.5。

#### 7.1.2 项目数据

```text
data/projects/<project_id>/
├── manifest.json
├── uploads/<asset_id>/original.<ext>
├── uploads/<asset_id>/normalized.jpg
├── references/<reference_id>/metadata.json
├── references/<reference_id>/profile.json
├── references/<reference_id>/samples/       # 已授权的少量抽帧，按保留策略清理
├── styles/selection.json                    # preset/custom/reference 三选一及确认摘要
├── creative-plan/<plan_id>.json             # CreativePlan（含 directions/scene_plan/input_digest）
├── prompts/<proposal_id>.json
├── feedback/<feedback_id>.json              # proposed/accepted/rejected/applied
├── storyboard/draft.json                    # 当前可编辑草稿，含 version/updated_at
├── revisions/<revision_id>/storyboard.json  # 供审核展示的分镜副本
├── revisions/<revision_id>/render_input.json # 渲染唯一权威输入：分镜 + CreativePlan 摘要 + 设置 + 不可变素材摘要
├── revisions/<revision_id>/subtitles.ass
├── revisions/<revision_id>/render.log
├── revisions/<revision_id>/output.mp4
└── tmp/                                     # 成功或失败后清理
```

- `project_id` / `asset_id` / `revision_id` / `plan_id` / `feedback_id` 使用 UUIDv4，由服务端生成，不接受客户端路径。
- 路径在 manifest 内以 `local://` 或 `oss://` URI 保存；`StorageBackend` 解析。
- **不再有** `share/` 子目录、share token、token_digest。
- `storyboard/draft.json` 是唯一当前草稿；`storyboard:generate` 与 `PUT /storyboard` 都必须提交 `expected_version`（项目尚无草稿时为 `null`），在同一项目锁内做乐观并发检查，成功后版本加 1、计算 storyboard digest，并清空 `confirmed_version / confirmed_input_digest / confirmed_at`。版本不匹配返回 `409 STORYBOARD_VERSION_CONFLICT`。
- "待渲染确认载荷"由有效文字/提示词来源、完整 storyboard、已确认 StyleProfile 及来源摘要、CreativePlan（确认的 direction + scene_plan 摘要）、字幕/BGM/动效/四个 capability Switch、按顺序素材 ID/文案/normalized SHA-256、父 revision/已接受反馈计划组成；标题和分享状态不纳入（**首版无分享状态**）。使用 UTF-8、键排序、数组保序、无多余空白的 canonical JSON 计算 SHA-256。
- `POST /storyboard:confirm` 必须提交当前 storyboard version 和 canonical input digest；后端在项目锁内从当前项目状态重新构造载荷并匹配后，才保存 `confirmed_version`、`confirmed_input_digest` 和时间。任何会改变确认载荷的操作——文字/提示词、风格来源或画像、字幕/BGM/动效/任一 capability、素材上传/删除/排序/文案、反馈计划应用、分镜生成/编辑——都必须原子清空确认；只改标题或存储后端切换不清空。
- `POST /renders` 必须重新构造当前确认载荷，并校验 storyboard version 和 input digest 与服务端确认记录完全一致，否则返回 `409 STORYBOARD_NOT_CONFIRMED` 或 `STORYBOARD_CONFIRMATION_STALE`；该门禁适用于规则与四种 capability 输出的分镜，不能由客户端绕过。
- 通过确认门禁后，`POST /renders` 冻结实际文字/已接受提示词提案、StyleProfile、CreativePlan（direction + scene_plan + 4 段叙事弧 + 情绪曲线）、ProjectSettings、四个 capability 状态、素材摘要、反馈来源、字体/BGM 和全部工具/provider 版本到 `render_input.json`。worker 与 retry 只读该不可变文件，后续设置/工具升级不改变历史输出。
- normalized 素材文件创建后不可原地覆盖；从项目草稿删除素材时，若任一 revision 的 `render_input.json` 仍引用它则保留物理文件，只从当前 assets 列表移除，项目整体删除时才回收。worker 读取前验证 SHA-256，不一致则以 `ASSET_INTEGRITY_ERROR` 失败。
- JSON 写入采用"同目录临时文件 + `fsync` + 原子替换"，避免进程中断产生半个文件。
- 每个项目使用进程内异步锁串行化 manifest、草稿、进度、取消更新；manifest 的 `version` 作为 CAS 版本，防止并发更新丢失。
- `manifest.json` 是项目元数据和 revision 状态的唯一事实来源；派生文件可重新生成。
- 服务端启动时扫描 manifests 恢复索引，不维护额外全局数据库文件。

### 7.2 核心模型

```json
{
  "schema_version": 1,
  "version": 3,
  "id": "uuid",
  "title": "旅行回忆",
  "created_at": "ISO-8601 UTC",
  "updated_at": "ISO-8601 UTC",
  "assets": [
    {"id": "uuid", "filename": "a.jpg", "mime_type": "image/jpeg", "width": 1440, "height": 1920, "caption": "第一天到达海边", "order": 0}
  ],
  "text_content": "祝贺孩子金榜题名",
  "style_selection": {
    "mode": "preset",
    "preset_id": "school_banquet",
    "custom_text": null,
    "reference_id": null,
    "profile_version": 1,
    "confirmed_profile_digest": "sha256:<hex>"
  },
  "settings": {
    "subtitles_enabled": true,
    "music_enabled": true,
    "effects_enabled": true,
    "capability_state": {
      "planner":          { "enabled": false, "binding_override": null, "last_run": null, "last_proposal_id": null },
      "prompt_optimizer": { "enabled": false, "binding_override": null, "last_run": null, "last_proposal_id": null },
      "caption_effects":  { "enabled": false, "binding_override": null, "last_run": null, "last_proposal_id": null },
      "feedback_parser":  { "enabled": false, "binding_override": null, "last_run": null, "last_proposal_id": null }
    }
  },
  "prompt": { "original_text": "记录一次温暖的海边旅行", "active_source": "original", "accepted_proposal_id": null },
  "latest_revision_id": "uuid",
  "draft_storyboard": { "version": 2, "updated_at": "ISO-8601 UTC", "path": "storyboard/draft.json", "digest": "sha256:<hex>", "confirmed_version": 2, "confirmed_input_digest": "sha256:<hex>", "confirmed_at": "ISO-8601 UTC" },
  "creative_plan": {
    "id": "uuid",
    "version": 1,
    "status": "confirmed",
    "source": "provider:minimax-default+MiniMax-M3",
    "selected_direction_index": 1,
    "input_digest": "sha256:<hex>",
    "updated_at": "ISO-8601 UTC"
  },
  "revisions": [
    {"id": "uuid", "parent_revision_id": null, "feedback_id": null, "status": "completed", "is_final": false, "progress": 100, "stage": "completed", "render_input_digest": "sha256:<hex>", "warning_codes": [], "error_code": null, "created_at": "ISO-8601 UTC"}
  ]
}
```

> manifest **不包含** `share` 字段。`is_final` 仅为"用户满意版本"标记，与任何外部分享无关。

`references/<reference_id>/profile.json`：

```json
{
  "version": 1,
  "source": "reference_url",
  "theme_tags": ["celebration", "family", "youth"],
  "palette": ["#D9B45B", "#9B1C31", "#F6E7C1"],
  "brightness": 0.68,
  "saturation": 0.61,
  "contrast": 0.55,
  "average_shot_seconds": 2.4,
  "pace": "medium_fast",
  "motion_intensity": 0.42,
  "caption": {"region": "bottom", "density": "medium", "size": "large"},
  "music": {"bpm": 112, "energy": "uplifting", "tags": ["celebration"]},
  "confidence": {"theme": 0.72, "palette": 0.91, "pace": 0.88},
  "tool_versions": {"yt_dlp": "pinned", "ffmpeg": "pinned", "opencv": "pinned"},
  "confirmed_digest": null
}
```

- 风格画像只保存抽象特征和少量授权抽帧，不保存参考视频音乐/台词正文。用户编辑后通过确认 API 生成 digest。
- preset 和 custom 也规范化为同一 StyleProfile，确保 RulePlanner 不关心来源。

`feedback/<feedback_id>.json`：

```json
{
  "id": "uuid",
  "source_revision_id": "uuid",
  "status": "proposed",
  "raw_comment": "字幕再大一点，节奏慢一些",
  "parser_source": "rule_dictionary | provider:minimax-default+MiniMax-M3",
  "changes": [
    {"op": "set_caption_scale", "value": 1.15, "confidence": 0.9},
    {"op": "set_pace", "value": "slower", "confidence": 0.8}
  ],
  "unsupported_text": [],
  "created_at": "ISO-8601 UTC"
}
```

- 反馈计划只允许白名单操作；状态为 `proposed → accepted / rejected`，accepted 应用后创建新草稿并记录 `applied_revision_id`。
- 无法可靠解释的自由文本进入 `unsupported_text`，必须由用户用结构化控件澄清，系统不得猜测。
- 当 `feedback_parser` 关闭时，`parser_source="rule_dictionary"`，只使用有限意图词典；当 capability 启用时输出带 `confidence`。

`prompts/<proposal_id>.json`：

```json
{
  "id": "uuid",
  "model": "MiniMax-M3",
  "original_text": "记录一次温暖的海边旅行",
  "proposed_text": "以温暖细腻的叙事记录海边旅行……",
  "proposed_digest": "sha256:<hex>",
  "status": "proposed",
  "accepted_text": null,
  "created_at": "ISO-8601 UTC",
  "decided_at": null
}
```

- 状态只允许 `proposed → accepted` 或 `proposed → rejected`，不可逆；再次优化创建新 proposal。
- `accepted` 必须来自独立确认 API，并提交 `proposed_digest` 防止确认错版本；"编辑后接受"把用户最终确认文本写入 `accepted_text`。
- `active_source=optimized` 只在 proposal 已 accepted 时合法；生成 storyboard 和 `render_input.json` 必须保存实际提示词文本、来源、proposal ID、模型名和摘要。未接受提案只能留作预览审计，不能被系统自动选用。
- 用户修改原始提示词后，后端必须清除旧 `accepted_proposal_id` 并把 `active_source` 切回 `original`；旧 proposal 保留审计但不能复用。关闭 capability 只阻止新请求，不撤销用户已经接受并拥有的文本；用户可显式"恢复原文"。

`creative-plan/<plan_id>.json`（CreativePlan，结构见 §9B Zod）：

- 包含 `creative_intent` / `narrative_arc`（4 段必填）/ `emotional_curve` / `bgm_rationale` / `directions[]`（2–3 个）/ `scene_plan` / `input_digest` / `source`。
- 状态机：`draft → proposed → confirmed → applied_to_storyboard → stale`；任何输入变化（素材/文字/风格/capability override/feedback 应用）使其变 `stale`。
- `selected_direction_index` 记录用户选择的方向；只有选过方向后才能进入 `confirmed`。

`storyboard.json`：

```json
{
  "version": 1,
  "duration_seconds": 18.0,
  "music_category": "acoustic/warm",
  "scenes": [
    {"asset_id": "uuid", "start": 0.0, "duration": 4.5, "subtitle": "第一天，我们抵达海边", "motion": "zoom_in", "transition": "fade"}
  ]
}
```

### 7.3 状态机

`StyleAnalysisJob.status`：`queued → downloading → probing → sampling → analyzing → merging → awaiting_confirmation`，任一运行阶段可到 `failed/cancelling/cancelled`。部分工具失败但核心字段可用时仍进入 `awaiting_confirmation` 并带 warnings；只有用户确认画像后 reference 状态为 `confirmed`。

`CreativePlan.status`：`draft → proposed → confirmed → applied_to_storyboard → stale`；任何输入变化使其变 `stale`，禁止渲染。

`FeedbackPlan.status`：`proposed → accepted → applied` 或 `proposed → rejected`；source revision 删除前禁止删除其计划，应用时必须校验 source revision 和当前 draft version 未过期。

`Revision.status` 只取终态/调度态，`Revision.stage` 只表示运行中的进度阶段：

| 当前 status | 允许操作/事件 | 下一 status | stage 规则 |
|---|---|---|---|
| `queued` | worker 领取 | `running` | `planning` |
| `queued` | 用户取消 | `cancelled` | `cancelled` |
| `running` | 阶段推进 | `running` | `planning → preparing → rendering → finalizing` |
| `running` | 用户取消 | `cancelling` | 保持当前阶段 |
| `cancelling` | 子进程退出/重启恢复 | `cancelled` | `cancelled` |
| `running` | probe 成功 | `completed` | `completed` |
| `running` | 任一阶段异常 | `failed` | 失败时所在阶段 |

- `draft` 是项目编辑状态，不是 revision status；revision 创建后初始即为 `queued`。
- `completed`、`failed`、`cancelled` 为不可逆终态。重试永远创建新 revision。`is_final` 是独立交付标记，同项目最多一个；设置新 final 时原 final 自动撤销但文件保留。
- 服务重启时：遗留 `queued` 保持不变；`running` 回到 `queued` 并清零瞬时进度；`cancelling` 变为 `cancelled`；终态不变。
- 状态只能由后端按表迁移。每次迁移都在项目锁内原子更新 manifest；前端不得直接提交 `status`、`stage`、输出路径。

### 7.4 数据保留

- 参考视频原始分析副本只放项目 `tmp/reference-*`，风格画像完成或失败后立即删除；默认只保留 profile、metadata 和最多 12 张低清抽帧，项目删除时一并删除。
- 用户可手工删除参考抽帧而保留画像。
- 首版默认项目和 revision 永久保留，直到用户主动删除。
- 删除项目时先将目录原子移动到 `data/trash/`，再异步删除，以减少半删除状态。
- **存储后端切换后：** 数据不自动迁移；`scripts/storage-migrate.ts` 提供 `export / import / verify` 三个子命令，详见 §7.5。
- 提供磁盘可用空间指标和管理员清理命令；自动过期策略待引入用户体系后再决定。

### 7.5 Storage Backend 抽象

#### 7.5.1 接口

```ts
// backend/src/storage/backend.ts
export interface StorageBackend {
  readonly kind: "local" | "aliyun_oss" | "memory";

  // URI 约定："local://relative/path" 或 "oss://key"（bucket 在 backend 配置内）。
  putObject(key: string, body: Buffer | NodeJS.ReadableStream,
            opts?: { contentType?: string; cacheControl?: string }): Promise<void>;

  getObjectStream(key: string): Promise<NodeJS.ReadableStream>;

  headObject(key: string): Promise<{ size: number; etag: string;
                                      contentType?: string; exists: boolean }>;

  deleteObject(key: string): Promise<void>;
  exists(key: string): Promise<boolean>;

  list(prefix: string, opts?: { limit?: number; cursor?: string })
    : AsyncIterable<{ key: string; size: number; etag: string }>;

  copy(srcKey: string, dstKey: string): Promise<void>;

  // 可选：直接浏览器上传/下载的签名 URL。Local 返回同源代理 URL；OSS 返回 presigned URL。
  signedUrl?(key: string, opts: { method: "GET" | "PUT"; ttlSeconds: number })
    : Promise<string>;

  // 用于 /api/v1/capabilities。
  ping(): Promise<{ ok: boolean; latency_ms: number; error_code?: string }>;
}
```

#### 7.5.2 实现

- **`LocalFileSystemAdapter`**：包 `fs/promises`；URI 前缀 `local://` 解析到配置根目录（默认 `./data/projects`）；0700 目录 / 0600 文件；`signedUrl` 返回 `/api/v1/storage/local/{key}?...` + HMAC 签名。
- **`AliyunOSSAdapter`**：包官方 SDK（高 Star，Apache-2.0 类）；URI 前缀 `oss://`；`signedUrl` 返回真实 presigned URL；优先 STS 临时凭证（自动续签），静态 AccessKey 需管理员二次确认 + warning；与 `SafeProviderHttpClient` 类似做 SSRF/DNS 防护但 endpoint 白名单放宽到阿里云 OSS 公网 endpoint。
- **`MemoryStorageAdapter`**：仅测试用，进程内 Map。

#### 7.5.3 选择与迁移

- 运行时根据 `settings.json` 的 `storage.backend` 注入唯一一个 adapter（启动时校验；切换 backend 需重启或热重载）。
- 切换 backend **不做自动迁移**。`scripts/storage-migrate.ts` 提供：
  - `export --backend <local|aliyun_oss> --output <tar-or-oss-prefix>`：把当前 backend 上的全部项目导出。
  - `import --source <tar-or-oss-prefix> --backend <local|aliyun_oss>`：把导出物导入新 backend，逐文件校验 SHA-256 后写入新位置。
  - `verify --left <a> --right <b>`：逐文件比较两侧的 SHA-256，输出差异表。
- 切换前管理员必须先 `export`，在新 backend `import`，`verify` 通过后再改 `settings.json` 的 `storage.backend` 并重启。

### 7.6 Repository DB Seam

```ts
// backend/src/repositories/projects.ts
export interface ProjectRepository {
  create(project: NewProject): Promise<Project>;
  getById(id: string): Promise<Project | null>;
  list(opts?: { limit?: number; cursor?: string }): Promise<ProjectSummary[]>;
  update(id: string, patch: ProjectPatch, opts: { expectedVersion: number })
    : Promise<Project>;                          // CAS, throws VERSION_CONFLICT
  delete(id: string): Promise<void>;
  withProjectLock<T>(id: string, fn: () => Promise<T>): Promise<T>;
}
```

- `LocalProjectRepository implements ProjectRepository`：当前实现；manifest CAS + 临时文件 + fsync + rename；底层所有非 JSON 二进制访问走 `StorageBackend`。
- `DbProjectRepository implements ProjectRepository`（v0.3 占位）：v0.1 文件存在，方法 `throw new Error("DB not enabled in v0.1")`；启动配置 `REPOSITORY_BACKEND` 仅接受 `local`，其他值启动报错并退出。
- DB-aware 扩展（v0.3 设想，不在 v0.1 实现）：`searchByTitle / listByStatus / listByUpdatedRange / countByUser / aggregateStorageBytes / findOrphanAssets`。需要 DB 查询的服务应依赖窄接口（如 `ProjectSearchQueries`），便于文件系统实现优雅降级。
- `LocalProjectRepository` 是 v0.1 唯一实例化的实现；manifest 仍是事实来源；DB 实现接入必须复现 CAS / digest / 原子替换语义，否则 version/digest 契约需重测。

## 8. API 设计

统一前缀 `/api/v1`，成功返回 JSON；错误格式固定为：

```json
{"error":{"code":"INVALID_IMAGE","message":"图片格式不受支持","details":{}}}
```

| 方法与路径 | 用途 | 关键响应 |
|---|---|---|
| `GET /health` | 存活检查 | `{"status":"ok"}` |
| `GET /api/v1/capabilities` | FFmpeg、分析工具、OSS、四个 AI capability | 返回工具版本/可用性、storage backend 类型与状态、四个 capability 的 provider/model 与 enabled_by_default，**不返回密钥** |
| `GET /api/v1/system-settings/ai-providers` | 读取供应商 profiles 与 capability 绑定 | PublicProviderProfile 数组；无完整 Key |
| `POST /api/v1/system-settings/ai-providers` | 新增供应商、协议、base、Key、模型 | `201` + 脱敏 profile |
| `PUT /api/v1/system-settings/ai-providers/{pid}` | 修改/替换 Key、地址、模型、启用状态 | version CAS + 脱敏 profile |
| `DELETE /api/v1/system-settings/ai-providers/{pid}` | 删除未绑定/未引用 profile | `204` 或 409 |
| `POST /api/v1/system-settings/ai-providers/{pid}:test` | 测试供应商连接/模型 | 脱敏状态、模型、耗时 |
| `PUT /api/v1/system-settings/capability-bindings` | 分别绑定四个 capability（planner / prompt_optimizer / caption_effects / feedback_parser）的 provider_id 与 model，并设置 `enabled_by_default` | 更新后的 capability_bindings |
| `GET /api/v1/system-settings/storage` | 读取存储后端配置（脱敏） | PublicStorageConfig |
| `PUT /api/v1/system-settings/storage` | 切换 backend 或修改 OSS 参数 | 新的脱敏配置；静态 AK 走二次确认；要求事先完成 `storage-migrate` |
| `POST /api/v1/system-settings/storage:test` | 测试 OSS 连接（STS 续签 / 列出 bucket） | 脱敏状态、延迟 |
| `GET /api/v1/projects` | 项目列表 | 项目摘要数组 |
| `POST /api/v1/projects` | 创建项目 | `201` + project |
| `GET /api/v1/projects/{id}` | 项目详情 | project |
| `PATCH /api/v1/projects/{id}` | 改标题、设置（含 capability_state）、素材文案/顺序 | 更新后的 project |
| `DELETE /api/v1/projects/{id}` | 删除项目 | `204` |
| `GET /api/v1/styles/presets` | 列出升学宴、生日宴等版本化预设 | preset 摘要和默认 StyleProfile |
| `POST /api/v1/projects/{id}/style-selection` | 保存 preset/custom/reference 三选一 | selection；改变后清除分镜确认 |
| `POST /api/v1/projects/{id}/references:analyze` | 提交 allowlist 链接、授权确认并创建分析任务 | `202` + analysis job/reference ID |
| `GET /api/v1/projects/{id}/references/{refId}` | 查询解析进度、警告和风格画像 | metadata + profile + confidence |
| `POST /api/v1/projects/{id}/references/{refId}:confirm` | 编辑并确认抽象风格画像 | confirmed profile digest |
| `DELETE /api/v1/projects/{id}/references/{refId}` | 删除参考 metadata/profile/samples | `204` |
| `POST /api/v1/projects/{id}/assets` | multipart 上传图片 | `201` + asset |
| `DELETE /api/v1/projects/{id}/assets/{assetId}` | 删除素材 | `204` |
| `POST /api/v1/projects/{id}/prompt-proposals` | 用户开启 prompt_optimizer 后调用 M3 生成候选，不改变有效提示词 | `201` + proposed proposal |
| `POST /api/v1/projects/{id}/prompt-proposals/{pid}:accept` | 提交 proposal digest 和最终确认文本，显式接受 | accepted proposal + active prompt |
| `POST /api/v1/projects/{id}/prompt-proposals/{pid}:reject` | 明确拒绝提案，继续使用原提示词 | rejected proposal |
| `POST /api/v1/projects/{id}/creative-plan:generate` | 调用 planner capability（开启）或 RulePlanner；输入：素材有序列表、StyleProfile、duration_hint。返回带 2–3 个方向 + `creative_intent` + 情绪曲线的 `CreativePlan`（`status=proposed`，scene_plan 仅在选方向后展开） | `201` + CreativePlan（无 `scene_plan`） |
| `POST /api/v1/projects/{id}/creative-plan:select-direction` | 用户选择方向（`direction_index`），后端展开完整 `scene_plan`；状态仍为 `proposed` | `200` + CreativePlan（含 scene_plan） |
| `POST /api/v1/projects/{id}/creative-plan:confirm` | 提交 input digest 确认 `CreativePlan`；记录 `confirmed_input_digest`、`confirmed_at`；任何输入变化使其变 `stale` | confirmed CreativePlan |
| `PUT /api/v1/projects/{id}/storyboard` | 携带当前 version 保存草稿并清除旧确认 | 新 version/digest；版本冲突返回 409 |
| `POST /api/v1/projects/{id}/storyboard:generate` | 携带 `expected_version`；默认规则生成，caption_effects 开关开启时只替换字幕/特效；前置：已确认 StyleProfile 与已确认 CreativePlan | 新 version + warnings + content source；冲突返回 409 |
| `POST /api/v1/projects/{id}/storyboard:confirm` | 提交当前 version/input digest，服务端从提示词+设置+有序素材+分镜+确认 CreativePlan 摘要复算后确认 | confirmed version/input digest/time |
| `POST /api/v1/projects/{id}/renders` | 校验当前分镜已确认、CreativePlan 已确认，原子冻结草稿、创建 revision 并排队 | `202` + revision；未确认/已过期返回 409 |
| `GET /api/v1/projects/{id}/revisions/{rid}` | 查询进度或结果 | revision |
| `POST /api/v1/projects/{id}/revisions/{rid}/feedback:plan` | 将自由文本+结构化控件转成修改计划；`feedback_parser` 开启时调用 capability，否则使用有限意图词典；输出 `changes[]` + `unsupported_text[]` + `confidence` | proposed plan |
| `POST /api/v1/projects/{id}/feedback/{fid}:accept` | 接受并应用修改计划到新草稿 | updated draft + parent revision |
| `POST /api/v1/projects/{id}/feedback/{fid}:reject` | 拒绝计划，不改变项目 | rejected plan |
| `POST /api/v1/projects/{id}/revisions/{rid}:finalize` | 将满意版本标记为 final | revision；同项目仅一个 final |
| `DELETE /api/v1/projects/{id}/revisions/{rid}/finalize` | 撤销 final 以继续修改 | `204` |
| `POST /api/v1/projects/{id}/revisions/{rid}:cancel` | 取消任务 | revision |
| `POST /api/v1/projects/{id}/revisions/{rid}:retry` | 在锁内逐字节复制源 `render_input.json` 并创建新任务，不读取当前项目草稿/设置 | `202` + new revision |
| `GET /api/v1/projects/{id}/revisions/{rid}/video` | 播放/下载，支持 Range | `video/mp4` |

> **v0.2 起 `share` 端点已移除。** 无 `POST /share / DELETE /share / GET /share / GET /share/video`。

关键错误码补充：`INVALID_AI_PROVIDER`、`AI_PROVIDER_IN_USE`、`AI_PROVIDER_NOT_CONFIGURED`、`AI_PROVIDER_CONNECTION_FAILED`、`CAPABILITY_NOT_REGISTERED`、`INVALID_REFERENCE_URL`、`REFERENCE_AUTHORIZATION_REQUIRED`、`REFERENCE_UNSUPPORTED`、`REFERENCE_LIMIT_EXCEEDED`、`STYLE_ANALYSIS_FAILED`、`STYLE_PROFILE_NOT_CONFIRMED`、`CREATIVE_PLAN_NOT_CONFIRMED`、`CREATIVE_PLAN_INVALID`、`CREATIVE_PLAN_STALE`、`FEEDBACK_UNSUPPORTED`、`FEEDBACK_PLAN_STALE`、`STORAGE_BACKEND_UNAVAILABLE`、`OSS_PERMISSION_DENIED`、`OSS_CONFIG_INVALID`、`EDITOR_SPEC_VIOLATION`。其余沿用项目、素材、分镜、渲染错误码。

## 9. 风格分析、创意生成、字幕、特效与 BGM

### 9.1 风格来源与参考视频分析

1. **预设模式：** 从版本化 StyleProfile 库选择；升学宴强调庄重喜庆、金红配色、成长叙事和中等节奏，生日宴强调温暖活泼、祝福字幕和轻快节奏。用户仍可编辑画像。
2. **自定义模式：** 将主题、受众、色调、节奏、情绪、字幕和特效偏好规范化为 StyleProfile；缺失字段由规则默认值补全并标记 `inferred=true`。
3. **参考模式：**
   - URL 只接受配置的公开平台 host 和单视频；先用 yt-dlp 读取 metadata，验证时长/大小/播放列表/协议，再临时获取最低满足分析的分辨率。
   - FFmpeg 抽帧/音频；OpenCV 提取色调/明暗/饱和度/运动量；PySceneDetect 计算镜头节奏；librosa 提取 BPM/能量；PaddleOCR 只统计字幕区域/密度；CLIP 从预定义主题标签集评分。
   - 分析器输出独立 JSON，ReferenceStyleService 按字段置信度合并。任何工具失败只降低对应字段置信度，不伪造结果。
   - 页面展示画像、证据抽帧、工具来源和置信度；用户编辑并确认后生成 digest。系统只模仿抽象风格，不复制画面/音乐/台词。
4. 任何风格来源变化或画像编辑都会使旧分镜确认失效。

#### 9.1.1 参考链接安全与合规

- 只允许 `https` 和配置的抖音/YouTube 等平台 host，不接受 IP、localhost、私网 DNS、userinfo、query 中的凭据或任意重定向目标；沿用 `SafeProviderHttpClient` 的 DNS 固定/非公网拒绝原则。
- yt-dlp 以固定版本、无 shell、无 playlist、无 cookie、最大 5 分钟/200 MB/720p、超时和受限临时目录运行；关闭插件/配置文件自动加载，不执行 postprocessor 脚本。
- 分析任务使用低权限 Linux 用户/容器，限制 CPU、内存、进程数、文件数、网络出口和总时长；stdout/stderr 截断并脱敏。
- 用户必须勾选授权声明；系统不绕过登录、DRM、地区限制或平台防护。原始文件分析后立即删除。

### 9.2 自动创意与确定性规则

`PlannerService` 优先调用 `CapabilityRegistry(planner)`；失败/关闭/超时/输出 Zod 校验不过时降级到 `RulePlanner`。两个路径产出**schema parity**（Task 21 验证）。

1. **`CreativePlan.creative_intent` 先输出**：一句主题、4 段叙事弧（opening / development / climax / resolution）、mood 标签、整体情绪曲线关键点。
2. **2–3 个 `directions[]` 输出**：每个方向含 title / summary / visual_keywords / differentiator；不包含完整 scene_plan。
3. 用户在 `CreativePlanStep` 中**选择方向**（`POST /creative-plan:select-direction`）；后端为所选方向展开完整 `scene_plan`。
4. 用户**确认 `CreativePlan`**（`POST /creative-plan:confirm`）；记录 `input_digest`，status → `confirmed`。
5. `StoryboardService` 接收确认的 CreativePlan，默认由 `RulePlanner` 实施；caption_effects 开启时只替换字幕/动效字段（不改素材/时长上限/BGM 文件）。
6. 有图片时每张至少一个候选镜头并按文案/视觉顺序重排；无图片时按句子生成 2–8 个动态文字卡，使用已授权纹理、渐变、粒子/几何背景和 Ken Burns 类运动。
7. 镜头默认时长 `clamp(字符数/6, 2.5, 6.0)`，再根据 StyleProfile 的 pace/average_shot_seconds 校准；总时长 15–60 秒。
8. 总文案按标点切句；相邻镜头避免相同 motion；字幕每行约 18 个中文字符、最多两行，以像素宽度最终校验。
9. **bookend / call-back：** `narrative_arc.bookend.opening_scene_id == closing_scene_id`（复用素材或同 motion+transition），并填 `echo_description`。
10. 情绪曲线（至少 4 个时间点：开头/发展/高潮/收尾）每个点含 `valence / arousal / visual_cue / bgm_cue / subtitle_cue`；`scene_plan` 的每个 scene 关联到对应情绪点。
11. BGM 从 StyleProfile 的情绪/BPM/energy 映射到已授权曲库，按目标节奏裁剪；H3 无权选择文件。
12. 特效强度根据主题和场景控制，升学宴/生日宴避免频闪和过度故障效果；所有 motion/transition 来自白名单。
13. 系统必须生成完整默认方案，用户无需先知道镜头语言；同时保留字幕、节奏、BGM、特效、配色和顺序的可编辑入口。

#### 9.2.1 反馈修改规则

- 结构化控件直接映射白名单操作；自由文本由 `CapabilityRegistry(feedback_parser)` 解析为白名单操作 + `confidence`，关闭时使用有限意图词典。
- 解析结果必须由用户接受才能应用；任何不被白名单覆盖的自由文本进入 `unsupported_text`，由用户用结构化控件澄清，系统不得猜测。
- 修改计划必须列出 `before/after`、受影响镜头和是否需要重新生成字幕/特效/BGM；应用前用户确认。
- 每次应用创建新草稿并以 `parent_revision_id` 连接来源，不改写历史 MP4；可回退任一 revision 并从其继续分支。
- 只有用户点击"设为满意版本"才结束循环并标记 final。

### 9.3 风格模板与时间线

| 风格 | 色彩/字幕 | 动效与转场 | BGM 分类 |
|---|---|---|---|
| 升学宴 | 金/红/暖白，庄重喜庆 | 成长回顾→录取/祝福高潮，稳健推近与淡化 | celebratory/uplifting |
| 生日宴 | 暖橙/粉/彩色，活泼温馨 | 年龄/回忆/祝福文字卡，轻快缩放和滑动 | upbeat/warm |
| 婚礼 | 米白/香槟金/柔粉 | 慢推近、交叉淡化、誓言重点字幕 | romantic/acoustic |
| 轻松幽默 | 暖黄强调、较粗字幕 | 快速轻缩放、滑动；避免频闪 | upbeat/light |
| 浪漫温馨 | 暖粉/米白、柔和阴影 | 慢推近、交叉淡化 | acoustic/warm |
| 科技感 | 青蓝、细描边 | 平移、轻微故障切换；控制强度 | electronic/minimal |

时间线契约：`Scene.duration` 表示该镜头从开始到结束的完整屏幕时间；除最后一镜外，结尾与下一镜重叠转场。轻松幽默、浪漫温馨、科技感的默认转场时长分别为 0.4–0.5、0.7–0.9、0.4–0.5 秒，且不得超过相邻任一镜头时长的 25%。第 1 镜只做 0.4 秒画面淡入，最后一镜 `transition=none` 并做 0.6 秒淡出。镜头开始偏移按 `next_start = current_start + current_duration - transition_duration` 计算，`Storyboard.duration_seconds = 最后一镜 start + duration`；规则规划器以该公式反算并保证最终 15–60 秒，RenderPlan 单测逐帧验证偏移，ffprobe 允许误差不超过 0.5 秒。

BGM 契约：预设和参考画像把 `music.tags / energy / bpm` 映射到授权曲库目录与曲目 metadata；升学宴默认 celebratory，生日宴 upbeat/warm，婚礼 romantic/acoustic。候选先按标签和 BPM 距离排序，再以项目/风格 digest 稳定选取。选曲仅在创建 revision 时冻结到 `render_input.json`；新增曲库不影响历史版本，文件/许可证摘要不一致则失败。

### 9.4 AI 辅助能力边界

四个 capability 的统一规则：

- 每个 capability 有独立 Switch、独立 provider/model 绑定（系统设置）、独立 input/output Zod schema、独立 gate（`none / preview_accept / plan_confirm / storyboard_confirm`）、独立 fallback（`rule_planner / noop / keep_original`）、独立降级策略。
- 每个 capability 调用只接受协议允许的字段；adapter 不接受任意 header / 模板代码 / 自由路径。
- 失败/超时/未配置/校验失败 → 走 fallback；不静默换供应商/模型。
- 关闭 capability 时后端不得调用任何 AI 供应商。
- 任何变更有效提示词 / 任一 capability Switch / CreativePlan 确认 / feedback 应用 / 风格切换都会使现有 CreativePlan 与 storyboard 标记 `stale`，必须重新生成并确认后才能渲染。

#### 9.4.1 Planner（默认绑定 MiniMax-M3）

- 默认绑定 MiniMax-M3；管理员可在系统设置切换到其他已配置供应商/模型；预览必须显示实际 provider/model。
- 开启后 `PlannerService` 调用 capability 输入：素材（有序）/ 文案 / StyleProfile（已确认）/ duration_hint / language。
- 输出 schema：完整 `CreativePlan`（含 `creative_intent` / 4 段叙事弧 / bookend / `emotional_curve` / `bgm_rationale` / 2–3 个 `directions`）。`scene_plan` 仅在用户选方向后展开。
- **门禁两步：** (a) `POST /creative-plan:select-direction` 选择 1 个方向；(b) `POST /creative-plan:confirm` 提交 input digest 完成确认。任一拒绝/超时/离开页面都不修改项目状态。
- 失败/关闭 → `RulePlanner` 输出等价 schema 的 `CreativePlan`，UI 显示 warning。

#### 9.4.2 Prompt Optimizer（默认绑定 MiniMax-M3）

- 默认绑定 MiniMax-M3；管理员可切换供应商/模型。
- 只有用户开启 capability 并点击优化才调用；输入仅含当前提示词、StyleProfile 与必要文字，不上传图片二进制。
- 输出 schema 只允许候选文本与修改说明，不得生成字幕/BGM/路径/命令。
- API 成功只产生 `proposed`；页面必须展示原文/提案差异；只有用户点击"接受"或"编辑后接受"，后端确认 proposal digest 后才更新 active prompt。
- 拒绝、关闭弹窗、离开页面、网络超时或关闭 capability 均保持原提示词；系统不得自动接受、默认勾选接受或用提案覆盖原文。

#### 9.4.3 Caption / Effects（默认绑定 MiniMax-H3）

- 默认绑定 MiniMax-H3；管理员可独立切换供应商/模型，与其他 capability 互不关联。
- 只有用户开启 capability 并生成方案时调用；输入采用确认的 CreativePlan 摘要、素材顺序和已确认 StyleProfile。
- 输出 schema 只允许每镜字幕/字幕行/动效/转场候选；不得改变图片、时长上限、BGM 文件、路径或命令。
- 结果必须经过 Zod、白名单与 §9C 编辑器规范校验，并标记实际 provider/model 后进入 StoryboardEditor 确认。

#### 9.4.4 Feedback Parser（默认绑定 MiniMax-M3）

- 默认绑定 MiniMax-M3；管理员可切换供应商/模型。
- 开启后由 `FeedbackService` 调用；输入：原始文本 + 结构化控件 + 当前 CreativePlan + 当前 storyboard。
- 输出 schema：`changes[]`（白名单操作 + `confidence`）、`unsupported_text[]`、`per_op_confidence`、`rationale`。
- 任何不被白名单覆盖的文本进入 `unsupported_text`；系统不得猜测，必须由用户用结构化控件澄清。
- 关闭时使用有限意图词典，输出仍走相同 schema（`parser_source="rule_dictionary"`）。
- 结果必须由用户接受才应用到新草稿。

#### 9.4.5 供应商配置、降级与隐私

- `AIProviderRegistry` 从 `data/system/settings.json` 读取 capability 绑定；内部凭据只给 adapter，GET/capabilities/日志只用 PublicProviderProfile。设置保存后热更新。
- `/capabilities` 分别报告四个 capability 的 provider、model、可用状态与 `enabled_by_default`；未配置/禁用时关闭对应项目 Switch，不静默换绑。
- MiniMax 与 OpenAI-compatible adapter 各自使用固定认证/请求构造，响应统一转为 capability schema；15 秒超时、最多重试 1 次，授权失败不重试。
- 提示词优化失败保留原文；字幕/特效失败退回 RulePlanner；planner 失败退回 RulePlanner；feedback_parser 失败退回有限意图词典。任一 capability 关闭时不得调用任何 AI 供应商。
- 日志不记录任何供应商 Key、完整提示词/字幕、请求体或原始响应；只记录 capability_id、binding provider/model、ID、耗时和脱敏错误码。
- 每个供应商在系统设置中独立测试；连接成功不自动开启项目功能。

### 9.5 FFmpeg 渲染步骤

1. 有图片时用 Sharp 纠正 EXIF/色彩；无图片时根据 StyleProfile 生成 1080×1920 程序化背景和文字卡素材。
2. 图片镜头使用模糊填充背景+完整前景或经用户确认的裁切；文字镜头使用渐变、纹理、粒子/几何动效和动态排版。
3. 每个镜头用 `zoompan`/`scale`/`crop` 生成中间无音频片段。
4. 按 storyboard 转场合并镜头；转场不可用时降级为硬切，不执行任意外部脚本。
5. 生成 ASS 字幕，使用项目内置中文字体并限制在 §9C 安全区。
6. 使用 `render_input.json` 中冻结的 BGM 和字体，验证文件及许可证条目摘要；BGM 按 §9C 规格裁剪/循环、响度归一化 -16 LUFS 并设置 1.0s 淡入淡出，禁止运行时重新选曲。
7. 合成音视频和字幕，输出 H.264/AAC MP4；运行 `ffprobe` 验证编码、尺寸、时长和可播放性。
8. 原子移动为 `output.mp4`，清理 revision 临时文件，状态变为 `completed`。

## 9A. Capability Registry 概览

### 9A.1 注册表类型

```ts
// backend/src/integrations/capabilities/registry.ts
export type CapabilityId =
  | "planner"
  | "prompt_optimizer"
  | "caption_effects"
  | "feedback_parser";

export interface CapabilityDescriptor {
  id: CapabilityId;
  scenario: string;                              // 用户在系统设置看到的"场景"描述
  inputSchema: ZodTypeAny;
  outputSchema: ZodTypeAny;
  gate: "none" | "preview_accept" | "plan_confirm" | "storyboard_confirm";
  fallback: "rule_planner" | "noop" | "keep_original";
  defaultBinding: { provider_id: string; model: string };
  defaultEnabled: false;
}
```

### 9A.2 Capability → 场景映射（用户可见）

| Capability ID | 用户场景 | 系统默认绑定 | 默认开关 | Gate | Fallback |
|---|---|---|---|---|---|
| `planner` | 作为视频策划师及设计师，根据素材/风格/文字给出创意、理解和视频生成方案 | MiniMax-M3 | OFF | `plan_confirm`（两步：方向选择 + 方案确认） | `RulePlanner` 输出 schema parity 的 `CreativePlan` |
| `prompt_optimizer` | 优化提示词 | MiniMax-M3 | OFF | `preview_accept` | `keep_original` |
| `caption_effects` | 生成字幕/特效 | MiniMax-H3 | OFF | `storyboard_confirm` | `RulePlanner` 字幕/动效 |
| `feedback_parser` | 把自由文本反馈解析成结构化修改操作；其他视频生成相关辅助功能 | MiniMax-M3 | OFF | `preview_accept` | 有限意图词典（`rule_dictionary`） |

### 9A.3 锁定规则

- v0.1 锁定四个 capability ID；管理员只能改绑定、测试连接、切换 `enabled_by_default`，**不能新增 capability**。
- 新增 capability 必须代码新增 adapter 与 Zod schema（对应到代码新增 `backend/src/integrations/capabilities/<id>.ts`）。
- 关闭 capability 时后端不得调用任何 AI 供应商；前端 Switch 与后端双重把关。

### 9A.4 Provider Binding 规则

- 每个 capability 在 `settings.json` 中保存 `{ provider_id, model, enabled_by_default }`；`provider_id` 必须引用已配置并启用的 `ai_providers[]` 条目。
- 项目级 override（`settings.capability_state.<id>.binding_override`）可覆盖默认绑定；override 优先级高于系统默认；override 引用 profile 必须 enabled。
- 切换 profile 引用 capability → 旧引用方（项目 override 或其他 capability）若仍引用旧 profile → 返回 409 `AI_PROVIDER_IN_USE`。

## 9B. Creative Director 视角规范（CreativePlan）

`CreativePlan` 是系统对"视频长什么样"的**结构化、可解释、可校验**的描述。它不依赖具体 AI/规则实现，由 `PlannerService`（AI）或 `RulePlanner`（规则）产出，schema 一致（Task 21 parity）。

### 9B.1 顶层字段

```ts
export const CreativePlanSchema = z.object({
  schema_version: z.literal(1),
  id: z.string().uuid(),
  status: z.enum(["draft", "proposed", "confirmed", "applied_to_storyboard", "stale"]),
  source: z.string(),                          // "rule_planner" 或 "provider:<id>+<model>"
  selected_direction_index: z.number().int().min(0).max(2),
  input_digest: z.string().regex(/^sha256:[0-9a-f]{64}$/),

  creative_intent: CreativeIntentSchema,
  narrative_arc: NarrativeArcSchema,
  emotional_curve: EmotionalCurveSchema,
  bgm_rationale: BGMRationaleSchema,
  directions: z.array(DirectionSchema).min(2).max(3),
  scene_plan: ScenePlanSchema.optional(),       // 仅在用户选方向后存在
});
```

### 9B.2 CreativeIntent

```ts
CreativeIntentSchema = z.object({
  theme: z.string().max(200),
  opening: z.string().max(300),
  development: z.string().max(300),
  climax: z.string().max(300),
  resolution: z.string().max(300),
  mood: z.array(z.string()).max(8),
});
```

### 9B.3 NarrativeArc（4 段必填）

```ts
NarrativeArcSchema = z.object({
  beats: z.array(z.object({
    beat: z.enum(["setup", "development", "climax", "resolution"]),
    scene_ids: z.array(z.string().uuid()).min(1),
    summary: z.string().max(200),
  })).length(4),                                // 严格 4 段
  bookend: z.object({
    opening_scene_id: z.string().uuid(),
    closing_scene_id: z.string().uuid(),
    echo_description: z.string().max(200),
  }),
});
```

- 即使是 15s 视频也要保留 4 段叙事；"发展"可以压缩为 1 个镜头。
- `bookend.opening_scene_id == closing_scene_id`（复用素材 ID 或同 motion+transition）；`EditorSpecValidator` 必须校验。

### 9B.4 EmotionalCurve

```ts
EmotionalCurveSchema = z.array(z.object({
  at_seconds: z.number().min(0),
  valence: z.number().min(-1).max(1),           // -1 消极 .. +1 积极
  arousal: z.number().min(0).max(1),
  visual_cue: z.string().max(120),
  bgm_cue: z.string().max(120),
  subtitle_cue: z.string().max(120),
})).min(4);                                      // 至少 4 点（开头/发展/高潮/收尾）
```

### 9B.5 BGM Rationale

```ts
BGMRationaleSchema = z.object({
  selected_track_id: z.string(),                 // 本地曲库 id，绝不暴露文件路径
  bpm: z.number().int().min(40).max(220),
  energy: z.enum(["low", "medium", "high"]),
  reason: z.string().max(300),
});
```

### 9B.6 Directions（2–3 选 1）

```ts
DirectionSchema = z.object({
  index: z.number().int().min(0).max(2),
  title: z.string().max(80),
  summary: z.string().max(300),
  visual_keywords: z.array(z.string()).max(10),
  differentiator: z.string().max(300),
});
```

- 用户通过 `POST /creative-plan:select-direction` 选择一个方向；后端才展开 `scene_plan`。

### 9B.7 ScenePlan（用户选方向后展开）

```ts
ScenePlanSchema = z.object({
  duration_seconds: z.number().min(15).max(60),
  scenes: z.array(SceneSchema).min(1),
});

SceneSchema = z.object({
  id: z.string().uuid(),
  beat: z.enum(["setup", "development", "climax", "resolution"]),
  asset_id: z.string().uuid().nullable(),       // null 表示 text_card
  text_card: z.object({
    headline: z.string().max(60),
    sub: z.string().max(120).optional(),
  }).nullable(),
  start: z.number().min(0),
  duration: z.number().min(1).max(8),
  subtitle: z.string().max(120).nullable(),
  motion: MotionWhitelistSchema,
  transition: TransitionWhitelistSchema,
  emotion_point: z.object({
    at_seconds: z.number(),
    valence: z.number(),
    arousal: z.number(),
  }),
});
```

### 9B.8 Stale 规则

任何输入变化（素材/文字/风格/capability override/feedback 应用/确认门禁条件变化）使 `CreativePlan.status` 立即变为 `stale`，禁止渲染；用户必须重新生成并重新确认。

## 9C. Video Editor 视角规范

所有场景编辑器/字幕/动效/转场/BGM 输出**必须**满足本节规范；不满足时 `EditorSpecValidator` 返回 `EDITOR_SPEC_VIOLATION` 并阻止渲染。这些数字同时锁进 Task 19 的契约测试，AI 输出可被自动校验。

### 9C.1 时间线契约

| 维度 | 规格 |
|---|---|
| 输出尺寸 | 1080×1920（9:16 竖屏），30 fps，`yuv420p` |
| 总时长 | 15–60 秒（`scene_plan.duration_seconds` ∈ [15, 60]） |
| 单镜时长 | 2.5–6.0 秒 |
| 镜头衔接 | 除末镜外，每镜结尾与下一镜重叠转场；`next_start = current_start + current_duration - transition_duration` |
| 首镜淡入 | 0.4 秒 |
| 末镜淡出 | 0.6 秒且 `transition=none` |
| `Storyboard.duration_seconds` | 末镜 `start + duration`；ffprobe 允许误差 ≤0.5 秒 |

### 9C.2 安全区

- 顶部安全区：屏幕顶 8%（避免平台 UI 遮挡）。
- 底部安全区：屏幕底 12%（字幕放置区域）。

### 9C.3 字幕规格

| 维度 | 规格 |
|---|---|
| 行数 | 1–2 行 |
| 每行字数 | 中文 ≤18 字 / 英文 ≤36 字 |
| 字号 | 屏幕高 4.5%（1080×1920 下约 86 px 等效） |
| 位置 | 底部安全区内，下边距 = 屏幕高 12% |
| 对齐 | 居中 |
| 描边 | 黑色 2 px，50% 不透明阴影 |
| 字体 | 内置 Noto Sans SC（项目 `assets/fonts/NotoSansSC-Regular.otf`） |
| 动画 | 入场淡入 0.2s；出场无（与下一镜重叠转场时切走） |
| 烧录 | FFmpeg/libass；ASS 写入 `revisions/<rid>/subtitles.ass` |

### 9C.4 BGM 规格

| 维度 | 规格 |
|---|---|
| 源 | 本地授权曲库，按 `bgm_rationale.selected_track_id` 选取；运行时禁止重新选曲 |
| 淡入 | 1.0 秒 |
| 淡出 | 1.0 秒 |
| 响度归一化 | -16 LUFS |
| 循环点 | 检测静音或 BPM 对齐的弱拍 |
| 裁剪 | 按目标时长 `target_duration = scene_plan.duration_seconds`，循环或截断 |
| 验证 | worker 读取前校验文件 SHA-256 与 `render_input.json` 中 frozen 摘要一致；`LICENSES.md` 摘要不一致则拒绝渲染 |

### 9C.5 动效白名单

```ts
export const MotionWhitelistSchema = z.enum([
  "kenburns_in", "kenburns_out", "kenburns_pan",
  "push_in", "push_out", "pan_left", "pan_right",
  "particles_drift", "geometric_rotate", "static",
]);
```

- `kenburns_*` 缩放比例 1.05–1.15；`push_in/out` 强度 0.05–0.1；`pan_*` 距离 ≤ 屏幕宽 10%。
- 升学宴/生日宴避免频闪与过度故障效果；科技感可使用 `geometric_rotate` 与轻微 `particles_drift`。

### 9C.6 转场白名单与时长

```ts
export const TransitionWhitelistSchema = z.enum([
  "fade", "slide_left", "slide_right", "zoom_cut", "none",
]);
```

| 风格 | 默认转场时长 |
|---|---|
| 升学宴 / 生日宴 / 婚礼 | 0.6–0.8 秒 |
| 轻松幽默 / 科技感 | 0.4–0.5 秒 |
| 浪漫温馨 | 0.7–0.9 秒 |

- 任意转场时长不得超过相邻任一镜头时长的 25%。
- `EditorSpecValidator` 在渲染前校验时长一致性。

### 9C.7 色彩与 LUT

- preset 色卡作为全局 LUT 应用；每个 scene 在 LUT 之上仅允许亮度/饱和度微调 ±10%。
- FFmpeg filter 通过白名单构建器生成；用户/AI 文本不得进入 filter 表达式。
- 字幕描边颜色、阴影透明度按 §9C.3 固定，不通过 AI 改写。

## 10. 非功能需求

### 10.1 性能与容量

- 目标环境：4 vCPU、8 GB RAM、可用磁盘不少于 20 GB。
- 30 秒、10 张 1080p 图片的视频，在目标服务器上 3 分钟内完成作为首版性能目标，不作为跨硬件绝对承诺。
- 上传使用流式写盘，不把整个文件读入内存。
- 渲染队列默认并发 1；风格分析队列默认并发 1。Python/Go/Rust 工具运行在 Linux 受限容器/用户中，总 CPU/内存/进程/磁盘受配额控制。
- 单个参考视频默认 ≤5 分钟/200 MB/720p 分析副本；分析目标 3 分钟内完成，重模型关闭时应更快。
- 视频响应支持 HTTP Range。
- OSS 路径下走签名 URL 直传/直读，前端上传分片 ≤10 MB；下载走 Range；OSS endpoint 不进入前端打包。

### 10.2 安全与隐私

- 根据文件魔数和 Sharp 解码结果验证类型，不信任扩展名和客户端 MIME。
- 对像素总数设上限，防止解压炸弹；移除 EXIF 元数据。
- 文件名只用于展示，磁盘路径全部使用服务端 UUID；拒绝路径穿越和符号链接。
- 参考 URL、AI provider URL、OSS endpoint 三者使用不同 schema 和网络策略：参考 URL 仅允许平台 host；provider URL 允许管理员确认的公网 HTTPS provider；OSS endpoint 白名单放宽到阿里云公网 endpoint。三者均执行 DNS/IP/重定向防 SSRF。
- 所有第三方 CLI 使用绝对可执行路径、固定参数模板、无 shell、独立临时目录和 JSON 输出 schema；工具版本及容器 digest 冻结到 revision。
- 参考视频授权声明、分析时间和来源 URL 摘要写入审计；公开日志不记录完整带 query URL。
- 限制请求体、上传数量、文字长度和同时任务数。
- 生产环境限定 CORS 来源并使用 HTTPS；反向代理以 Basic Auth 保护编辑 SPA 和项目 API；匿名访问仅放 `/health`（返回固定 `{"status":"ok"}`，不泄露版本/路径/能力）。
- Basic Auth 凭据只以部署 secret/htpasswd 文件注入，不进入仓库、镜像、浏览器持久存储或项目文件；MiniMax Key 与 OSS AccessKey 按用户要求只存 `data/system/settings.json`，该文件权限必须为 0600、禁止提交 Git 和默认备份。前后端不记录凭据、Key、完整提示词或字幕正文。
- 系统设置路由与页面必须受 Basic Auth 保护并启用 CSRF/Origin 校验；生产只允许 HTTPS。GET/错误/capabilities 永不返回完整 Key，PUT 请求体不得进入访问日志或错误上报。
- **OSS 凭据按 0600 持久化；首选 STS 临时凭证（≥15 分钟过期自动续签）；AccessKey 直填需管理员二次确认 + warning；signed URL TTL 默认 3600 秒（范围 60–86400s）；OSS endpoint 走与 `SafeProviderHttpClient` 一致的 SSRF/DNS 固定规则但白名单放宽到阿里云 OSS 公网 endpoints。**
- 四个 capability Switch 默认关闭；关闭时后端不得发起供应商请求。所有 capability 提案必须以服务端状态和摘要校验显式同意，不能仅依赖前端按钮状态。
- 32 字节安全随机数 + SHA-256 + `crypto.timingSafeEqual` 模式保留，供 OSS signed URL 签名与未来 capability-binding secret 复用。
- 明示用户必须拥有上传素材及音乐的使用权。

### 10.3 可观测性

- JSON 结构化日志字段：`request_id`、`project_id`、`revision_id`、`capability_id`、`binding_provider`、`binding_model`、`storage_backend`、`stage`、`duration_ms`、`error_code`。
- `/health` 只判断进程；`/api/v1/capabilities` 从系统设置的脱敏快照报告 FFmpeg、字体、曲库、已采用开源组件及四个 capability。
- 系统设置变更日志只记录操作者、时间、配置版本和"Key已设置/替换/清除"、"OSS 凭证模式切换"，不记录 Key、API Base 查询参数、OSS AccessKey 或请求体。
- 每个 revision 保存 FFmpeg stderr 到 `render.log`，API 只返回安全摘要。
- 记录上传数、队列长度、各阶段耗时、成功率和磁盘余量；首版先输出日志，后续接监控系统。

## 11. 测试与验收策略

### 11.1 测试层级

- 后端单元测试：Zod、文件仓库、provider profile/绑定、URL 安全、StyleProfile 合并、创意规则、反馈白名单、M3/H3/planner/feedback_parser 同意门禁与降级、状态机、ASS、**EditorSpecValidator（§9C 契约）**、**CreativePlan 状态机与 stale 规则**。
- **能力注册表契约测试：** 四个 capability ID 锁定；`CapabilityRegistry.resolve(id)` 在 v0.1 不允许 id 拼写漂移；admin 不能通过 settings.json 新增 capability。
- **planner parity 测试（Task 21）：** 同一份输入（同一素材列表/文案/StyleProfile/duration_hint）下，`PlannerService`(AI mock) 与 `RulePlanner` 产出的 `CreativePlan` 通过同一 Zod schema 校验，且关键字段（`creative_intent.theme` / 4 段叙事弧 beat 顺序 / `directions[].title` / `bgm_rationale.selected_track_id`）在确定性 seed 下完全相同或可解释地映射。
- **storage backend 测试：** `MemoryStorageAdapter` 跑通所有 9 个方法；`LocalFileSystemAdapter` 权限位 0700/0600 校验；`AliyunOSSAdapter` 用 mock SDK 跑通 STS 续签、AK 二次确认、signed URL TTL、`ping()` 失败处理；`storage-migrate.ts` 的 `export/import/verify` 三命令 round-trip 测试。
- 工具契约测试：对 yt-dlp/FFmpeg/OpenCV/PySceneDetect/librosa/PaddleOCR/CLIP 使用固定 fixtures 验证 JSON schema、超时、资源限制、部分失败降级和版本记录。
- 后端集成测试：Supertest 覆盖风格/分析/创意方案/反馈/provider/渲染/存储 API；真实 FFmpeg 生成图片型和纯文字型 MP4；外部平台与 AI 供应商使用受控 mock。
- 前端组件测试：三种风格模式、预设下拉、自定义输入、参考 URL/授权、画像编辑确认、四个 capability Switch、AI 供应商管理、能力绑定、CreativePlanStep（方向选择 + 方案确认 + bookend 校验）、反馈计划与 final 标记、**FeedbackParserPreview**、**CapabilityBindingsTable**、**StorageSettingsForm**。
- E2E：分别覆盖预设升学宴、生日宴、自定义、mock 参考链接、纯文字和图文输入；循环两次反馈后 final、下载；**Capability ON/OFF 全路径**；**storage backend 切换**（local → OSS mock）；AI 默认关闭及自定义供应商可选路径。
- 人工视觉验收：升学宴、生日宴、自定义、参考画像和纯文字至少各一条，检查创意合理性（`creative_intent` / 4 段叙事弧 / bookend）、字幕安全区、节奏、BGM、特效和移动端播放。

### 11.2 MVP 完成定义

以下条件全部满足才算首版完成：

1. 用户可只填文字、只传图片或二者结合；至少一项非空。纯文字输入可生成包含程序化背景、动态文字卡、BGM、字幕和特效的视频。
2. 风格来源三选一：预设下拉至少含升学宴/生日宴/婚礼等，自定义描述可编辑，参考视频链接可异步分析。
3. 参考链接必须经授权确认、平台/大小/时长/安全校验；输出主题、色调、节奏、字幕布局、BPM/音乐情绪和置信度，用户确认画像后才能生成。
4. 系统必须针对简单描述主动生成 `CreativePlan`：含 `creative_intent` + 4 段叙事弧 + bookend + 情绪曲线 + 2–3 个 `directions`（用户选 1）+ 完整 `scene_plan` + BGM 理由；`PlannerService` 与 `RulePlanner` 输出 schema parity，AI 关闭/失败时规则流程完整可用。
5. 四个 Ant Design Switch（planner / prompt_optimizer / caption_effects / feedback_parser）默认未选中且独立；prompt_optimizer 提案须接受、planner 须方向选择 + 方案确认、caption_effects 须分镜确认、feedback_parser 须白名单操作 + 置信度预览。
6. 管理员可在网页新增/修改/测试/禁用 AI 供应商 profile（供应商、协议、请求地址、模型、API Key），并在能力绑定表里分别为四个 capability 绑定 provider/model；默认 planner/prompt_optimizer/feedback_parser → MiniMax-M3，caption_effects → MiniMax-H3。任何 GET/日志不回显 Key。
7. 管理员可在系统设置选择存储后端（local 默认 / aliyun_oss 可选），填写 endpoint/bucket/region/prefix 与 STS 或 AK；切换 backend 不做自动迁移，`scripts/storage-migrate.ts` 的 `export/import/verify` round-trip 一致。`ProjectRepository` 接口已定义，`DbProjectRepository` 占位文件存在且启动配置 `REPOSITORY_BACKEND` 仅接受 `local`。
8. 用户可在预览后提交自由文本+结构化修改意见；`feedback_parser` 开启时调用 capability 并显示每个操作 `confidence`，否则使用有限意图词典；系统生成可确认修改计划，每次应用创建新 revision 并保留父子关系，至少连续完成两轮反馈仍可回退历史。
9. 用户可将满意 revision 标记 final、下载；同项目仅一个 final，可撤销继续修改。
10. 输出为 1080×1920 H.264/AAC MP4，包含确认后的风格、BGM、字幕、动效和转场；FFmpeg `ffprobe` 验证编码、尺寸、时长（误差 ≤0.5s）、`yuv420p`。
11. yt-dlp/FFmpeg/OpenCV 等工具在 Linux 受限执行，原始参考副本分析后删除；部分分析工具失败可降级并显示置信度/警告。
12. 所有首选开源候选实施时核验 Star≥6000、许可、维护和安全；低于门槛必须书面例外。核心流程不依赖付费或不稳定免费额度 API。
13. 页面持续显示分析/渲染状态，刷新和服务重启可恢复；非法 URL/文件、超限、磁盘不足、工具/AI 失败有稳定错误码且不泄密。
14. Ant Design 是唯一主要 UI 库；Web 主栈保持 React/Vite/Express TypeScript，Python/Go/Rust 仅作为 Linux 受控工具/容器。
15. `docs/oss-adoption.md` 和第三方通知记录 Star 快照、版本/commit、许可证、最小复用模块、Linux 方式、资源与替代方案。
16. **`§9C 编辑器规范全部通过 EditorSpecValidator 与契约测试`**（时间线/安全区/字幕/BGM/动效白名单/转场白名单/色彩）。
17. **`CapabilityRegistry` 锁定四个 capability ID**；admin 不能通过 settings.json 新增 capability；`docs/capability-scenarios.md` 记录场景映射。
18. **`scripts/storage-migrate.ts` round-trip 通过**；切换 backend 后 SHA-256 一致。
19. Vitest/Supertest、工具契约、真实 FFmpeg、前端组件和 Playwright E2E 全部通过。
20. README 可让开发者启动 Web 与分析工具，在 AI 关闭状态完成"文字/图片→风格→创意视频→两轮反馈→final 下载"。

---

# 实施计划

> **For agentic workers:** 实施前使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`，逐任务执行；每个步骤使用 `- [ ]` 跟踪。

**目标：** 交付 Linux 单服务器上的视频创作网站：文字/图片 + 预设/自定义/参考风格 → planner 生成结构化 CreativePlan → 多轮 feedback → final 下载（不做分享）。  
**架构：** React/Ant Design 调用 Express TypeScript；Web 服务通过受控 adapters 调用不限语言的高质量开源 CLI/容器，分析参考风格并用 FFmpeg 渲染。**AI 能力通过 CapabilityRegistry 管理（v0.1 四个固定 ID：planner / prompt_optimizer / caption_effects / feedback_parser），管理员在系统设置按 capability 绑定供应商/模型**。**存储通过 StorageBackend 接口抽象（默认 local，可选 Aliyun OSS）**；ProjectRepository 接口留 DB seam（v0.3 接入）。  
**技术栈：** Node.js 22、Express 5、TypeScript、Zod、React/Vite/Ant Design、Sharp、FFmpeg/ffprobe、yt-dlp、Python 分析容器（OpenCV/PySceneDetect/librosa，可选 PaddleOCR/CLIP）、官方阿里云 OSS SDK、Vitest/Supertest/Playwright、Docker Compose。

## 12. 实施任务

### Task 1：初始化 npm 工作区、Express 和 Ant Design 基线

**文件：** 创建 `.gitignore`、`package.json`、`package-lock.json`、`tsconfig.base.json`、`eslint.config.js`、`README.md`、`THIRD_PARTY_NOTICES.md`、`docs/oss-adoption.md`、`docs/capability-scenarios.md`、`frontend/package.json`、`frontend/vitest.config.ts`、`frontend/src/test/setup.ts`、`backend/package.json`、`backend/tsconfig.json`、`backend/vitest.config.ts`、`backend/src/app.ts`、`backend/src/server.ts`、`backend/src/config.ts`、`backend/tests/`、`data/.gitkeep`、`assets/fonts/LICENSES.md`、`assets/music/LICENSES.md`、`assets/backgrounds/LICENSES.md` 及六类风格目录的 `.gitkeep`。

- [ ] 确认本机安装 Node.js 22、npm、FFmpeg 7 和 Git；运行 `node -v && npm -v && ffmpeg -version && ffprobe -version`，四项均成功。
- [ ] `.gitignore` 忽略 node_modules、构建/模型缓存、`data/projects/`、`data/trash/`、`data/system/settings.json` 和参考临时文件；不创建任何 AI 密钥环境变量示例。
- [ ] 创建 npm workspaces 和共享 TypeScript 配置；所有依赖使用 `--save-exact`，提交唯一根 lockfile。根开发依赖精确安装 TypeScript、tsx、Vitest、ESLint 及 TypeScript 插件、Prettier；后端安装 Supertest 及类型、Express 类型；前端安装 Testing Library、user-event、jest-dom 和 jsdom。
- [ ] 前端精确安装 React、Vite、Ant Design、icons、TanStack Query；后端精确安装 Express 5、Zod、Sharp、Busboy、file-type、undici 及安全 IP 分类库；**安装官方阿里云 OSS SDK 作为 dependency（虽 v0.1 不强制启用，但因包管理要求统一安装并 `--save-exact`）**。创建前后端 Vitest 配置、测试 setup、ESLint 配置和最小健康检查测试，禁止 npm exec 临时下载未锁定包。
- [ ] 前端在 `ConfigProvider` 中设置中文 locale 和主题 token；后端创建最小 `app.ts`/`server.ts`，实现 `GET /health`。配置根脚本 `dev/build/typecheck/lint/test:backend/test:frontend`，每个命令只使用 lockfile 依赖。
- [ ] 创建 `assets/music/{celebratory,birthday,wedding,humorous,romantic,tech}`、`assets/backgrounds/`、`assets/fonts/` 和许可证记录模板；建立 OSS 审计模板与第三方通知。
- [ ] README 写明准确命令；运行 `npm ls --all` 确认无缺失/外部临时依赖，CI 使用 `npm ci` 可从 lockfile 离线复现已缓存安装。
- [ ] 运行 `npm run typecheck && npm run build && npm run test:backend -- --run && npm run test:frontend -- --run`，预期全部退出码 0。
- [ ] 提交：`chore: initialize express and ant design workspace`。

### Task 2：领域模型、高星开源审计与多语言工具契约

**文件：** 创建 `backend/src/domain/schemas.ts`、`backend/src/tools/schemas.ts`、`scripts/audit-oss.ts`、`scripts/check-tools.ts`、`analysis-tools/requirements.lock`、`analysis-tools/analyze_reference.py`、`analysis-tools/tests/`；修改 `docs/oss-adoption.md`、`THIRD_PARTY_NOTICES.md`。

- [ ] 用 Zod 定义 Project、ContentInput、StyleSelection、StyleProfile、StyleAnalysisJob、FeedbackPlan、AIProviderProfile、**CreativePlan（含 4 段 narrative_arc、bookend、emotional_curve、directions、scene_plan、status 状态机）**、CapabilityBinding、StorageConfig、Revision；Python 输出 schema 必须有相同版本和 contract fixtures。
- [ ] 审计 yt-dlp、FFmpeg、OpenCV、PySceneDetect、librosa、PaddleOCR、CLIP/OpenCLIP、MoneyPrinterTurbo、OpenMontage、Pixelle、hyperframes、video-use、NarratoAI、autoclip、Sharp、Ant Design、**官方阿里云 OSS SDK**；记录 Star 快照日期、是否 ≥6000、许可证、维护、安全、Linux 方式、资源和复用模块。
- [ ] `audit-oss` 默认拒绝 Star<6000、归档、许可证不兼容或高危未解决项目；低 Star 例外必须含 `exception_owner/reason/no_alternative/expiry`。flycut-caption、videoflow、short-video-factory 不得默认进入依赖。
- [ ] Python 分析依赖精确锁定且带 hash；`analyze_reference.py` 从 stdin/文件读取固定 job JSON，只向 stdout 输出版本化 JSON，日志走 stderr，不接受命令字符串。
- [ ] `check-tools.ts` 验证 Linux 可执行文件/容器 digest、版本范围、许可证清单和 JSON schema；缺少可选 OCR/CLIP 只报告 degraded，缺少 yt-dlp/FFmpeg/OpenCV 核心则 capability 不可用。
- [ ] 运行 `npm exec -- tsx scripts/audit-oss.ts --check --min-stars 6000 docs/oss-adoption.md && npm exec -- tsx scripts/check-tools.ts --contract-only && npm run test:backend -- --run backend/tests/domain backend/tests/tools`，预期通过。
- [ ] 提交：`feat: define domains and audited tool contracts`。

### Task 3a：文件仓库与 capability 注册表数据模型

**文件：** 创建 `backend/src/repositories/projects.ts`（interface）、`backend/src/repositories/file-projects.ts`（LocalProjectRepository）、`backend/src/repositories/db-projects.ts`（v0.1 占位）、`backend/src/repositories/system-settings.ts`（升级到 schema_version=3）、`backend/src/services/system-settings.ts`（升级）、`backend/src/api/capabilities.ts`、`backend/src/integrations/capabilities/registry.ts`（CapabilityDescriptor + 四个 capability 描述符注册）；测试对应文件。

- [ ] 实现 `ProjectRepository` interface 与 `LocalProjectRepository`（Promise mutex + version CAS + 临时文件 + sync + rename）；`DbProjectRepository` 占位文件，方法抛 `Error("DB not enabled in v0.1")`；启动配置 `REPOSITORY_BACKEND` 仅接受 `local`，其他值启动报错。
- [ ] 实现 settings.json v3（capability_bindings 4 项 + storage 段）；首次启动创建不含 Key 的 MiniMax 默认 profile、四个 capability 默认绑定、storage backend=local、0700/0600 权限。
- [ ] 实现 provider profile CRUD、连接测试、能力绑定 API、**存储后端 API**；Key/AccessKeySecret 留空保持、替换后清空输入、GET 只返回掩码；绑定/引用中的 profile 不能删除；切换 storage backend 走二次确认 + warning。
- [ ] 定义不可混用的 `InternalProviderCredentials` / `InternalOSSCredentials` 与 `PublicProviderProfile` / `PublicStorageConfig`；类型/运行时测试保证 Key/AccessKeySecret 不能 JSON 序列化、记录或进入 Express response。
- [ ] `CapabilityRegistry` 在 v0.1 锁定四个 capability ID（planner / prompt_optimizer / caption_effects / feedback_parser）；提供 `resolve(id) → { descriptor, adapter, binding, gate }`；测试保证非这四个 ID 的调用抛 `CAPABILITY_NOT_REGISTERED`。
- [ ] `/api/v1/capabilities` 返回高星工具版本/可用性、storage backend 类型与状态、四个 capability 的 provider/model 与 enabled_by_default；设置变更热更新，无需重启。
- [ ] 运行 `npm run test:backend -- --run backend/tests/repositories backend/tests/integrations/capabilities backend/tests/api/system-settings.test.ts backend/tests/api/capabilities.test.ts`，预期全部通过且无 Key 泄漏。
- [ ] 提交：`feat: add provider registry capability binding and file settings`。

### Task 3b：Storage Backend 接口与 Local / Memory 实现

**文件：** 创建 `backend/src/storage/backend.ts`（interface）、`backend/src/storage/local.ts`（LocalFileSystemAdapter）、`backend/src/storage/memory.ts`（MemoryStorageAdapter）；测试 `backend/tests/storage/`。

- [ ] 实现 `StorageBackend` 9 个方法（putObject/getObjectStream/headObject/deleteObject/exists/list/copy/signedUrl/ping）；URI 约定 `local://` / `oss://`。
- [ ] `LocalFileSystemAdapter`：`local://` 映射到配置根目录；0700 目录 / 0600 文件；`signedUrl` 返回 `/api/v1/storage/local/{key}?...` + HMAC 签名。
- [ ] `MemoryStorageAdapter`：测试用，进程内 Map；覆盖所有方法。
- [ ] `ping()` 返回 `{ ok, latency_ms, error_code? }`；local 走 stat，memory 走直接 true。
- [ ] 启动时根据 `settings.json.storage.backend` 注入唯一一个 adapter；切换 backend 需重启或显式热重载。
- [ ] 运行 `npm run test:backend -- --run backend/tests/storage`，预期全通过。
- [ ] 提交：`feat: add storage backend abstraction and local adapter`。

### Task 3c：Aliyun OSS Adapter（feature-flagged）

**文件：** 创建 `backend/src/storage/aliyun-oss.ts`（AliyunOSSAdapter）；测试 `backend/tests/storage/aliyun-oss.test.ts`（mock SDK）。

- [ ] 实现 `AliyunOSSAdapter`；URI 前缀 `oss://`；`signedUrl` 返回真实 presigned URL；优先 STS 临时凭证（自动续签），静态 AccessKey 需管理员二次确认 + warning。
- [ ] 与 `SafeProviderHttpClient` 类似做 SSRF/DNS 防护，但 endpoint 白名单放宽到阿里云 OSS 公网 endpoint（如 `oss-cn-hangzhou.aliyuncs.com`）；列表由 `scripts/audit-oss.ts` 锁住。
- [ ] `ping()` 调用 bucket `head` 或 `list`；OSS 错误转换为 `STORAGE_BACKEND_UNAVAILABLE` / `OSS_PERMISSION_DENIED`。
- [ ] mock SDK 测试覆盖：正常 get/put/list、STS 过期自动续签一次、AK 二次确认触发、signed URL TTL、`ping()` 失败。
- [ ] 提交：`feat: add aliyun oss adapter (feature-flagged)`。

### Task 4：实现项目 CRUD API

**文件：** 创建 `backend/src/api/projects.ts`、`backend/src/api/errors.ts`；修改 `backend/src/app.ts`；测试 `backend/tests/api/test_projects.ts`。

- [ ] API 测试覆盖文字-only、图片-only、图文、全空拒绝；preset/custom/reference 三种 StyleSelection 互斥；切换风格/画像、素材、feedback 会清空确认，标题不清空。
- [ ] Express Router + Zod 实现项目 CRUD 和 style-selection；预设 ID 必须来自版本化库，自定义描述限制长度，reference 必须指向已分析记录。
- [ ] PATCH 接受业务白名单字段（含 `capability_state.<id>.enabled` 与 `binding_override`）；**客户端不能直接设置 accepted proposal、confirmed profile、confirmed CreativePlan、final、工具来源或模型来源**。
- [ ] 删除采用移入 trash；存在活动渲染时返回 `409 RENDER_ALREADY_ACTIVE`。
- [ ] **manifest 不含 share 字段；项目详情 endpoint 不返回任何分享相关结构。**
- [ ] 运行 `npm run test:backend -- --run backend/tests/api/test_projects.ts`，预期全通过。
- [ ] 提交：`feat: add project management api`。

### Task 5：实现文字/图片素材输入与规范化

**文件：** 创建 `backend/src/services/uploads.ts`、`backend/src/api/assets.ts`；测试 `backend/tests/services/test_uploads.ts`、`backend/tests/api/test_assets.ts`，素材 `backend/tests/fixtures/images/`。

- [ ] 测试 JPG/PNG/WebP、伪文件/超限/像素炸弹/路径穿越，以及 0 图片 + 文字成功、图片 + 空文字成功、两者全空失败。
- [ ] Busboy 流式写盘并用 file-type + Sharp 双检；文字按 Unicode 长度/控制字符/HTML 清理规则校验，但保留用户原始纯文本语义。
- [ ] Sharp 纠正 EXIF、转 sRGB、去元数据；写入 `StorageBackend`（local/oss 均可）；无图片时生成器不创建伪上传，而在 RenderPlan 中创建 text_card 场景。
- [ ] 上传、删除、排序、文字或逐图文案变化均清空风格/分镜/CreativePlan 相关确认；历史 revision 引用文件保持不可变。
- [ ] 运行 `npm run test:backend -- --run backend/tests/services/test_uploads.ts backend/tests/api/test_assets.ts`，预期全通过。
- [ ] 提交：`feat: add validated image uploads`。

### Task 6：实现预设/自定义/参考视频风格分析

**文件：** 创建 `backend/src/tools/runner.ts`、`backend/src/tools/reference-analyzer.ts`、`backend/src/services/reference-styles.ts`、`backend/src/services/style-profiles.ts`、`backend/src/api/styles.ts`、`backend/src/api/references.ts`、`backend/src/jobs/analysis-queue.ts`；测试对应 tools/services/api/jobs 文件。

- [ ] 建立升学宴、生日宴、婚礼、浪漫、幽默、科技的版本化 StyleProfile fixtures，测试 preset/custom/reference 三种来源统一输出 schema。
- [ ] 实现参考 URL 校验和授权门禁：HTTPS、平台 allowlist、单视频、≤5 分钟/200 MB、无 Cookie/登录/DRM；所有参数数组调用，无 shell。
- [ ] LinuxToolRunner 以受限用户/容器执行固定版本 yt-dlp/FFmpeg 和 Python 分析器，限制 CPU/内存/PID/文件/网络/超时，截断脱敏 stderr，始终清理原视频。
- [ ] 合并 OpenCV/PySceneDetect/librosa/PaddleOCR/CLIP 输出；核心字段按置信度选取，OCR/CLIP 等可选失败生成 warning 而不阻塞。
- [ ] 实现分析队列、进度/取消/重启恢复、profile 编辑/确认 API；画像未确认时 `StoryboardService.generate` 与 `PlannerService.generate` 返回 `STYLE_PROFILE_NOT_CONFIRMED`。
- [ ] 契约测试覆盖抖音/YouTube mock metadata、平台变更、playlist/超限/恶意 URL、工具超时/畸形 JSON/部分失败、临时文件零残留。
- [ ] 运行 `npm run test:backend -- --run backend/tests/tools backend/tests/services/reference-styles.test.ts backend/tests/api/references.test.ts backend/tests/jobs/analysis-queue.test.ts`，预期全通过。
- [ ] 提交：`feat: analyze and confirm video style profiles`。

### Task 7a：Capability Registry 骨架与通用 adapter

**文件：** 创建 `backend/src/integrations/capabilities/registry.ts`、`backend/src/integrations/capabilities/base.ts`（CapabilityAdapter 抽象类）、`backend/src/integrations/capabilities/planner.ts`（注册描述符 + mock 输出）、`backend/src/integrations/capabilities/prompt-optimizer.ts`、`backend/src/integrations/capabilities/caption-effects.ts`、`backend/src/integrations/capabilities/feedback-parser.ts`；测试 `backend/tests/integrations/capabilities/`。

- [ ] `CapabilityRegistry` 暴露 `resolve(id) / list() / describe(id)`；非四个 ID 之一抛 `CAPABILITY_NOT_REGISTERED`。
- [ ] 每个 capability 描述符含 `inputSchema / outputSchema / gate / fallback / defaultBinding / defaultEnabled`；`gate` 按 §9A.2 固定。
- [ ] CapabilityAdapter 基类负责超时（15s）、重试（≤1）、绑定查询、Zod 校验、失败 fallback；子类只实现 `callProvider(input) → output`。
- [ ] mock 测试每个 capability 的成功路径、绑定切换、禁用状态、超时、非法 schema。
- [ ] 提交：`feat: add capability registry and adapter skeleton`。

### Task 7b：Planner capability（默认 MiniMax-M3）

**文件：** 创建 `backend/src/services/planner.ts`（PlannerService）、`backend/src/services/rule-planner.ts`（RulePlanner，Task 21 parity）、`backend/src/api/creative-plan.ts`；修改 `backend/src/domain/schemas.ts`；测试 `backend/tests/services/planner.test.ts`、`backend/tests/services/rule-planner.test.ts`、`backend/tests/api/creative-plan.test.ts`。

- [ ] `PlannerService.generate(input)`：调用 `CapabilityRegistry(planner)`；失败/关闭/超时/校验不过 → 降级 `RulePlanner`；记录 `source`。
- [ ] `CreativePlan` Zod 含 `creative_intent / narrative_arc / emotional_curve / bgm_rationale / directions / scene_plan / status / input_digest`；`status` 初始 `proposed`，无 `scene_plan`。
- [ ] `POST /creative-plan:select-direction`：展开 `scene_plan`，仍 `proposed`；`POST /creative-plan:confirm`：校验 input digest，状态 `confirmed`。
- [ ] 输入变化（素材/文字/风格/capability override/feedback 应用）使 `CreativePlan.status` → `stale`；UI 阻止渲染并提示重新生成。
- [ ] mock 测试覆盖：planner 成功 → 方案走通；planner 失败 → RulePlanner parity 输出；direction 选择 + confirm；stale 规则。
- [ ] 提交：`feat: add planner capability and creative plan workflow`。

### Task 7c：Prompt Optimizer capability（默认 MiniMax-M3）

**文件：** 修改 `backend/src/services/prompts.ts`（接入 CapabilityRegistry(prompt_optimizer)）、`backend/src/api/prompt-proposals.ts`；测试 `backend/tests/services/prompts.test.ts`、`backend/tests/api/prompt-proposals.test.ts`。

- [ ] PromptService 走 CapabilityRegistry(prompt_optimizer)；关闭/未配置/超时/失败 → `keep_original`（不动 active prompt）。
- [ ] 接受必须提交 proposal digest；拒绝/关闭/超时/离开页面均保持原提示词。
- [ ] 修改原文 → 清空旧 accepted_proposal_id，active_source 切回 original。
- [ ] mock 测试覆盖默认 MiniMax-M3 绑定及切换到 OpenAI-compatible profile/model 后热生效；覆盖禁用、无 Key、401/429/500、超时、非法 schema、Key 清除。
- [ ] 提交：`feat: add prompt optimizer capability binding`。

### Task 7d：Caption / Effects capability（默认 MiniMax-H3）

**文件：** 修改 `backend/src/services/storyboards.ts`（接入 CapabilityRegistry(caption_effects)）；测试 `backend/tests/services/storyboard-ai.test.ts`。

- [ ] AIContentService 调用 caption_effects 绑定；输入：当前 CreativePlan 摘要、素材顺序、StyleProfile；输出：每镜字幕/字幕行/动效/转场候选（白名单）。
- [ ] 输出 schema 校验 + 白名单校验 + §9C 编辑器规范校验；失败 → `RulePlanner` 字幕/动效。
- [ ] 标记实际 provider/model 进入 StoryboardEditor 确认；不得改变图片、时长上限、BGM 文件、路径或命令。
- [ ] mock 测试覆盖默认 MiniMax-H3 绑定及切换；禁用、非法 schema、降级。
- [ ] 提交：`feat: add caption and effects capability binding`。

### Task 7e：Feedback Parser capability（默认 MiniMax-M3）

**文件：** 修改 `backend/src/services/feedback.ts`（接入 CapabilityRegistry(feedback_parser)）；测试 `backend/tests/services/feedback-ai.test.ts`。

- [ ] FeedbackService 调用 feedback_parser 绑定；输入：原始文本 + 结构化控件 + 当前 CreativePlan + 当前 storyboard；输出：`changes[]`（白名单操作 + `confidence`）、`unsupported_text[]`、`per_op_confidence`、`rationale`。
- [ ] 关闭时使用有限意图词典（`parser_source="rule_dictionary"`），输出 schema 一致。
- [ ] 任何不被白名单覆盖的文本进入 `unsupported_text`；系统不得猜测；必须由用户用结构化控件澄清。
- [ ] mock 测试覆盖默认 MiniMax-M3 绑定及切换；禁用、非法 schema、降级、`unsupported_text` 触发。
- [ ] 提交：`feat: add feedback parser capability binding`。

### Task 8a：StoryboardService 接受 CreativePlan

**文件：** 修改 `backend/src/services/storyboards.ts`、`backend/src/api/storyboards.ts`；测试 `backend/tests/api/storyboards.test.ts`。

- [ ] `POST /storyboard:generate` 前置：StyleProfile 已确认 + CreativePlan 已确认 + 任何会改变确认载荷的变更都使旧确认失效。
- [ ] caption_effects 开启时仅替换字幕/动效字段；其他字段不动。
- [ ] 通过 adapters 复用审计通过的 MoneyPrinterTurbo / OpenMontage / Pixelle / hyperframes 独立算子；至少集成一个适配当前 schema 的高星分镜/模板算子；若全部因许可证/运行条件失败，先提交 ADR 和 spike 证据，再允许最小规则实现。
- [ ] 无图片时生成 text_card 和程序化背景场景；有图片时重排/裁切策略可解释且不丢素材引用。
- [ ] `EditorSpecValidator`（§9C）在 `generate` 与 `confirm` 前对 scene_plan 做契约校验；不通过返回 `EDITOR_SPEC_VIOLATION` 并列出违规字段。
- [ ] 运行 `npm run test:backend -- --run backend/tests/api/storyboards.test.ts`，预期通过。
- [ ] 提交：`feat: integrate storyboard with creative plan and editor spec`。

### Task 8b：FeedbackService 与白名单修改计划

**文件：** 修改 `backend/src/services/feedback.ts`、`backend/src/api/feedback.ts`；测试 `backend/tests/api/feedback.test.ts`。

- [ ] 实现 FeedbackService 白名单操作（`set_pace / set_caption_scale / set_palette / set_bgm_energy / set_bgm_volume_db / set_motion_intensity / set_transition_duration_seconds / reorder_scenes / trim_scene / rewrite_subtitle`）、`unsupported_text`、`before/after` 预览、`accept/reject/apply`。
- [ ] 应用创建新草稿并记录 parent revision，不改历史输出。
- [ ] `POST /feedback:plan` 走 CapabilityRegistry(feedback_parser)；关闭时使用有限意图词典，输出 schema 一致。
- [ ] feedback_parser 输出必须含 `per_op_confidence`；UI 显示置信度。
- [ ] 提交：`feat: integrate feedback service with feedback parser capability`。

### Task 9a：FFmpeg 渲染管线 + 编辑器契约

**文件：** 创建 `backend/src/render/pipeline.ts`、`backend/src/render/probe.ts`、`backend/src/render/text-cards.ts`、`backend/src/render/editor-spec.ts`（§9C 校验器）；测试 `backend/tests/render/test_pipeline.ts`、`backend/tests/render/test_editor_spec.ts`；生产字体、背景和六类 BGM 放入已登记 assets 目录。

- [ ] 集成测试同时渲染纯文字升学宴、图文生日宴和参考画像视频；断言程序化背景、动态文字、字幕、BGM、转场、取消和错误清理。
- [ ] 使用 spawn shell:false 和 AbortController 调用 FFmpeg；所有 filter 由白名单构建器生成。
- [ ] 实现图片 contain/裁切、text_card 渐变/纹理/几何动效、镜头 motion/transition、ASS（按 §9C.3 规格）、BGM 按 §9C.4（BPM 匹配/循环/淡化/-16 LUFS 归一化/1.0s 淡入淡出）和 H.264/AAC 编码。
- [ ] `EditorSpecValidator` 实现 §9C 全部校验（时间线、安全区、字幕、BGM、动效白名单、转场白名单、bookend）；不通过返回结构化错误并阻止渲染。
- [ ] 测试 fixture 使用脚本生成的纯色图片和正弦波音频，不冒充生产曲库；生产环境引入 Noto Sans SC（SIL OFL）及每种风格至少 1 首经负责人确认可商用/再分发的 BGM，并按 Task 1 字段登记许可证和 SHA-256。
- [ ] 实现 `scripts/check-assets.ts`，运行 `npm exec -- tsx scripts/check-assets.ts --font assets/fonts/NotoSansSC-Regular.otf --music-root assets/music --min-tracks-per-style 1 --verify-digests`；预期六类风格均通过且无未登记文件。
- [ ] 音乐选择只在创建 revision 时按 §9C.4 的分类→目录映射、规范化路径排序和 SHA-256 取模执行，并把曲目/字体路径、文件摘要和许可证条目摘要冻结到 `render_input.json`；worker 禁止重新选曲，没有许可证记录或摘要不匹配时拒绝渲染。
- [ ] 解析 FFmpeg `-progress` 输出，将阶段进度限制在 0–99；只有 probe 验证成功后写 100。
- [ ] 用 ffprobe 断言输出：1080×1920、30 fps、H.264、有音频时 AAC、时长误差不超过 0.5 秒、`yuv420p`。
- [ ] 运行 `npm run test:backend -- --run backend/tests/render/test_pipeline.ts backend/tests/render/test_editor_spec.ts`，预期全通过。
- [ ] 提交：`feat: render vertical videos with ffmpeg and editor spec`.

### Task 9b：资产 / CI smoke

**文件：** 创建 `scripts/check-assets.ts`、`scripts/check-ffmpeg.ts`；保持 CI 集成。

- [ ] `check-assets.ts` 校验字体/背景/音乐 SHA-256 与许可证条目摘要。
- [ ] `check-ffmpeg.ts` 校验 FFmpeg 版本与必备 filter。
- [ ] CI 阶段运行上述两个脚本。
- [ ] 提交：`chore: add asset and ffmpeg smoke checks`。

### Task 10：实现任务队列、恢复、取消与重试

**文件：** 创建 `backend/src/jobs/queue.ts`、`backend/src/api/renders.ts`；修改 `backend/src/app.ts`；测试 `backend/tests/jobs/test_queue.ts`、`backend/tests/api/test_renders.ts`。

- [ ] 测试分析/渲染队列互不覆盖、同类型并发 1、取消/恢复；render 拒绝未确认 StyleProfile/CreativePlan/storyboard/AI 提案或过期 feedback；排队后工具版本、provider/model、画像和资产均被冻结；retry 逐字节复制。
- [ ] 实现统一 JobSupervisor 管理 AnalysisQueue 和 RenderQueue；SIGTERM 停止接单，用 AbortController 终止 Node 子进程，并让 LinuxToolRunner 终止整个进程组/容器。
- [ ] `POST /renders` 锁内重构确认载荷，冻结 StyleProfile、CreativePlan（direction + scene_plan + 4 段叙事弧 + 情绪曲线）、ProjectSettings、四个 capability 状态、反馈来源、工具/container 版本、资产/BGM/字体摘要到 `render_input`；worker 不读取当前状态、不重新调用 AI/分析工具。
- [ ] retry 在项目锁内逐字节复制源 revision 的 `render_input.json` 到新 revision，记录相同输入摘要后入队；当前草稿、设置和素材元数据不参与重试。
- [ ] 服务启动时按状态表恢复：`queued` 保持、`running` 回到 `queued`、`cancelling` 变 `cancelled`、终态不变。
- [ ] 提交前按 `输入规范化文件总大小 × 2 + 预估未压缩帧缓存上限 2 GB + 目标输出 500 MB` 计算所需空间；`max(该估值, 5 GB)` 加配置磁盘保留量后仍不足则返回 `INSUFFICIENT_STORAGE`。
- [ ] 实现提交、状态、取消和重试 API；保证同项目最多一个活动任务。
- [ ] 运行 `npm run test:backend -- --run backend/tests/jobs/test_queue.ts backend/tests/api/test_renders.ts`，预期全通过。
- [ ] 提交：`feat: add recoverable render queue`。

### Task 11：实现视频播放、下载与 final 标记

**文件：** 创建 `backend/src/services/media.ts`、`backend/src/api/media.ts`；测试 `backend/tests/api/test_media.ts`。**v0.1 不再做分享功能；删除所有 share 相关代码、端点、manifest 字段与 UI。**

- [ ] 测试 Range、下载、final 标记/撤销、revision lineage 列表；只有 completed 可 final。
- [ ] 视频端点只由项目/revision 元数据解析路径（通过 `StorageBackend` 走 `local://` 或 `oss://`），不接收客户端路径；Range 头按规范处理。
- [ ] `final` 标记同项目仅一个；设置新 final 时旧 final 自动撤销但文件保留；撤销 final 不删除 revision。
- [ ] lineage 接口按 `parent_revision_id` 链返回根到叶的有序 revisions；删除分支时禁止删除有 child 的中间 revision。
- [ ] **删除任何 `share` / `share_token` / `share_digest` 字段、端点、组件、tests。** 监控 `git grep -nE "share|分享"` 命中 0（v0.1 范围内）。
- [ ] 运行 `npm run test:backend -- --run backend/tests/api/test_media.ts`，预期全通过。
- [ ] 提交：`feat: add video playback, download and finalize`。

### Task 12：实现前端项目骨架与 React + Ant Design 集成

**文件：** 创建 `frontend/src/app/router.tsx`、`frontend/src/app/theme.ts`、`frontend/src/app/AppShell.tsx`、`frontend/src/api/client.ts`、`frontend/src/api/types.ts`、各 feature 目录骨架；测试 `frontend/tests/`。

- [ ] 验证 `frontend/vitest.config.ts` 集成 `jsdom` + `@testing-library/jest-dom`；测试渲染组件、路由跳转、HTTP error 状态。
- [ ] `ConfigProvider` 配置中文 locale；`App` 包裹全局 `message / notification / modal`；`Layout + Menu + Header` 提供"项目"/"系统设置"导航，主入口只有已登录管理员可见。
- [ ] 实现 `api/client.ts`：使用 TanStack Query 封装的 fetch；统一 `error.code`/`message` 解析；401/403 跳转到 Basic Auth 提示页；公开异常列表不泄露 Key/AccessKey。
- [ ] 在 React Query hooks 中**屏蔽** `api_key / access_key_secret / sts_token` 字段（即使后端误返回也不写入本地 state）。
- [ ] `AppShell` 始终显示 FFmpeg 状态、字体发现状态、六个 BGM 分类曲目数、四个 capability 的 provider/model 脱敏快照与 `enabled_by_default`；不显示任何 Key。
- [ ] 路由表覆盖 `/`、`/projects/:id`、`/projects/:id/revisions/:rid`、`/settings`；**无 `/share/*` 路由**。
- [ ] 运行 `npm run test:frontend -- --run frontend/tests/app`，预期全通过。
- [ ] 提交：`feat: scaffold frontend with ant design and react router`。

### Task 13：实现项目列表页与项目编辑页（步骤条含 CreativePlanStep）

**文件：** 创建 `frontend/src/features/projects/*`、`frontend/src/features/editor/{AssetStep,StyleSourceStep,StyleProfileEditor,CreativePlanStep,AISettingsStep,StoryboardEditor}.tsx`；测试 `frontend/tests/features/editor/*`。

- [ ] 项目列表 Test：新建表单、删除项目（带确认）、封面 / 标题 / 状态展示；上限 9 卡/页。
- [ ] 项目编辑步骤条：**素材 → 风格来源 → 风格画像 → 创意方案（planner）→ AI 设置 → 生成 → 预览反馈 → 最终交付**。
- [ ] `AssetStep`：图片拖拽（`Upload.Dragger`）、缩略图、排序（`Button` ↑↓）、删除、逐图 caption；纯文字/纯图片/图文三种状态；空时给出明确引导。
- [ ] `StyleSourceStep`：`Radio.Group` 三选一；preset 走 `Select`，custom 走 `Input.TextArea`，reference 走 `Input + Checkbox`；模式切换清除旧画像并标记 `stale`。
- [ ] `StyleProfileEditor`：使用 `Descriptions / Tag / ColorPicker / Slider` 展示主题、节奏、运动、字幕、BGM、置信度；必须 confirm 才能进入下一步。
- [ ] **`CreativePlanStep`（新增）**：
  - 步骤 1：`POST /creative-plan:generate` → 展示 `creative_intent` + 4 段叙事弧 + mood + 情绪曲线图 + BGM 理由 + 2–3 个方向卡（`Card` + `Radio.Group`）。
  - 步骤 2：用户选 1 方向 → `POST /creative-plan:select-direction` → 展开完整 `scene_plan`（表格 + 镜头缩略图 + bookend 高亮）+ 自动选择理由 + 编辑入口。
  - 步骤 3：`POST /creative-plan:confirm` → 提交 input digest；状态 `confirmed`；输入变化自动 `stale` 并阻止渲染。
- [ ] `AISettingsStep`：四个独立 `Switch` + 旁边显示 provider/model 脱敏快照与未配置/降级状态；未配置链接 `/settings`；`Text` 解释 MiniMax 是辅助功能。
- [ ] `StoryboardEditor`：表格编辑每镜；动效和转场用 `Select`（白名单）；字幕按 §9C.3 校验字数；与 `EditorSpecValidator` 后端一致。
- [ ] E2E 跑通"新建项目 → 纯文字生成 → 文案编辑 → 预设升学宴 → planner 生成 → 选方向 → 确认 → 生成 MP4 → 预览"。
- [ ] 提交：`feat: build project pages with creative plan editor`。

### Task 14：实现预览反馈页与系统设置页（能力绑定 + 存储）

**文件：** 创建 `frontend/src/features/feedback/*`、`frontend/src/features/settings/*`、`frontend/src/features/capabilities/*`；测试 `frontend/tests/features/feedback/*`、`frontend/tests/features/settings/*`。

- [ ] 预览反馈页：9:16 播放器（`<video>` + Range）；当前 revision 与兄弟 revision 缩略图；下载按钮；"设为满意版本"在 completed 状态才可点。
- [ ] 反馈面板同时提供 `Input.TextArea` + 结构化控件（更快/更慢、字幕、色调、BGM 情绪/音量、特效强度、转场、镜头顺序/时长）；按钮固定文案"提交修改计划"。
- [ ] **`FeedbackParserPreview`（新增）**：当 `feedback_parser` 启用时显示解析后白名单操作列表与每个 `confidence`；不接受的文本自动进入 `unsupported_text` 提示；用户必须明确接受才能应用。
- [ ] 系统设置页：
  - `AI 供应商`：`Table` + `Drawer/Form` 管理 profile（供应商名、协议 `minimax | openai_compatible`、HTTPS Base URL、API Key `Input.Password`、模型数组、启用）；Key 字段不预填；保存后只返回掩码。
  - **`CapabilityBindingsTable`（新增）**：四行 capability（`planner / prompt_optimizer / caption_effects / feedback_parser`），分别选择 provider/model/场景说明；每行展示 `enabled_by_default`；切换 binding 走二次确认。
  - **`StorageSettingsForm`（新增）**：`Radio.Group` 选择 `local`（默认） / `aliyun_oss`；OSS 字段 `endpoint / bucket / region / prefix / credentials_mode (static_access_key | sts_role)`；`sts_role` 时显示 `sts_role_arn / sts_session_duration_seconds`；`static_access_key` 时输入 AccessKey/Secret 走 `Input.Password` + 二次确认 + warning；`signed_url_ttl_seconds` 默认 3600（范围 60–86400）；测试连接按钮。
- [ ] 移动端提供简化版"上传/查看"，复杂排版在桌面端。
- [ ] **不渲染任何分享相关组件或按钮**。
- [ ] 提交：`feat: build feedback and settings pages with capability bindings`。

### Task 15：E2E 测试、可视化验收与监控指标

**文件：** 创建 `frontend/tests/e2e/` 下的 Playwright 测试；`scripts/smoke-test.ts`；`docs/visual-acceptance.md`；CI workflow `.github/workflows/ci.yml`。

- [ ] Playwright 跑通：
  1. 预设升学宴 + 纯文字 → CreativePlan（planner OFF）→ 渲染 → 反馈两次 → final → 下载。
  2. 预设生日宴 + 5 张图片 → planner ON（mock MiniMax-M3）→ direction 选择 → 字幕/特效 H3 调用 → 渲染 → feedback_parser ON → final。
  3. 自定义风格 + 4 张图片 → planner ON → 渲染 → 反馈（自由文本触发 `unsupported_text`）→ 强制结构化澄清 → 渲染 → final。
  4. 参考链接（mock yt-dlp）→ 画像确认 → 渲染 → 修改字幕 → 渲染。
  5. 全部 capability OFF → 完全规则生成 → 渲染 → 反馈（有限意图词典）→ final。
  6. **存储后端切换（local → mock OSS）→ `storage-migrate export/import/verify` round-trip → 渲染成功。**
- [ ] `scripts/smoke-test.ts` 跑通后端冒烟：启动后 `GET /health` → `GET /api/v1/capabilities` → 创建项目 → 上传图片 → 选择 preset → planner OFF → 渲染 → 标记 final → 下载。
- [ ] 记录 5 段人工视觉验收：升学宴、生日宴、自定义、参考画像、纯文字各 1 条，写入 `docs/visual-acceptance.md`，覆盖 `creative_intent / 4 段叙事弧 / bookend / 字幕安全区 / 节奏 / BGM / 特效 / 移动端`。
- [ ] CI workflow 顺序：`npm ci` → `npm run typecheck` → `npm run lint` → `npm run test:backend -- --run` → `npm run test:frontend -- --run` → `npm exec -- tsx scripts/audit-oss.ts` → `npm exec -- tsx scripts/check-tools.ts` → `npm exec -- tsx scripts/check-assets.ts` → `npm exec -- tsx scripts/check-ffmpeg.ts` → Playwright E2E。
- [ ] 提交：`feat: add e2e tests and visual acceptance docs`。

### Task 16：文档与第三方通知

**文件：** 修改 `README.md`、`docs/oss-adoption.md`、`docs/capability-scenarios.md`、`THIRD_PARTY_NOTICES.md`；新增 `docs/visual-acceptance.md`、`docs/editor-spec.md`、`docs/creative-plan.md`。

- [ ] README 覆盖：架构图、目录、启动命令、AI 关闭默认流程、Basic Auth、磁盘与备份、限制与首版不做。
- [ ] `docs/oss-adoption.md`：每条工具记录 Star 快照日期、精确版本/commit、许可证、复用模块、Linux 部署方式、资源需求、风险、替代方案。低 Star 例外单独段落。
- [ ] `docs/capability-scenarios.md`：四个 capability 的用户场景、输入输出 schema、Gate、fallback、默认绑定、隐私与降级。
- [ ] `docs/editor-spec.md`：照抄 §9C 数字，便于非工程读者查阅。
- [ ] `docs/creative-plan.md`：照抄 §9B 字段说明 + 4 段叙事弧示例 + bookend/情绪曲线示例。
- [ ] `THIRD_PARTY_NOTICES.md`：列出所有依赖（含 OSS SDK、Ant Design、Noto Sans SC、曲库）的许可证页与许可摘要。
- [ ] 提交：`docs: add capability, editor spec and creative plan docs`。

### Task 17：部署与运维

**文件：** 创建 `docker-compose.yml`、`Dockerfile`（前端 + 后端 + 分析工具）、`systemd` 单元示例（可选）、`DEPLOYMENT.md`、`docs/operational-metrics.md`。

- [ ] `docker-compose.yml`：web 节点、分析工具节点；卷挂载 `data/`、`assets/`；web 暴露 8080；分析节点只接受 web 节点发起的本地任务。
- [ ] `Dockerfile`（后端）：非 root 用户、`HEALTHCHECK` 指向 `/health`、最小镜像层；前端：构建静态资源。
- [ ] `DEPLOYMENT.md`：写入反向代理 + Basic Auth + HTTPS / 0600 权限 / `data/system/settings.json` 备份策略 / 备份默认排除 settings 文件 / 显式包含时的 0600 警告。
- [ ] `docs/operational-metrics.md`：列出日志字段、磁盘监控、capability 状态、storage backend 状态、任务队列长度。
- [ ] 提交：`feat: add docker compose and deployment docs`。

### Task 18：能力注册表契约锁定（v0.1 不增 capability）

**文件：** 测试 `backend/tests/integrations/capabilities/registry-lockdown.test.ts`；文档 `docs/capability-scenarios.md`。

- [ ] 注册表启动时实例化 4 个描述符：`planner / prompt_optimizer / caption_effects / feedback_parser`。
- [ ] 调用 `CapabilityRegistry.resolve("planner_typo")` 抛 `CAPABILITY_NOT_REGISTERED`；`list()` 只能返回 4 个 ID。
- [ ] `settings.json` 中 `capability_bindings` 字段只能出现这 4 个 key；其他 key 启动报错。
- [ ] admin API 不提供"新增 capability"端点；不允许通过 UI 编辑注册表。
- [ ] 提交：`feat: lock capability registry to four ids`。

### Task 19：编辑器规范契约测试（§9C 数字锁进 fixture）

**文件：** `backend/tests/render/editor-spec.contract.test.ts`；`backend/tests/render/editor-spec.fixture.ts`；`backend/src/render/editor-spec.ts`。

- [ ] 为 §9C 七个维度各准备一组 fixture：合法/非法两种。
- [ ] 时间线：合法 30s 8 镜；非法 70s、5s、单镜 9s、首镜 0.5s、末镜 fade、尾→头错位 — 全部 `EDITOR_SPEC_VIOLATION` 并指出维度。
- [ ] 字幕：合法中文 18 字 / 英文 36 字 / 2 行；非法 20 字 / 38 字 / 3 行 / 字号 7% — 全部拦截。
- [ ] BGM：合法 1.0s 淡入淡出 + -16 LUFS；非法 0.5s 淡入 / 0s 淡出 / 噪声峰值 — 全部拦截。
- [ ] 动效：合法 `kenburns_in`；非法 `blur_zoom`。
- [ ] 转场：合法 `fade` 0.6s < 6.0s 镜头；非法 `fade` 1.5s > 25% / `spin`。
- [ ] bookend：合法 `opening_scene_id == closing_scene_id`；非法不同。
- [ ] 色彩：合法 preset LUT + 亮度 ±10%；非法 LUT 偏移 ±25%。
- [ ] AI 输出必须经过 `EditorSpecValidator` 校验；任一不通过 → `RulePlanner` 实施并记录 warning。
- [ ] 提交：`test: lock editor spec contract tests`。

### Task 20：存储后端迁移工具

**文件：** `scripts/storage-migrate.ts`、`backend/tests/scripts/storage-migrate.test.ts`。

- [ ] `export --backend <local|aliyun_oss> --output <tar-or-oss-prefix>`：列举所有项目，逐文件读出 → 写目标位置。
- [ ] `import --source <tar-or-oss-prefix> --backend <local|aliyun_oss>`：逐文件读出 → 写入新位置，并记录 SHA-256。
- [ ] `verify --left <a> --right <b>`：两边逐文件 SHA-256 比较，输出差异 CSV。
- [ ] `round-trip` 测试：本地 → 导出 → 导入到 MemoryStorage → verify → 0 差异。
- [ ] 切换 backend 前必须先 export，新 backend import + verify 通过后才能改 `settings.json.storage.backend` 并重启。
- [ ] 提交：`feat: add storage backend migrate tool`。

### Task 21：RulePlanner 与 PlannerService 的 schema parity

**文件：** `backend/src/services/rule-planner.ts`、`backend/tests/services/planner-parity.test.ts`。

- [ ] RulePlanner 输出与 `PlannerService`（AI）输出共用同一 `CreativePlan` Zod schema。
- [ ] 测试用固定 fixture（有序素材、文案、StyleProfile、duration_hint）与确定性 seed 跑两遍：AI 路径 mock、RulePlanner 路径实跑。
- [ ] 关键字段必须一致：`creative_intent.theme` 与四段叙事弧 beat 顺序、`directions[].title` 前缀可解释为 "rule:" / "ai:" 或语义配对、`bgm_rationale.selected_track_id` 在风格预设下完全一致。
- [ ] 任何 AI 字段超出 RulePlanner schema → Zod 校验失败 → 降级；绝不静默忽略。
- [ ] 提交：`test: planner parity ai vs rule`。

## 13. 里程碑

- **M0（Task 1-2）基线就绪：** workspace、Ant Design 基线、Zod 设计与 OSS 审计全部通过测试。
- **M0.5（Task 3a/3b/3c）核心基座：** CapabilityRegistry 四 ID 锁定、StorageBackend 三实现、ProjectRepository 接口 + Local/占位 DB、SystemSettings v3 全部通过；admin Web 可配置能力绑定 + 存储后端。
- **M1（Task 4-6）素材 + 风格：** 项目 CRUD + 输入 + 风格来源三选一 + 异步分析 + 画像确认。
- **M1.5（Task 8a Part 1）Storyboard + Editor Spec：** StoryboardService 接受 CreativePlan 默认 RulePlanner 路径。
- **M2（Task 7a/7b/7c/7d/7e）AI 能力注册表 + 四个 capability 闭环：** planner/prompt_optimizer/caption_effects/feedback_parser 端到端可用、降级正确、门禁生效。
- **M2.5（Task 14 Part 1）AI Web 设置：** CapabilityBindingsTable + StorageSettingsForm；管理员可在网页上完成 M0.5 配置。
- **M3（Task 9a/9b/10）渲染管线：** FFmpeg 集成 + EditorSpecValidator + 任务队列 + 恢复/取消/重试。
- **M3.5（Task 11）播放下载 final：** 视频 API、Range、final 标记、**无分享**。
- **M4（Task 12-14）前端完整闭环：** 项目编辑器步骤条（含 CreativePlanStep）、反馈面板（含 FeedbackParserPreview）、能力绑定、存储设置。
- **M4.5（Task 18-21）契约锁定：** 能力注册表锁定、编辑器规范契约、存储迁移工具、planner parity 测试全部通过。
- **M5（Task 15-17）质量 & 部署：** E2E + 视觉验收 + 文档 + Docker Compose + 部署文档。

## 14. 后续演进（v0.2+）

- **v0.2.x**：通过新增 `backend/src/integrations/capabilities/<id>.ts` 扩展能力注册表（不在 v0.1 范围内）。可能的扩展：转场风格、背景音乐推荐、字幕多语言、镜头节奏 debug 视图。
- **v0.3**：
  - DB 接入：`DbProjectRepository` 实现 v0.1 接口契约；扩展 `ProjectSearchQueries`（按标题/状态/时间范围查询、统计存储用量、孤儿素材）。
  - **OSS 升级为默认之一**：当 `storage-migrate` 工具稳定后，引入 OSS 默认 profile 与命名规范；保留 local 作为单机回退。
- **v0.4**：多用户、账号体系、团队协作、版本分支对比。
- **v0.5+**：AI 作曲 / 数字人 / 文生视频（仅在许可证、服务条款与生成质量达标时引入；当前不在 v0.1 范围）。
- **后续复用关注点：** 当 v0.2 引入"分享"功能时，必须重新设计 token 与速率限制策略；不复用现有 OSS / capability token；HTML 端点在新功能完成 Quality/Security 审计后再设计。

## 15. 参考视频与创意质量校准

- 参考模式仅用于抽象风格（主题、色调、节奏、字幕布局、BGM 情绪、动效强度），不复制原视频画面、音乐、台词、人物与受保护模板。授权参考仅临时下载低清分析副本，提取后按 §7.4 删除。
- 系统对每条视频必须主动生成结构化 `CreativePlan`：主题 + 4 段叙事弧 + bookend + 情绪曲线 + 2–3 方向 + BGM 理由 + 完整 `scene_plan`。
- 用户确认 `CreativePlan` 后才能渲染；任何输入变化（素材/文字/风格/AI 开关/feedback 应用）立刻标记 `stale`，强制重新生成。
- 视觉验收必须包含：4 段叙事完整、bookend 收尾呼应、字幕安全区合规、节奏合理、BGM 与情绪匹配、特效与场景匹配、移动端可播。
- 编辑器规范（§9C）数字全部通过 `EditorSpecValidator` 与契约测试；AI 输出自动校验一致后才可直接用于渲染，否则降级。