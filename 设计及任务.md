# 视频生成网站设计及实施任务

> **文档状态：** 可执行基线（2026-08-03）  
> **目标读者：** 产品、设计、前后端、测试与部署人员  
> **实施约定：** 执行任务时使用任务中的复选框跟踪；按任务顺序实施和验收，不并行修改同一模块。

## 1. 结论摘要

首版建设一个**单机部署、无数据库、面向竖屏图文短视频**的网站。用户上传图片并填写文字，选择“轻松幽默、浪漫温馨、科技感”等风格后，系统自动生成分镜、字幕、转场/画面动效并匹配合规背景音乐，异步渲染为可预览、修改、下载和链接分享的 MP4。

推荐方案：

- 前端：React + TypeScript + Vite。
- 后端：FastAPI + Python；Python 更适合编排 FFmpeg、图片预处理和后续 AI 能力。
- 渲染：FFmpeg 为唯一必需渲染引擎，Pillow 做图片预处理，ASS 做字幕排版。
- 存储：应用服务器文件系统；每个项目使用 `manifest.json` 保存状态和版本，不引入数据库。
- 任务：进程内单消费者队列；同一时间默认只渲染 1 个任务，避免 FFmpeg 耗尽服务器资源。
- AI：MiniMax 只提供两个**独立、默认关闭**的辅助能力，共用一个 `MINIMAX_API_KEY`：默认 `MiniMax-M3` 生成提示词优化提案，必须经用户预览并显式接受后才可使用；默认 `MiniMax-H3` 生成字幕和特效内容，并进入分镜预览确认。两个默认模型都可由部署者替换。常规用户完全不启用 MiniMax 也能使用全部核心视频流程。
- BGM：首版根据风格**自动选择并裁剪本地已授权曲库**。语言模型不能直接生成音频，不把“选曲”误称为“AI 作曲”；真正的音乐生成 API 放到后续版本。

## 2. 已知条件、假设与范围边界

### 2.1 已知条件

1. 当前目录只有需求文档与候选项目清单，尚无代码，也不是 Git 仓库。
2. 首版明确不使用数据库，上传数据保存在网站所在应用服务器。
3. MiniMax 配置必须简单：单租户部署管理员只配置一个服务端环境变量 `MINIMAX_API_KEY`；提示词优化模型默认 `MiniMax-M3`，字幕/特效模型默认 `MiniMax-H3`，均可由高级环境变量替换。两个项目级功能开关默认关闭，普通使用者不在网页填写或查看 Key。
4. 用户给出的抖音链接无法通过当前抓取方式读取具体视频内容，因此不能把其镜头、字体、音乐或转场细节当作已确认需求。

### 2.2 首版默认产品参数

以下参数作为可直接开发的默认决策；拿到参考视频样片后只调整模板参数，不改整体架构：

| 项目 | 首版默认值 |
|---|---|
| 输出 | MP4，H.264 + AAC，1080×1920，30 fps，`yuv420p` |
| 时长 | 15–60 秒；按文字量和图片数自动计算 |
| 图片 | 1–20 张；JPG、PNG、WebP；单张不超过 15 MB |
| 文字 | 总计不超过 5,000 个 Unicode 字符；每张图可有独立文案 |
| 风格 | `humorous`、`romantic`、`tech` 三种内置模板 |
| 动效 | 缓慢推近、平移、轻微缩放；按模板组合，不使用高风险闪烁效果 |
| 转场 | 淡入淡出、滑动、缩放切换；默认 0.4–0.8 秒 |
| 字幕 | 底部安全区内，白字描边；按分镜时间轴生成 ASS 并烧录 |
| 音频 | 自动匹配本地已授权 BGM，循环/裁剪至视频长度，淡入淡出 |
| MiniMax 提示词优化 | 默认关闭；启用后由 `MiniMax-M3`（可替换）生成提案，预览并显式接受前不得用于后续生成 |
| MiniMax 字幕/特效 | 默认关闭；启用后由 `MiniMax-H3`（可替换）生成，结果必须进入分镜预览并由用户确认后渲染 |
| 预览 | 提示词提案先做原文/优化文对比；完成渲染后网页播放器预览；首版不做浏览器内实时逐帧预渲染 |
| 修改 | 修改文字、顺序、风格或字幕后创建新修订并重新渲染 |
| 分享 | 生成不可预测的只读链接，支持复制链接和 Web Share API |
| 用户体系 | 首版无注册、账号和团队协作；编辑后台仅供单运营者/受信任内网使用 |
| 访问边界 | 生产反向代理必须用 Basic Auth 保护 SPA 与项目 API；仅 `/share/*`、`/api/v1/share/*` 及最小化的 `/health` 匿名，health 只返回 `{"status":"ok"}` |
| 部署 | 单实例、单机磁盘；不承诺多实例共享状态和水平扩容 |

### 2.3 首版不做

- 文生视频、数字人、口型同步、声音克隆、AI 图片生成。
- 自动发布到抖音/小红书等平台；“分享”仅指系统内公开链接和浏览器系统分享。
- 浏览器时间线剪辑器、逐帧特效编辑、多人协作。
- AI 原创作曲；只使用来源和商用授权明确的本地曲库。
- 数据库、对象存储、Redis、分布式队列、GPU 集群。
- 对参考抖音作品做下载、去水印或复刻其受版权保护的音乐/素材。

## 3. 用户与核心流程

### 3.1 目标用户

希望快速把图文素材制作成竖屏短视频、但不熟悉专业剪辑软件的内容创作者或运营人员。

### 3.2 主流程

1. **创建项目：** 输入项目标题。
2. **收集素材：** 上传图片；拖拽排序；为每张图填写文案，也可填写一段总文案。
3. **选择风格：** 选择三种模板之一，可开关字幕、BGM 和动效。
4. **生成方案：** 系统基于素材生成可编辑的分镜方案，包括每镜时长、字幕、动效、转场和 BGM 分类。
5. **确认并渲染：** 用户确认后提交；页面展示排队、处理、成功或失败状态。
6. **审核与修改：** 播放生成结果；修改文案、图片顺序或风格后再次渲染，旧修订仍保留。
7. **最终交付：** 下载 MP4；按需开启只读分享链接并复制或调用系统分享。

### 3.3 异常流程

- 不合法图片：上传阶段立即拒绝并说明格式、尺寸或容量问题。
- MiniMax 未配置/超时/返回格式错误：记录警告，自动采用规则方案，不阻断渲染。
- FFmpeg 失败：任务标记为 `failed`，保存脱敏后的错误摘要；用户可原参数重试。
- 服务重启：按 7.3 状态表恢复；`queued` 保持、`running` 重新排队、`cancelling` 结束为 `cancelled`。
- 磁盘空间不足：提交渲染前拒绝新任务并提示清理项目；不在渲染中途静默删除数据。

## 4. 方案比较与选型

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| React + FastAPI + FFmpeg 自建管线 | 边界清晰、可控、无外部视频 API 成本、与 Python 媒体生态契合 | 需自行实现模板和任务状态 | **首版采用** |
| React + Node.js + Remotion/HTML 渲染 | 前后端统一 TypeScript，组件化画面方便 | Chromium 渲染资源占用高；音视频细节仍依赖 FFmpeg | 可作为后续模板引擎实验 |
| 集成 MoneyPrinterTurbo/Pixelle/OpenMontage 等整套系统 | 功能丰富、起步看似快 | 依赖复杂、范围远超图文视频、难以保持单 Key 和无数据库 | 仅参考流程，不直接作为核心依赖 |

### 4.1 `GitHubStar.md` 中项目的使用策略

- **FFmpeg：** 核心生产依赖，负责视频编码、缩放裁剪、转场、字幕和音频混合。
- **MoneyPrinterTurbo / Pixelle-Video：** 参考任务编排、素材管线和失败恢复思路，不直接复制完整架构。
- **hyperframes：** 参考 HTML/声明式场景表达；首版不引入第二渲染引擎。
- **videoflow：** 参考轻量“图片 + 文案 → 视频”交互流程；使用前须重新核验活跃度、许可证和代码质量。
- **flycut-caption：** 后续需要可视化字幕编辑器时评估；首版使用普通表单编辑字幕。
- **VibeVoice/VoxCPM：** 后续语音旁白阶段评估，不进入首版依赖。
- 任何开源代码引入前必须核验许可证、维护状态、安全问题和与当前版本的兼容性；Star 数不作为采用依据。

## 5. 产品界面设计

### 5.1 页面

1. **项目列表 `/`**
   - 创建项目入口。
   - 项目卡片展示封面、标题、更新时间、最新渲染状态。
   - 支持打开和删除；删除需二次确认。
2. **项目编辑 `/projects/:projectId`**
   - 步骤条：素材 → 风格与方案 → 生成 → 审核交付。
   - 素材区：拖拽上传、缩略图、排序、删除、逐图文案。
   - 设置区：风格、字幕、BGM、动效开关。
   - 方案区：按镜头编辑字幕和时长，展示转场/动效标签。
   - 生成区：任务进度、当前阶段、取消按钮。
3. **结果页 `/projects/:projectId/revisions/:revisionId`**
   - 9:16 播放器、错误/警告、下载、再次修改、开启/关闭分享。
4. **分享页 `/share/:token`**
   - 只读播放器和下载按钮，不暴露服务器文件路径及编辑接口。
5. **能力提示区**
   - 只显示“MiniMax 可用/未配置/最近一次请求已降级”，数据来自 capabilities 和分镜响应 warning。
   - API Key 只由单租户部署管理员通过服务端环境变量提供，网页不提供 Key 输入、连接测试、读取或回显能力。

### 5.2 交互原则

- 首屏直接说明三步：“上传图文 → 选择风格 → 生成视频”。
- 用户不能在没有图片时提交方案或渲染。
- 上传、生成方案、渲染是三个独立动作，避免一次失败后全部重来。
- 状态不只用颜色表达，始终配文字和图标。
- 移动端可完成上传和查看；复杂排序在桌面端体验优先。

## 6. 系统架构

```text
Browser
  │ HTTP/JSON + multipart + video range requests
  ▼
React SPA ───────────────► FastAPI
                              ├─ ProjectService
                              ├─ UploadService
                              ├─ StoryboardService ─► MiniMaxAdapter（可选）
                              │                     └► RulePlanner（兜底）
                              ├─ RenderQueue（单消费者）
                              │     └─ RenderPipeline ─► Pillow + FFmpeg
                              ├─ ShareService
                              └─ FileProjectRepository
                                      └─ ./data/projects/<project_id>/
```

### 6.1 组件职责

| 组件 | 单一职责 | 主要依赖 |
|---|---|---|
| `FileProjectRepository` | 原子读写项目 manifest，列出和删除项目 | Python 文件系统 |
| `UploadService` | 流式接收、校验、规范化和保存图片 | Pillow |
| `RulePlanner` | 不依赖 AI 生成确定性分镜 | 风格模板 |
| `MiniMaxAdapter` | 用一个 API Key 请求创意文案，并验证结构化响应 | HTTP 客户端 |
| `StoryboardService` | 选择 AI 或规则方案并统一输出 | Adapter、Planner |
| `RenderPipeline` | 将已确认方案渲染为 MP4 | FFmpeg、Pillow、ASS |
| `RenderQueue` | 排队、取消、恢复和串行执行任务 | asyncio、Repository |
| `ShareService` | 生成/撤销分享令牌并解析只读资源 | secrets、Repository |
| React API client | 封装 HTTP、错误和轮询 | fetch |
| Editor state | 管理页面草稿，不复制后端持久化职责 | React Query/本地状态 |

### 6.2 目录结构

```text
video-developer/
├── README.md
├── .gitignore
├── .env.example
├── docker-compose.yml
├── data/.gitkeep
├── assets/
│   ├── fonts/NotoSansSC-Regular.otf
│   └── music/
│       ├── LICENSES.md
│       ├── humorous/
│       ├── romantic/
│       └── tech/
├── frontend/
│   ├── package.json
│   ├── vite.config.ts
│   ├── src/
│   │   ├── app/router.tsx
│   │   ├── api/client.ts
│   │   ├── api/types.ts
│   │   ├── components/
│   │   ├── features/projects/
│   │   ├── features/editor/
│   │   ├── features/render/
│   │   ├── features/share/
│   │   └── styles/
│   └── tests/
├── backend/
│   ├── requirements.txt
│   ├── app/
│   │   ├── main.py
│   │   ├── config.py
│   │   ├── api/
│   │   ├── domain/models.py
│   │   ├── repositories/file_projects.py
│   │   ├── services/uploads.py
│   │   ├── services/storyboards.py
│   │   ├── services/shares.py
│   │   ├── integrations/minimax.py
│   │   ├── render/plan.py
│   │   ├── render/subtitles.py
│   │   ├── render/pipeline.py
│   │   └── jobs/queue.py
│   └── tests/
└── scripts/
    ├── check_ffmpeg.py
    └── smoke_test.py
```

## 7. 数据设计（文件系统）

### 7.1 磁盘布局

```text
data/projects/<project_id>/
├── manifest.json
├── uploads/<asset_id>/original.<ext>
├── uploads/<asset_id>/normalized.jpg
├── storyboard/draft.json             # 当前可编辑草稿，含 version/updated_at
├── revisions/<revision_id>/storyboard.json  # 供审核展示的分镜副本
├── revisions/<revision_id>/render_input.json # 渲染唯一权威输入：分镜+设置+不可变素材摘要
├── revisions/<revision_id>/subtitles.ass
├── revisions/<revision_id>/render.log
├── revisions/<revision_id>/output.mp4
└── tmp/                         # 成功或失败后清理
```

- `project_id`、`asset_id`、`revision_id` 使用 UUIDv4，由服务端生成，不接受客户端路径。
- `storyboard/draft.json` 是唯一当前草稿；`storyboard:generate` 与 `PUT /storyboard` 都必须提交 `expected_version`（项目尚无草稿时为 `null`），在同一项目锁内做乐观并发检查，成功后版本加 1。版本不匹配返回 `409 STORYBOARD_VERSION_CONFLICT`。
- `POST /renders` 在同一项目锁内读取并校验草稿，创建 revision，并原子写入 `render_input.json`：内嵌 storyboard、完整 `ProjectSettings`、每个被引用图片的 `asset_id`/固定 `normalized_relpath`/SHA-256/宽高/展示文案，以及冻结的 `font` 和 `music`。`font` 包含相对路径、SHA-256 和许可证条目摘要；启用 BGM 时先按 9.2 规则选定唯一曲目，`music` 包含 category、相对路径、SHA-256 和许可证条目摘要，禁用时为 `null`。所有字段均计入 `render_input_digest`；同时写 `storyboard.json` 供审核。`render_input.json` 是 worker 和 retry 的唯一权威输入，后续编辑草稿、设置、素材元数据、字体或曲库目录不改变选择。
- normalized 素材文件创建后不可原地覆盖；从项目草稿删除素材时，若任一 revision 的 `render_input.json` 仍引用它则保留物理文件，只从当前 assets 列表移除，项目整体删除时才回收。worker 读取前验证 SHA-256，不一致则以 `ASSET_INTEGRITY_ERROR` 失败。
- JSON 写入采用“同目录临时文件 + `fsync` + 原子替换”，避免进程中断产生半个文件。
- 每个项目使用进程内异步锁串行化 manifest、草稿、进度、取消和分享更新；manifest 的 `version` 作为 CAS 版本，防止并发更新丢失。
- `manifest.json` 是项目元数据和 revision 状态的唯一事实来源；派生文件可重新生成。
- 服务端启动时扫描 manifests 恢复索引，不维护额外全局数据库文件。

### 7.2 核心模型

```json
{
  "schema_version": 1,
  "version": 3,
  "id": "uuid",
  "title": "旅行回忆",
  "created_at": "ISO-8601 UTC",
  "updated_at": "ISO-8601 UTC",
  "assets": [
    {"id": "uuid", "filename": "a.jpg", "mime_type": "image/jpeg", "width": 1440, "height": 1920, "caption": "第一天到达海边", "order": 0}
  ],
  "settings": {"style": "romantic", "subtitles_enabled": true, "music_enabled": true, "effects_enabled": true},
  "latest_revision_id": "uuid",
  "draft_storyboard": {"version": 2, "updated_at": "ISO-8601 UTC", "path": "storyboard/draft.json"},
  "share": {"enabled": true, "revision_id": "uuid", "token_digest": "sha256:<hex>", "created_at": "ISO-8601 UTC"},
  "revisions": [
    {"id": "uuid", "status": "completed", "progress": 100, "stage": "completed", "render_input_digest": "sha256:<hex>", "warning_codes": [], "error_code": null, "created_at": "ISO-8601 UTC"}
  ]
}
```

`storyboard.json`：

```json
{
  "version": 1,
  "duration_seconds": 18.0,
  "music_category": "acoustic/warm",
  "scenes": [
    {"asset_id": "uuid", "start": 0.0, "duration": 4.5, "subtitle": "第一天，我们抵达海边", "motion": "zoom_in", "transition": "fade"}
  ]
}
```

### 7.3 状态机

`Revision.status` 只取终态/调度态，`Revision.stage` 只表示运行中的进度阶段：

| 当前 status | 允许操作/事件 | 下一 status | stage 规则 |
|---|---|---|---|
| `queued` | worker 领取 | `running` | `planning` |
| `queued` | 用户取消 | `cancelled` | `cancelled` |
| `running` | 阶段推进 | `running` | `planning → preparing → rendering → finalizing` |
| `running` | 用户取消 | `cancelling` | 保持当前阶段 |
| `cancelling` | 子进程退出/重启恢复 | `cancelled` | `cancelled` |
| `running` | probe 成功 | `completed` | `completed` |
| `running` | 任一阶段异常 | `failed` | 失败时所在阶段 |

- `draft` 是项目编辑状态，不是 revision status；revision 创建后初始即为 `queued`。
- `completed`、`failed`、`cancelled` 为不可逆终态。重试永远创建新 revision，旧 revision 不改回 `queued`。
- 服务重启时：遗留 `queued` 保持不变；`running` 回到 `queued` 并清零瞬时进度；`cancelling` 变为 `cancelled`；终态不变。
- 状态只能由后端按表迁移。每次迁移都在项目锁内原子更新 manifest；前端不得直接提交 `status`、`stage`、输出路径或分享令牌。

### 7.4 数据保留

- 首版默认项目永久保留，直到用户主动删除。
- 删除项目时先将目录原子移动到 `data/trash/`，再异步删除，以减少半删除状态。
- 提供磁盘可用空间指标和管理员清理命令；自动过期策略待引入用户体系后再决定。

## 8. API 设计

统一前缀 `/api/v1`，成功返回 JSON；错误格式固定为：

```json
{"error":{"code":"INVALID_IMAGE","message":"图片格式不受支持","details":{}}}
```

| 方法与路径 | 用途 | 关键响应 |
|---|---|---|
| `GET /health` | 存活检查 | `{"status":"ok"}` |
| `GET /api/v1/capabilities` | FFmpeg、MiniMax、风格能力 | 能力布尔值，不返回密钥 |
| `GET /api/v1/projects` | 项目列表 | 项目摘要数组 |
| `POST /api/v1/projects` | 创建项目 | `201` + project |
| `GET /api/v1/projects/{id}` | 项目详情 | project |
| `PATCH /api/v1/projects/{id}` | 改标题、设置、素材文案/顺序 | 更新后的 project |
| `DELETE /api/v1/projects/{id}` | 删除项目 | `204` |
| `POST /api/v1/projects/{id}/assets` | multipart 上传图片 | `201` + asset |
| `DELETE /api/v1/projects/{id}/assets/{assetId}` | 删除素材 | `204` |
| `POST /api/v1/projects/{id}/storyboard:generate` | 携带 `expected_version`（首次为 null），生成并原子保存草稿 | 新 version + warnings；冲突返回 409 |
| `PUT /api/v1/projects/{id}/storyboard` | 携带当前 version 保存草稿 | 新 version；版本冲突返回 409 |
| `POST /api/v1/projects/{id}/renders` | 原子冻结当前草稿、创建 revision 并排队 | `202` + revision |
| `GET /api/v1/projects/{id}/revisions/{rid}` | 查询进度或结果 | revision |
| `POST /api/v1/projects/{id}/revisions/{rid}:cancel` | 取消任务 | revision |
| `POST /api/v1/projects/{id}/revisions/{rid}:retry` | 在锁内逐字节复制源 `render_input.json` 并创建新任务，不读取当前项目草稿/设置 | `202` + new revision |
| `GET /api/v1/projects/{id}/revisions/{rid}/video` | 播放/下载，支持 Range | `video/mp4` |
| `POST /api/v1/projects/{id}/revisions/{rid}/share` | 为指定成功 revision 开启/轮换分享 | 一次性返回 URL |
| `DELETE /api/v1/projects/{id}/share` | 撤销项目当前分享 | `204` |
| `GET /api/v1/share/{token}` | 分享页元数据 | 令牌绑定 revision 的只读标题和视频 URL |
| `GET /api/v1/share/{token}/video` | 分享令牌所绑定的视频，支持 Range | `video/mp4` |

一个项目同一时刻只有一个活动分享链接，且链接永远固定到创建时指定的 `completed` revision；生成新 revision 不会改变旧链接内容。再次分享同一或其他 revision 会轮换 token 并使旧链接立即失效。token 由 32 字节随机值构成，manifest 保存 `SHA-256(token)`；随机 token 自身具有 256 位熵，无需另存盐，比较使用常量时间函数。

关键错误码：`PROJECT_NOT_FOUND`、`ASSET_NOT_FOUND`、`ASSET_INTEGRITY_ERROR`、`INVALID_IMAGE`、`FILE_TOO_LARGE`、`INVALID_STORYBOARD`、`STORYBOARD_VERSION_CONFLICT`、`RENDER_ALREADY_ACTIVE`、`RENDER_FAILED`、`INSUFFICIENT_STORAGE`、`SHARE_DISABLED`。

## 9. 分镜、字幕、特效与 BGM 规则

### 9.1 确定性规则方案

1. 每张图片至少一个镜头，默认时长 `clamp(文字字符数 / 6, 2.5, 6.0)` 秒。
2. 总时长超过 60 秒时按比例缩短，但每镜不低于 2 秒；仍超限则拒绝并提示减少素材。
3. 总文案按标点切句，未填写逐图文案时按图片顺序均匀分配。
4. 相邻镜头避免使用相同 motion；首尾使用淡入/淡出。
5. 字幕每行最多约 18 个中文字符，最多两行；以渲染后像素宽度为最终准则。

### 9.2 风格模板

| 风格 | 色彩/字幕 | 动效与转场 | BGM 分类 |
|---|---|---|---|
| 轻松幽默 | 暖黄强调、较粗字幕 | 快速轻缩放、滑动；避免频闪 | upbeat/light |
| 浪漫温馨 | 暖粉/米白、柔和阴影 | 慢推近、交叉淡化 | acoustic/warm |
| 科技感 | 青蓝、细描边 | 平移、轻微故障切换；控制强度 | electronic/minimal |

时间线契约：`Scene.duration` 表示该镜头从开始到结束的完整屏幕时间；除最后一镜外，结尾与下一镜重叠转场。轻松幽默、浪漫温馨、科技感的默认转场时长分别为 0.4、0.8、0.5 秒，且不得超过相邻任一镜头时长的 25%。第 1 镜只做 0.4 秒画面淡入，最后一镜 `transition=none` 并做 0.6 秒淡出。镜头开始偏移按 `next_start = current_start + current_duration - transition_duration` 计算，`Storyboard.duration_seconds = 最后一镜 start + duration`；规则规划器以该公式反算并保证最终 15–60 秒，RenderPlan 单测逐帧验证偏移，ffprobe 允许误差不超过 0.5 秒。

BGM 契约：领域分类固定映射为 `upbeat/light → assets/music/humorous`、`acoustic/warm → assets/music/romantic`、`electronic/minimal → assets/music/tech`。只纳入 `LICENSES.md` 中记录且文件存在的曲目；按规范化相对路径排序，再用 `SHA-256(project_id + style)` 对曲目数取模。该选择仅在 `POST /renders` 创建 revision 时执行一次并冻结到 `render_input.json`；worker 不重新枚举曲库。新增或重排曲库不影响已创建 revision，冻结文件丢失或摘要不一致时返回 `ASSET_INTEGRITY_ERROR`。

### 9.3 MiniMax 使用边界

- 输入仅包含用户文字、图片文件名/顺序和风格，不上传图片二进制，除非未来明确启用多模态能力并取得用户同意。
- 输出只允许改变字幕润色、短标题、motion/transition 枚举和 BGM 分类，不能产生 FFmpeg 参数、文件路径或命令。
- 使用服务端固定 system prompt 和 JSON Schema 校验；超时 15 秒，最多重试 1 次。
- 环境仅要求 `MINIMAX_API_KEY`。适配器目标模型固定为用户要求的 `MiniMax-H3`，API 版本固定为 `v1`；官方账户若不提供该模型，capabilities 报告不可用并全量使用规则方案，不静默换模型。`MINIMAX_API_BASE` 和 `MINIMAX_MODEL` 仅作为部署者高级覆盖项写入 `.env.example`，普通用户界面不展示。Task 7 依据实施时的 MiniMax 官方文档锁定完整请求 URL、Bearer 认证头和 JSON schema，并以 mock 契约测试作为代码完成门槛；真实外部 smoke 仅在部署方提供合规测试 Key 和网络时执行，不阻塞无 Key MVP。
- 日志不记录 Key 和完整用户文案；错误信息先脱敏。

### 9.4 FFmpeg 渲染步骤

1. 用 Pillow 自动纠正 EXIF 方向并转为 sRGB JPEG。
2. 生成 1080×1920 背景：原图等比填充并模糊；前景图等比包含，避免主体裁切。
3. 每个镜头用 `zoompan`/`scale`/`crop` 生成中间无音频片段。
4. 按 storyboard 转场合并镜头；转场不可用时降级为硬切，不执行任意外部脚本。
5. 生成 ASS 字幕，使用项目内置中文字体并限制在平台安全区。
6. 使用 `render_input.json` 中冻结的 BGM 和字体，验证文件及许可证条目摘要；BGM 裁剪/循环、响度归一化并设置 1 秒淡入淡出，禁止运行时重新选曲。
7. 合成音视频和字幕，输出 H.264/AAC MP4；运行 `ffprobe` 验证编码、尺寸、时长和可播放性。
8. 原子移动为 `output.mp4`，清理 revision 临时文件，状态变为 `completed`。

## 10. 非功能需求

### 10.1 性能与容量

- 目标环境：4 vCPU、8 GB RAM、可用磁盘不少于 20 GB。
- 30 秒、10 张 1080p 图片的视频，在目标服务器上 3 分钟内完成作为首版性能目标，不作为跨硬件绝对承诺。
- 上传使用流式写盘，不把整个文件读入内存。
- 队列默认并发为 1；可通过服务端环境变量调到 2，但上线前必须压测。
- 视频响应支持 HTTP Range，以便浏览器拖动播放。

### 10.2 安全与隐私

- 根据文件魔数和 Pillow 解码结果验证类型，不信任扩展名和客户端 MIME。
- 对像素总数设上限，防止解压炸弹；移除 EXIF 元数据。
- 文件名只用于展示，磁盘路径全部使用服务端 UUID；拒绝路径穿越和符号链接。
- FFmpeg 使用参数数组启动，不拼接 shell 字符串；AI 输出只能映射白名单枚举。
- 限制请求体、上传数量、文字长度和同时任务数。
- 生产环境限定 CORS 来源并使用 HTTPS；反向代理以 Basic Auth 保护编辑 SPA 和项目 API，仅 `/share/*`、`/api/v1/share/*` 允许匿名持 token 访问，另匿名 `/health` 只返回固定存活状态且不泄露版本、路径或能力。首版不允许把未保护的项目 API 暴露到公网。
- Basic Auth 凭据只以部署 secret/htpasswd 文件注入，不进入仓库、镜像、浏览器持久存储或项目文件；前后端不记录凭据和 MiniMax Key。
- 分享 token 使用 32 字节安全随机数，manifest 仅保存 SHA-256 摘要并以常量时间比较；撤销或轮换后立即失效。
- 明示用户必须拥有上传素材及音乐的使用权。

### 10.3 可观测性

- JSON 结构化日志字段：`request_id`、`project_id`、`revision_id`、`stage`、`duration_ms`、`error_code`。
- `/health` 只判断进程；`/api/v1/capabilities` 报告 FFmpeg、字体、曲库和 MiniMax 是否可用。
- 每个 revision 保存 FFmpeg stderr 到 `render.log`，API 只返回安全摘要。
- 记录上传数、队列长度、各阶段耗时、成功率和磁盘余量；首版先输出日志，后续接监控系统。

## 11. 测试与验收策略

### 11.1 测试层级

- 后端单元测试：模型校验、路径安全、原子 manifest、规则分镜、MiniMax 降级、状态机、ASS 转义。
- 后端集成测试：用 2–3 张测试图片调用真实 FFmpeg，验证 MP4 流信息。
- API 测试：创建、上传、编辑、排队、取消、下载、分享与错误格式。
- 前端组件测试：上传限制、素材排序、步骤状态、进度和错误展示。
- E2E：从创建项目到下载、修改再渲染、分享与撤销。
- 人工视觉验收：三种风格各一条样片，检查主体裁切、字幕安全区、转场、音量和移动端播放。

### 11.2 MVP 完成定义

以下条件全部满足才算首版完成：

1. 新用户可在一个页面流程中上传 1–20 张合法图片和文字，选择三种风格之一。
2. 未配置 MiniMax 时可完整生成；配置一个有效 Key 后可获得 AI 润色方案，失败自动降级且有非阻断提示。
3. 输出为 1080×1920 H.264/AAC MP4，含所选字幕、动效、转场和已授权 BGM。
4. 页面持续显示真实任务状态，刷新页面后状态不丢失，服务重启可恢复未完成任务。
5. 用户可修改方案并生成新 revision，能预览和下载任一成功 revision。
6. 指定成功 revision 的分享链接可开启、访问、下载和撤销；生成新 revision 不改变旧链接内容，撤销/轮换后旧链接返回 404。
7. 非法文件、超限、磁盘不足、FFmpeg 失败均返回稳定错误码，无路径、Key 或命令泄漏。
8. 后端单元/API/FFmpeg 集成测试、前端单元测试和关键 E2E 全部通过。
9. README 可让新开发者在 10 分钟内启动开发环境并生成示例视频。
10. 参考抖音样片由产品方人工确认；若差异仅为模板参数，通过调整风格模板完成，不引入新架构。

---

# 实施计划

> **For agentic workers:** 实施前使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`，逐任务执行；每个步骤使用 `- [ ]` 跟踪。

**目标：** 交付一个无数据库、可在单服务器运行的图文竖屏视频生成网站。  
**架构：** React SPA 调用 FastAPI；后端以项目 manifest 管理文件状态，通过单消费者任务队列调用 FFmpeg；MiniMax 是带规则降级的可选分镜增强。  
**技术栈：** React、TypeScript、Vite、FastAPI、Pydantic、Pillow、FFmpeg/ffprobe、pytest、Vitest、Playwright、Docker Compose。

## 12. 实施任务

### Task 1：初始化仓库与开发基线

**文件：** 创建 `.gitignore`、`.env.example`、`README.md`、`frontend/`、`backend/requirements.txt`、`backend/app/main.py`、`backend/tests/`、`data/.gitkeep`、`assets/fonts/LICENSES.md`、`assets/music/LICENSES.md` 及三个风格目录的 `.gitkeep`。

- [ ] 确认本机安装 Node.js 22、Python 3.12、FFmpeg 7 和 Git；运行 `node -v && python3 --version && ffmpeg -version && ffprobe -version`，四项均成功。
- [ ] 在当前目录执行 `git init`，添加忽略项：`.env`、虚拟环境、`node_modules`、构建产物、`data/projects`、缓存和渲染临时文件；保留 `data/.gitkeep`。
- [ ] 创建 `assets/music/{humorous,romantic,tech}` 和 `assets/fonts`；两个 `LICENSES.md` 规定每项必须记录相对路径、来源 URL、作者、许可证、下载日期和 SHA-256，未登记资产不得打包。
- [ ] 用 Vite 创建 React + TypeScript 前端；安装依赖时使用 `--save-exact`，提交 lockfile。
- [ ] 创建 FastAPI 最小应用和精确版本的 `requirements.txt`，实现 `GET /health` 返回 `{"status":"ok"}`。
- [ ] 添加前后端格式化、lint 和测试命令；README 写明启动前后端的准确命令。
- [ ] 运行 `npm --prefix frontend run build` 和 `pytest backend/tests -q`，预期均退出码 0。
- [ ] 仅暂存本任务文件并提交：`chore: initialize video generator workspace`。

### Task 2：配置、能力探测与领域模型

**文件：** 创建 `backend/app/config.py`、`backend/app/domain/models.py`、`backend/app/api/capabilities.py`、`scripts/check_ffmpeg.py`；测试 `backend/tests/test_config.py`、`test_models.py`、`test_capabilities.py`。

- [ ] 先写测试，覆盖合法/非法风格、图片数、文字长度、scene 时长、状态迁移和环境变量默认值。
- [ ] 定义 `Style`、`ProjectSettings`、`Asset`、`Scene`、`Storyboard`、`Revision`、`Project` Pydantic 模型；使用带时区 UTC 时间。
- [ ] 配置 `DATA_DIR`、上传限制、磁盘下限、任务并发、FFmpeg 路径和 `MINIMAX_API_KEY`；Key 字段在 repr/日志中隐藏。
- [ ] 实现 `scripts/check_ffmpeg.py`，检查 `ffmpeg`、`ffprobe`、H.264 编码器、字体和曲库目录。
- [ ] 实现 `/api/v1/capabilities`，只返回能力布尔值、限制和风格列表。
- [ ] 运行 `pytest backend/tests/test_config.py backend/tests/test_models.py backend/tests/test_capabilities.py -q`，预期全通过。
- [ ] 提交：`feat: add configuration and domain models`。

### Task 3：实现文件项目仓库

**文件：** 创建 `backend/app/repositories/file_projects.py`、`backend/tests/repositories/test_file_projects.py`。

- [ ] 先写测试：创建/读取/列表/更新/不存在、并发写入后 JSON 完整、拒绝非 UUID、进程中断不覆盖旧 manifest，以及两个并发更新中旧 `version` 返回 CAS 冲突而不丢字段。
- [ ] 实现 `create`、`get`、`list`、`update`、`delete_to_trash`；写入采用每项目 `asyncio.Lock`、manifest `version`、临时文件、flush、`fsync`、`os.replace`。
- [ ] 保证所有路径从受验证 UUID 派生；禁止客户端文件名参与目录拼接。
- [ ] 应用启动时扫描项目目录；损坏 manifest 隔离并记录错误，不阻止其他项目启动。
- [ ] 运行 `pytest backend/tests/repositories/test_file_projects.py -q`，预期全通过。
- [ ] 提交：`feat: persist projects with atomic manifests`。

### Task 4：实现项目 CRUD API

**文件：** 创建 `backend/app/api/projects.py`、`backend/app/api/errors.py`；修改 `backend/app/main.py`；测试 `backend/tests/api/test_projects.py`。

- [ ] 先写 API 测试，覆盖创建、列表、读取、更新、删除、404、非法标题和统一错误结构。
- [ ] 实现 `/api/v1/projects` 路由，响应模型不暴露服务器路径、token hash 和内部日志。
- [ ] `PATCH` 只接受标题、设置、素材文案和素材顺序白名单字段。
- [ ] 删除采用移入 trash；存在活动渲染时返回 `409 RENDER_ALREADY_ACTIVE`。
- [ ] 运行 `pytest backend/tests/api/test_projects.py -q`，预期全通过。
- [ ] 提交：`feat: add project management API`。

### Task 5：实现安全图片上传与预处理

**文件：** 创建 `backend/app/services/uploads.py`、`backend/app/api/assets.py`；测试 `backend/tests/services/test_uploads.py`、`backend/tests/api/test_assets.py`，素材 `backend/tests/fixtures/images/`。

- [ ] 先写测试：JPG/PNG/WebP 成功；伪扩展名、损坏文件、超 15 MB、超像素、SVG、路径穿越文件名和第 21 张图片失败。
- [ ] multipart 内容流式写入随机临时文件，同时计算大小；达到上限立即终止。
- [ ] 用 Pillow 完整解码、应用 EXIF 方向、转 sRGB、移除元数据并输出 `normalized.jpg`。
- [ ] 保存原始展示文件名但磁盘只使用 UUID；失败时清理临时文件且不修改 manifest。
- [ ] 实现上传和删除 API；被当前 draft 引用时返回冲突；仅被历史 revision 引用时从当前 assets 列表移除但保留不可变 normalized 文件，直到项目整体删除。
- [ ] 运行 `pytest backend/tests/services/test_uploads.py backend/tests/api/test_assets.py -q`，预期全通过。
- [ ] 提交：`feat: add validated image uploads`。

### Task 6：实现规则分镜与风格模板

**文件：** 创建 `backend/app/services/storyboards.py`、`backend/app/render/templates.py`、`backend/app/api/storyboards.py`；测试 `backend/tests/services/test_storyboards.py`、`backend/tests/api/test_storyboards.py`。

- [ ] 先写参数化测试，覆盖三种风格、逐图文案、总文案切句、无文案、2 秒下限、60 秒上限、转场重叠时长公式和 motion 不连续重复。
- [ ] 实现确定性文本切句、字幕换行、时长/转场 offset 计算、motion/transition 轮换和 BGM 分类到曲库目录映射。
- [ ] 校验 storyboard 的 asset 引用属于当前项目；允许且仅允许相邻镜头按各自 transition 时长重叠，按 9.2 公式校验 offset、无间隙、总时长和白名单枚举。
- [ ] 实现生成和保存 storyboard API；两个接口都提交 `expected_version`，草稿写入固定 `storyboard/draft.json` 并原子更新 manifest；无图片返回 `INVALID_STORYBOARD`，旧版本保存/生成均返回 `409 STORYBOARD_VERSION_CONFLICT`。
- [ ] 运行 `pytest backend/tests/services/test_storyboards.py backend/tests/api/test_storyboards.py -q`，预期全通过且同一输入输出完全一致。
- [ ] 提交：`feat: generate deterministic storyboards`。

### Task 7：接入 MiniMax 单 Key 可选增强

**文件：** 创建 `backend/app/integrations/minimax.py`；修改 `backend/app/services/storyboards.py`；测试 `backend/tests/integrations/test_minimax.py`、`backend/tests/services/test_storyboard_fallback.py`。

- [ ] 先写 HTTP mock 测试：成功 JSON、超时、401、429、500、非 JSON、非法枚举、超长字幕和无 Key。
- [ ] 定义固定提示词和严格响应 schema；只发送必要文字、顺序和风格，不发送图片文件、路径和 Key。
- [ ] 客户端超时 15 秒、最多重试一次；授权失败不重试；所有失败转为稳定 warning code。
- [ ] 将合法 AI 响应映射回领域枚举，再执行与规则方案相同的完整校验。
- [ ] 配置缺失或任何调用失败时返回 RulePlanner 结果，并在响应加入 `AI_FALLBACK_USED`，HTTP 仍为成功。
- [ ] 运行 `pytest backend/tests/integrations/test_minimax.py backend/tests/services/test_storyboard_fallback.py -q`，预期 mock 契约与无 Key 降级测试全部通过，日志不含测试 Key 和完整输入文案；真实外部调用不是本任务完成前提。
- [ ] 提交：`feat: add optional MiniMax storyboard enhancement`。

### Task 8：实现字幕与渲染计划

**文件：** 创建 `backend/app/render/plan.py`、`backend/app/render/subtitles.py`；测试 `backend/tests/render/test_plan.py`、`backend/tests/render/test_subtitles.py`。

- [ ] 先写测试，覆盖中文、emoji、ASS 特殊字符、换行、负时间、镜头间隙、字幕关闭和三种模板。
- [ ] 把 revision 的 `render_input.json` 编译成内部 `RenderPlan`，验证输入摘要以及每张图片、冻结 BGM、字体和对应许可证条目的 SHA-256；所有文件路径由 repository 解析，所有动效映射到固定 FFmpeg 参数片段。
- [ ] 生成 ASS：内置字体、1080×1920 PlayRes、安全区、描边和风格色；正确转义 `{}`、反斜杠与换行。
- [ ] 禁止 AI/用户文本进入滤镜表达式；字幕只写入独立 ASS 文件。
- [ ] 运行 `pytest backend/tests/render/test_plan.py backend/tests/render/test_subtitles.py -q`，预期全通过。
- [ ] 提交：`feat: compile storyboards into safe render plans`。

### Task 9：实现 FFmpeg 渲染管线

**文件：** 创建 `backend/app/render/pipeline.py`、`backend/app/render/probe.py`、`scripts/check_assets.py`；测试 `backend/tests/render/test_pipeline.py`；测试资产放 `backend/tests/fixtures/media/`，生产字体放 `assets/fonts/NotoSansSC-Regular.otf`，生产 BGM 分别放三个 `assets/music/<style>/` 目录。

- [ ] 先写集成测试：2 张横竖图分别渲染启用/禁用字幕与音乐的视频；取消时子进程终止；错误时无伪造 output；revision 创建后新增/重排曲库不换曲，替换/删除冻结 BGM 或字体则以 `ASSET_INTEGRITY_ERROR` 失败。
- [ ] 用参数数组调用 subprocess，绝不使用 `shell=True`；限制临时目录和输出路径。
- [ ] 实现图片背景模糊 + 前景 contain、镜头 motion、转场、ASS 字幕、BGM 循环/裁剪/淡化和最终编码。
- [ ] 测试 fixture 使用脚本生成的纯色图片和正弦波音频，不冒充生产曲库；生产环境引入 Noto Sans SC（SIL OFL）及每种风格至少 1 首经负责人确认可商用/再分发的 BGM，并按 Task 1 字段登记许可证和 SHA-256。
- [ ] 实现 `scripts/check_assets.py`，运行 `python scripts/check_assets.py --font assets/fonts/NotoSansSC-Regular.otf --music-root assets/music --min-tracks-per-style 1 --verify-digests`；预期三个风格均通过且无未登记文件。
- [ ] 音乐选择只在创建 revision 时按 9.2 的分类→目录映射、规范化路径排序和 SHA-256 取模执行，并把曲目/字体路径、文件摘要和许可证条目摘要冻结到 `render_input.json`；worker 禁止重新选曲，没有许可证记录或摘要不匹配时拒绝渲染。
- [ ] 解析 FFmpeg `-progress` 输出，将阶段进度限制在 0–99；只有 probe 验证成功后写 100。
- [ ] 用 ffprobe 断言输出：1080×1920、30 fps、H.264、有音频时 AAC、时长误差不超过 0.5 秒、`yuv420p`。
- [ ] 运行 `pytest backend/tests/render/test_pipeline.py -q -m ffmpeg`，预期全通过。
- [ ] 提交：`feat: render vertical videos with ffmpeg`。

### Task 10：实现任务队列、恢复、取消与重试

**文件：** 创建 `backend/app/jobs/queue.py`、`backend/app/api/renders.py`；修改 `backend/app/main.py`；测试 `backend/tests/jobs/test_queue.py`、`backend/tests/api/test_renders.py`。

- [ ] 先写测试：FIFO 串行、重复提交冲突、状态顺序、进度持久化、取消 queued/running、失败、启动恢复；排队后修改 draft/settings/素材文案或新增/重排曲库不改变 `render_input.json`；修改当前项目后 retry 的 `render_input.json`（含 BGM/字体引用）仍与源 revision 逐字节相同。
- [ ] 实现 asyncio 单消费者队列；应用 lifespan 启动 worker，关闭时停止接单并安全终止当前进程。
- [ ] `POST /renders` 在项目锁内读取指定 draft version，冻结 storyboard、完整 settings、被引用图片的不可变路径/摘要，以及按 9.2 选中的 BGM 与字体/许可证摘要到 `render_input.json`，记录整体摘要，创建 revision 后才入队；worker 只接收 `project_id/revision_id`，验证全部摘要并读取该文件，绝不读取项目草稿/当前 settings、重新枚举曲库或创建第二个 revision。
- [ ] retry 在项目锁内逐字节复制源 revision 的 `render_input.json` 到新 revision，记录相同输入摘要后入队；当前草稿、设置和素材元数据不参与重试。
- [ ] 服务启动时按状态表恢复：`queued` 保持、`running` 回到 `queued`、`cancelling` 变 `cancelled`、终态不变。
- [ ] 提交前按 `输入规范化文件总大小 × 2 + 预估未压缩帧缓存上限 2 GB + 目标输出 500 MB` 计算所需空间；`max(该估值, 5 GB)` 加配置磁盘保留量后仍不足则返回 `INSUFFICIENT_STORAGE`。
- [ ] 实现提交、状态、取消和重试 API；保证同项目最多一个活动任务。
- [ ] 运行 `pytest backend/tests/jobs/test_queue.py backend/tests/api/test_renders.py -q`，预期全通过。
- [ ] 提交：`feat: add recoverable render queue`。

### Task 11：实现视频播放、下载和分享

**文件：** 创建 `backend/app/services/shares.py`、`backend/app/api/media.py`、`backend/app/api/shares.py`；测试 `backend/tests/api/test_media.py`、`backend/tests/api/test_shares.py`。

- [ ] 先写测试：完整下载、Range `206`、无效范围 `416`、未完成 revision `409`、为指定 completed revision 开启/轮换/撤销分享、新 revision 不改变旧链接内容、旧 token 失效。
- [ ] 视频端点只解析项目/revision 元数据，不接受任意磁盘路径；正确返回 `Content-Type`、`Content-Length`、`Accept-Ranges` 和安全文件名。
- [ ] 创建 32 字节安全随机 token，只保存 `SHA-256` 摘要并用 `hmac.compare_digest` 比较；manifest 同时记录绑定的 `revision_id` 与创建时间。
- [ ] 分享 API 只返回标题、视频 URL 和下载能力，不返回项目编辑信息。
- [ ] 运行 `pytest backend/tests/api/test_media.py backend/tests/api/test_shares.py -q`，预期全通过。
- [ ] 提交：`feat: add video delivery and revocable sharing`。

### Task 12：实现前端项目列表和素材编辑

**文件：** 创建 `frontend/src/app/router.tsx`、`frontend/src/api/client.ts`、`frontend/src/api/types.ts`、`frontend/src/features/projects/ProjectListPage.tsx`、`frontend/src/features/editor/AssetStep.tsx`；测试 `frontend/src/features/projects/ProjectListPage.test.tsx`、`frontend/src/features/editor/AssetStep.test.tsx`。

- [ ] 先写组件测试：空列表、创建项目、API 错误、上传进度、非法文件前置提示、排序、删除和逐图文案编辑。
- [ ] API client 统一解析后端错误结构，并给每个请求生成 request ID；类型与后端 OpenAPI 对齐。
- [ ] 实现项目列表和编辑页步骤条；上传区支持文件选择和拖放，显示缩略图、容量、顺序和文案。
- [ ] 排序/文案采用显式保存状态；保存失败保留本地输入并可重试。
- [ ] 实现键盘可操作排序替代方案、可见焦点和表单 label。
- [ ] 运行 `npm --prefix frontend test -- --run` 和 `npm --prefix frontend run build`，预期全通过。
- [ ] 提交：`feat: add project and asset editing UI`。

### Task 13：实现风格、分镜和生成进度界面

**文件：** 创建 `frontend/src/features/editor/StyleStep.tsx`、`frontend/src/features/editor/StoryboardEditor.tsx`、`frontend/src/features/render/RenderStep.tsx`；测试 `frontend/src/features/editor/StyleStep.test.tsx`、`frontend/src/features/editor/StoryboardEditor.test.tsx`、`frontend/src/features/render/RenderStep.test.tsx`。

- [ ] 先写测试：三种风格、开关、生成方案、AI 降级警告、编辑字幕/时长、非法方案、提交、轮询、刷新恢复、取消和失败重试。
- [ ] 风格卡明确展示色彩、动效和音乐特点；没有曲库或 FFmpeg 时根据 capabilities 禁用生成并说明原因。
- [ ] StoryboardEditor 按镜头展示图片、字幕、时长、motion 和 transition；只允许合法范围和枚举。
- [ ] RenderStep 提交后每 1 秒轮询；页面不可见时降频，completed/failed/cancelled 后停止。
- [ ] 展示阶段文字和真实百分比；失败显示安全错误信息及重试，不展示 render.log 原文。
- [ ] 运行 `npm --prefix frontend test -- --run src/features/editor src/features/render && npm --prefix frontend run typecheck && npm --prefix frontend run build`，预期全部退出码 0。
- [ ] 提交：`feat: add storyboard and render workflow UI`。

### Task 14：实现审核、修订、下载和分享界面

**文件：** 创建 `frontend/src/features/render/ResultPage.tsx`、`frontend/src/features/share/SharePage.tsx`、`frontend/src/features/share/ShareActions.tsx`；测试 `frontend/src/features/render/ResultPage.test.tsx`、`frontend/src/features/share/SharePage.test.tsx`、`frontend/src/features/share/ShareActions.test.tsx`。

- [ ] 先写测试：播放器 URL、下载、返回修改、revision 切换、开启/撤销分享、复制、Web Share 支持/不支持、无效 token。
- [ ] 结果页显示 9:16 播放器、revision 状态、警告、下载和“修改并重新生成”。
- [ ] 分享按钮优先调用 `navigator.share`，不支持时复制链接；复制失败时展示可选中的 URL。
- [ ] 分享页只读，token 无效/撤销后显示资源不可用，不泄露项目 ID 或服务端路径。
- [ ] 运行 `npm --prefix frontend test -- --run src/features/render/ResultPage.test.tsx src/features/share && npm --prefix frontend run typecheck && npm --prefix frontend run build`，预期全部退出码 0。
- [ ] 提交：`feat: add review download and share UI`。

### Task 15：端到端测试与无障碍/兼容性检查

**文件：** 创建 `frontend/e2e/video-flow.spec.ts`、`frontend/playwright.config.ts`、`frontend/vitest.config.ts`、`frontend/src/test/setup.ts`、`scripts/smoke_test.py`；修改 `frontend/package.json` 测试脚本。

- [ ] 用固定测试图片和测试 BGM 编写 E2E：创建 → 上传 → 排序/文案 → 选风格 → 生成方案 → 渲染 → 播放 → 下载 → 修改再渲染 → 分享 → 撤销。
- [ ] 增加异常 E2E：非法图片、MiniMax 降级、渲染失败、刷新恢复和磁盘不足模拟。
- [ ] `scripts/smoke_test.py` 通过 API 完成最短流程，并用 ffprobe 校验下载文件。
- [ ] 在 Chrome、Safari 和移动视口验证上传、播放、下载；运行 axe 检查关键页面无严重无障碍错误。
- [ ] 运行 `pytest backend/tests -q`、`npm --prefix frontend test -- --run`、`npm --prefix frontend run build`、`npm --prefix frontend run e2e`，预期全部退出码 0。
- [ ] 提交：`test: cover complete video generation workflow`。

### Task 16：容器化、运维和数据保护

**文件：** 创建 `backend/Dockerfile`、`frontend/Dockerfile`、`docker-compose.yml`、`scripts/backup_data.sh`；完善 `README.md`、`.env.example`。

- [ ] 后端镜像安装固定主版本 FFmpeg 和中文字体；以前台非 root 用户运行，挂载 `/app/data` 和只读 `/app/assets`。
- [ ] 前端生产镜像构建静态文件；反向代理用部署 secret 中的 htpasswd 保护编辑 SPA 与项目 API，仅匿名放行 `/share/*`、`/api/v1/share/*` 和 `/health`，并正确代理视频 Range。
- [ ] 设置上传请求体 320 MB、普通 API 超时 30 秒、上传超时 10 分钟、视频读取超时 10 分钟；渲染通过异步任务执行，不占用 HTTP 长连接。
- [ ] compose 声明 healthcheck、持久卷、日志轮转、htpasswd secret 和 `MINIMAX_API_KEY`；不把凭据或 Key 写入镜像和 compose 文件。
- [ ] 备份脚本在短暂停止接单后归档 manifests、uploads 和成功 outputs；恢复说明包含权限和完整性检查。
- [ ] README 写明开发、生产启动、曲库授权登记、单 Key 配置、备份恢复、磁盘清理和常见错误处理。
- [ ] 运行 `docker compose config --quiet`，预期退出码 0 且 `grep -R "MINIMAX_API_KEY=" docker-compose.yml frontend backend` 无匹配；运行 `docker compose up --build -d && python scripts/smoke_test.py --base-url http://localhost --basic-auth-env`，预期生成并 probe 有效 MP4。
- [ ] 提交：`chore: add production deployment and operations guide`。

### Task 17：发布验收

**文件：** 创建 `docs/release-checklist.md`、`docs/samples/README.md`、`scripts/release_check.py`、`scripts/restart_recovery_test.py`、`artifacts/.gitkeep`。

- [ ] 使用 `python scripts/release_check.py --styles humorous romantic tech --output artifacts/release` 以同一组固定素材生成三种风格样片；预期输出每条 MP4、ffprobe JSON、渲染耗时和文件大小清单。
- [ ] 产品方对照可访问的参考视频人工确认画幅、图片运动、字幕位置/节奏、转场强度和 BGM 氛围；若参考仍不可访问，则以 2.2 表格、9.2 时间线契约和三种样片作为明确替代验收基线，不宣称像素级复刻。
- [ ] 运行 `python scripts/release_check.py --verify-licenses assets/music/LICENSES.md assets/fonts`，预期所有音乐、字体和测试素材均有来源、作者、许可、下载日期及文件摘要。
- [ ] 在目标规格服务器运行 `python scripts/release_check.py --repeat 10 --assert-success-rate 1.0 --assert-no-active-jobs`，预期 10 次全部成功、无崩溃和无遗留活动状态，证据写入 `artifacts/release/summary.json`。
- [ ] 运行 `python scripts/restart_recovery_test.py`，预期 queued/running/cancelling 按状态表恢复；运行 `bash scripts/backup_data.sh create artifacts/backup.tar.zst && bash scripts/backup_data.sh restore-test artifacts/backup.tar.zst`，预期恢复副本校验和一致。
- [ ] 若部署环境提供合规 `MINIMAX_API_KEY`，运行 `python scripts/smoke_test.py --base-url http://localhost --minimax --redact-output` 并保存脱敏证据；无 Key 时记录 `not_configured`，确认规则降级通过，不阻塞发布。
- [ ] 运行 `pytest backend/tests -q && npm --prefix frontend test -- --run && npm --prefix frontend run typecheck && npm --prefix frontend run build && npm --prefix frontend run e2e`，预期全部退出码 0；再验证磁盘不足、分享撤销、项目删除和日志脱敏证据均写入 release checklist。
- [ ] 对照“11.2 MVP 完成定义”逐项签字并提交：`git add docs/release-checklist.md docs/samples artifacts/release/summary.json artifacts/release/*.probe.json && git commit -m "docs: add v0.1.0 release evidence"`；生成的 MP4 样片保留在服务器但不提交 Git。
- [ ] 全部通过后执行 `git tag -a v0.1.0 -m "video generator v0.1.0"`；运行 `git status --short` 预期无输出，`git tag --points-at HEAD` 预期包含 `v0.1.0`。

## 13. 推荐实施顺序与里程碑

| 里程碑 | 包含任务 | 可演示结果 |
|---|---|---|
| M1：基础与素材 | Task 1–5 | 创建项目、上传并安全保存图片 |
| M2：方案生成 | Task 6–8 | 三种风格的可编辑分镜，MiniMax 可选增强 |
| M3：视频产出 | Task 9–11 | 异步生成、恢复、播放、下载和分享 MP4 |
| M4：完整网站 | Task 12–14 | 用户可在网页走完整闭环 |
| M5：可发布 | Task 15–17 | 测试、容器部署、授权和发布证据齐全 |

依赖关系：M1 → M2 → M3 → M4 → M5。Task 12 的项目/素材页面可在 Task 4–5 API 稳定后开始；Task 13 的分镜与渲染界面必须等待 Task 6–10 的草稿版本、MiniMax warning、revision 和状态契约稳定。任何 API 类型变更必须先更新 OpenAPI 契约和前端类型。

## 14. 后续演进（不进入 v0.1.0）

1. **v0.2：** TTS 旁白、字幕按音频强制对齐、更多模板、项目复制。
2. **v0.3：** PostgreSQL + 对象存储 + Redis/Celery，多用户、登录和配额。
3. **v0.4：** 接入经审核的 AI 作曲/文生视频 API；采用 provider adapter，不改变项目/修订领域模型。
4. **v0.5：** 可视化时间线和字幕编辑器，评估 `flycut-caption`。
5. **更后期：** 社媒 OAuth/发布；需单独评估平台协议、审核机制、账号安全和合规，不复用当前公开分享 token 作为平台凭证。

## 15. 可选的早期视觉校准

文档中的 2.2 默认参数、9.2 风格/时间线契约和 Task 17 三种样片构成参考链接不可访问时的完整验收基线，因此不阻塞 Task 1，也不承诺对原作品做像素级复刻。若产品方能合法提供参考视频文件或录屏，应在 M1 结束前校准以下模板参数，以减少发布阶段返工：

- 单镜平均时长和字幕出现节奏；
- 图片是全屏裁切还是模糊背景 + 完整前景；
- 字体、字号、字幕位置和强调色；
- 转场种类和强度；
- 是否需要旁白（若需要则进入 v0.2，除非产品方批准调整首版范围）。
