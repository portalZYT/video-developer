# 视频生成网站设计及实施任务

> **文档状态：** 可执行基线（2026-08-03）  
> **目标读者：** 产品、设计、前后端、测试与部署人员  
> **实施约定：** 执行任务时使用任务中的复选框跟踪；按任务顺序实施和验收，不并行修改同一模块。

## 1. 结论摘要

首版建设一个**单机 Linux 部署、无数据库、面向竖屏图文短视频**的网站。用户可以只提供文字，也可以上传图片并填写文字；视频风格可选择预设（如升学宴、生日宴），填写自定义描述，或提交已获授权的参考视频链接。系统分析素材与风格后自动加入创意分镜、字幕、转场、画面动效和合规 BGM，生成可预览的 MP4；用户可反复提出修改意见并生成新 revision，满意后下载和分享。

推荐方案：

- 前端：React + TypeScript + Vite + Ant Design；业务页面统一使用 Ant Design 组件和设计令牌，不并行引入第二套通用 UI 库。
- 后端：Express 5 + TypeScript；Express 是 Node.js 生态最广泛采用、概念少且易理解的 API 框架，前后端统一语言便于维护。
- 集成工具：部署目标是 Linux。Web 前端/后端语言保持不变，但第三方开源工具不限语言，可将 Python、Go、Rust、C/C++ 项目作为固定版本 CLI、受限子进程或独立容器接入；统一经 TypeScript adapter 调用，不把第三方内部模型泄漏到业务 API。
- 渲染：FFmpeg 为核心渲染器，Sharp 做图片预处理；参考视频解析/风格分析优先复用 yt-dlp、OpenCV、PySceneDetect、librosa、PaddleOCR/CLIP 等高质量免费开源能力，实施前核验许可证与 Star。
- 存储：应用服务器文件系统；每个项目使用 `manifest.json` 保存状态和版本，不引入数据库。
- 任务：进程内单消费者队列；同一时间默认只渲染 1 个任务，避免 FFmpeg 耗尽服务器资源。
- AI：MiniMax 只提供两个**独立、默认关闭**的辅助能力。API Key 不使用环境变量，由管理员在网站“系统设置”页面填写并原子保存到服务器 `data/system/settings.json`；默认 `MiniMax-M3` 生成提示词优化提案，必须经用户预览并显式接受后才可使用；默认 `MiniMax-H3` 生成字幕和特效内容，并进入分镜预览确认。两个模型也可在系统设置中替换。常规用户完全不启用 MiniMax 也能使用全部核心流程。
- BGM：首版根据风格**自动选择并裁剪本地已授权曲库**。语言模型不能直接生成音频，不把“选曲”误称为“AI 作曲”；真正的音乐生成 API 放到后续版本。

## 2. 已知条件、假设与范围边界

### 2.1 已知条件

1. 当前目录只有需求与候选项目资料，尚无代码，也不是 Git 仓库。
2. 首版不使用数据库，部署到 Linux 单服务器，上传数据、风格画像和 revisions 保存到应用数据目录。
3. Web 主栈固定为 React + TypeScript + Vite + Ant Design，以及 Express 5 + TypeScript；集成工具语言不限。
4. MiniMax 配置由管理员在系统设置网页填写，保存到 `data/system/settings.json`，两个项目级开关默认关闭。
5. 参考视频链接仅用于提取主题、色调、节奏、字幕布局和动效强度等抽象风格，不复制原视频素材、音乐、台词、水印或可识别创作表达。用户必须确认拥有分析权限并遵守平台条款。
6. “免费”定义为可自托管的开源软件或无需付费即可运行的本地模型；云 API 的免费额度和条款会变化，不能承诺永久免费，因此不作为核心流程依赖。

### 2.2 首版默认产品参数

以下参数作为可直接开发的默认决策；拿到参考视频样片后只调整模板参数，不改整体架构：

| 项目 | 首版默认值 |
|---|---|
| 输出 | MP4，H.264 + AAC，1080×1920，30 fps，`yuv420p` |
| 时长 | 15–60 秒；按文字量和图片数自动计算 |
| 图片 | 0–20 张；JPG、PNG、WebP；单张不超过 15 MB；无图片但有文字时生成排版文字卡与程序化背景 |
| 文字 | 图片和文字至少提供一项；总计不超过 5,000 字符，每张图可有独立文案 |
| 风格来源 | 三选一：预设下拉、自定义描述、参考视频链接 |
| 预设风格 | `school_banquet`升学宴、`birthday`生日宴、`wedding`婚礼、`romantic`浪漫温馨、`humorous`轻松幽默、`tech`科技感 |
| 参考视频 | 支持allowlist平台的公开链接；默认最长5分钟、最大200MB、单视频；分析后展示风格画像供确认 |
| 反馈修订 | 每次反馈生成结构化修改方案，用户确认后创建新revision；次数不设产品上限，但受磁盘/任务限制 |
| 动效 | 缓慢推近、平移、轻微缩放；按模板组合，不使用高风险闪烁效果 |
| 转场 | 淡入淡出、滑动、缩放切换；默认 0.4–0.8 秒 |
| 字幕 | 底部安全区内，白字描边；按分镜时间轴生成 ASS 并烧录 |
| 音频 | 自动匹配本地已授权 BGM，循环/裁剪至视频长度，淡入淡出 |
| UI 组件库 | Ant Design React；导航、表单、开关、上传、步骤、卡片、表格、弹窗、通知、进度统一使用其组件 |
| MiniMax 提示词优化 | Ant Design `Switch`，默认未选中；点击可选中/取消，启用后由 `MiniMax-M3`（可在系统设置更换）生成提案，预览并显式接受前不得使用 |
| MiniMax 字幕/特效 | 独立 Ant Design `Switch`，默认未选中；点击可选中/取消，启用后由 `MiniMax-H3`（可更换）生成，结果必须进入分镜预览确认 |
| 预览 | 提示词提案先做原文/优化文对比；完成渲染后网页播放器预览；首版不做浏览器内实时逐帧预渲染 |
| 修改 | 修改文字、顺序、风格或字幕后创建新修订并重新渲染 |
| 分享 | 生成不可预测的只读链接，支持复制链接和 Web Share API |
| 用户体系 | 首版无注册、账号和团队协作；编辑后台仅供单运营者/受信任内网使用 |
| 访问边界 | 生产反向代理必须用 Basic Auth 保护 SPA 与项目 API；仅 `/share/*`、`/api/v1/share/*` 及最小化的 `/health` 匿名，health 只返回 `{"status":"ok"}` |
| 部署 | 单实例、单机磁盘；不承诺多实例共享状态和水平扩容 |

### 2.3 首版不做

- 文生视频、数字人、口型同步、声音克隆、AI 图片生成。
- 自动发布到抖音/小红书等平台；“分享”仅指系统内公开链接和浏览器系统分享。
- 浏览器时间线剪辑器、逐帧特效编辑、多人协作。
- AI 原创作曲；只使用来源和商用授权明确的本地曲库。
- 数据库、对象存储、Redis、分布式队列、GPU 集群。
- 未经授权下载、绕过登录/DRM/风控、去水印、保存或重新发布参考视频；授权参考仅临时下载低清分析副本，提取抽象风格后按保留策略删除。
- 对参考作品做像素级复刻，或复制其原始画面、音乐、台词、人物肖像和受保护模板。

## 3. 用户与核心流程

### 3.1 目标用户

希望快速把图文素材制作成竖屏短视频、但不熟悉专业剪辑软件的内容创作者或运营人员。

### 3.2 主流程

**可选前置配置：** 只有需要 MiniMax 时，管理员进入“系统设置”，填写 API Key、确认 M3/H3 模型并测试连接。没有 Key 时两个 AI `Switch` 显示不可用，但项目的规则生成流程保持可用。

1. **创建项目：** 输入项目标题。
2. **收集素材：** 上传图片；拖拽排序；为每张图填写文案，也可填写一段总提示词/总文案。
3. **可选优化提示词：** 默认关闭。用户主动开启并点击优化后，`MiniMax-M3` 只生成候选提案；页面并排显示原文与优化文。用户可接受、拒绝或编辑后接受，只有显式接受的文本才成为当前有效提示词。
4. **选择风格与可选 AI 内容：** 选择三种模板之一，可开关字幕、BGM 和动效；“MiniMax-H3 字幕/特效”是另一个默认关闭的独立开关。
5. **生成方案：** 默认由开源/本地规则能力生成分镜；若启用 H3，则只让 H3生成字幕和特效建议。系统展示可编辑分镜，包括每镜时长、字幕、动效、转场和规则生成的 BGM 分类。
6. **确认并渲染：** 用户确认最终提示词来源和分镜后提交；未接受的 M3 提案永远不能进入渲染输入。页面展示排队、处理、成功或失败状态。
7. **审核与修改：** 播放生成结果；修改文案、图片顺序或风格后再次渲染，旧修订仍保留。
8. **最终交付：** 下载 MP4；按需开启只读分享链接并复制或调用系统分享。

### 3.3 异常流程

- 不合法图片：上传阶段立即拒绝并说明格式、尺寸或容量问题。
- MiniMax 未配置/对应模型不可用/超时/返回格式错误：保持原提示词或采用规则字幕/特效，记录非阻断警告；不得自动接受 M3 提案，也不得在用户不知情时切换到其他模型。
- FFmpeg 失败：任务标记为 `failed`，保存脱敏后的错误摘要；用户可原参数重试。
- 服务重启：按 7.3 状态表恢复；`queued` 保持、`running` 重新排队、`cancelling` 结束为 `cancelled`。
- 磁盘空间不足：提交渲染前拒绝新任务并提示清理项目；不在渲染中途静默删除数据。

## 4. 方案比较与选型

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| React + Ant Design + Express 5 + FFmpeg | 前后端统一 TypeScript；Express 生态成熟、概念少；Ant Design 覆盖管理后台组件；FFmpeg 可控 | 仍需自行实现模板和任务状态 | **首版采用** |
| React + NestJS + FFmpeg | 模块/依赖注入规范完整 | 对当前单体 MVP 装饰器和抽象偏多，不符合简洁目标 | 项目复杂化后再评估 |
| React + Express + Remotion/HTML 渲染 | 前后端统一 TypeScript，画面组件化 | Chromium 渲染资源占用高；音视频细节仍依赖 FFmpeg | 仅作为后续模板引擎实验 |
| 集成 MoneyPrinterTurbo/Pixelle/OpenMontage 等整套系统 | 功能丰富、起步看似快 | 依赖复杂、范围远超图文视频、难以保持单 Key 和无数据库 | 仅参考流程，不直接作为核心依赖 |

### 4.1 `GitHubStar.md` 开源优先与最小功能单元策略

原则：先把需求拆成“图片规范化、字幕编辑、分镜规则、转场渲染、BGM 混音、任务编排”等最小单元，再从 `GitHubStar.md` 选择单一组件或可移植模块；禁止为了一个小功能整体引入大型项目。每个候选必须先完成许可证、依赖/供应链、安全、维护活跃度、包体积和接口稳定性审计，审计不通过则使用更小的成熟开源库或本地最小实现。

| 最小功能单元 | 首选来源 | v0.1 使用方式 | 整体项目处理 |
|---|---|---|---|
| 视频编码、缩放、转场、字幕烧录、音频混合 | FFmpeg | **直接采用命令行能力**，封装安全参数适配器 | 不另引入整套剪辑系统 |
| 图片方向、色彩和尺寸规范化 | Sharp（libvips） | **直接采用库能力** | 仅处理上传图片，不扩展 AI 修图 |
| 字幕编辑 UI | `x007xyz/flycut-caption` | 审计许可证、React和样式依赖；只在不引入第二套通用UI库时复用无样式核心/数据转换层，外壳统一用Ant Design | 不接入其AI语音识别或完整应用；不兼容则保留Ant Design表单编辑器 |
| 图文视频流程与规则分镜 | `Gintma/videoflow`、MoneyPrinterTurbo、Pixelle-Video | 对其可独立算法/模板做审计和对照测试；只移植与当前领域模型匹配的小模块，并保留来源声明 | 不运行原项目服务，不复制账号、数据库、Agent 等无关部分 |
| 声明式画面模板 | `heygen-com/hyperframes` | 作为模板表达候选做隔离 spike；仅在能替代自研模板且不引入 Chromium 重负担时采用最小包 | v0.1 仍以 FFmpeg 为唯一渲染器 |
| 字幕格式处理 | Subtitle Edit 的格式行为 | 参考兼容性测试用例；首版实际输出使用轻量 ASS 生成器 | 不引入 C# 桌面程序 |
| 配音 | VibeVoice/VoxCPM | 不进入 v0.1；需要旁白时再按独立功能审计 | 不因未来需求预装模型 |

项目必须维护 `docs/oss-adoption.md` 和 `THIRD_PARTY_NOTICES.md`，逐项记录“采用/拒绝”、精确版本或 commit、复用文件/模块、许可证、修改内容、风险及替代方案。Star 数只用于发现候选，不作为采用依据。MiniMax 不参与本地可实现的常规功能，只在用户主动开启特殊需求时辅助优化提示词或生成字幕/特效。

## 5. 产品界面设计

### 5.1 页面

1. **项目列表 `/`**
   - 创建项目入口。
   - 项目卡片展示封面、标题、更新时间、最新渲染状态。
   - 支持打开和删除；删除需二次确认。
2. **项目编辑 `/projects/:projectId`**
   - 步骤条：素材 → 风格与方案 → 生成 → 审核交付。
   - 素材区：拖拽上传、缩略图、排序、删除、逐图文案。
   - 设置区：使用 Ant Design `Form`、`Card` 和两个独立 `Switch`：“M3 优化提示词”和“H3 生成字幕/特效”；开关显示“已启用/未启用”，默认未选中，点击即可选中或取消。
   - 提示词提案区：使用 Ant Design `Modal`、`Flex`、`Card`、`Typography` 并排展示原文、优化文、模型名和“接受 / 编辑后接受 / 拒绝”；关闭弹窗、切换步骤或超时均不等于接受。
   - 方案区：使用 `Form`、`List`/`Table` 按镜头编辑字幕和时长，展示转场/动效标签及来源；只有用户确认后的分镜可提交。
   - 生成区：使用 `Progress`、`Alert` 和 `Button` 展示任务阶段、警告和取消操作。
3. **结果页 `/projects/:projectId/revisions/:revisionId`**
   - 9:16 播放器、错误/警告、下载、再次修改、开启/关闭分享。
4. **分享页 `/share/:token`**
   - 只读播放器和下载按钮，不暴露服务器文件路径及编辑接口。
5. **系统设置 `/settings`**
   - 顶部/侧边 `Menu` 提供“系统设置”，仅受信任管理员可进入。
   - “MiniMax 设置”使用 Ant Design `Form`：`Input.Password` 填写/替换 API Key，普通 `Input` 设置 API Base、提示词模型和字幕/特效模型；默认模型分别为 `MiniMax-M3`、`MiniMax-H3`。
   - `GET` 只返回是否已配置及掩码（例如 `••••1234`），绝不返回完整 Key；API Key 输入框初始为空，留空保存表示保持原值，另设二次确认的“清除 Key”按钮。
   - 提供“保存设置”和“测试连接”按钮。保存成功后 Key 只存在服务器配置文件；测试结果分别显示 M3/H3 可用性，不自动开启项目开关。
   - `Input.Password` 设置 `autoComplete="new-password"`，前端不把 Key 写入 URL、日志、localStorage、sessionStorage、React Query 缓存或错误上报。
6. **能力提示区**
   - 分别显示“M3 提示词优化”和“H3 字幕/特效”的可用/未配置/最近降级状态；可用不代表默认启用。
   - 页面明确说明 MiniMax 是特殊需求辅助功能，两个 `Switch` 未选中时不会向 MiniMax 发送任何内容。

### 5.2 交互原则

- 首屏直接说明三步：“上传图文 → 选择风格 → 生成视频”。
- 用户不能在没有图片时提交方案或渲染。
- 上传、提示词提案、生成方案、确认分镜、渲染是独立动作，避免一次失败后全部重来。
- 任何 M3 提案必须由明确的接受操作改变状态；默认、关闭、离开页面、超时和 API 成功都不能推定用户同意。
- M3 与 H3 开关互不联动：可只优化提示词、只生成字幕/特效、两者都用或都不用。改变有效提示词、H3 开关或风格后，现有 storyboard 标记为 stale，必须重新生成并确认后才能渲染。
- 状态不只用颜色表达，始终配文字和图标。
- 移动端可完成上传和查看；复杂排序在桌面端体验优先。

## 6. 系统架构

```text
Browser
  │ HTTP/JSON + multipart + video range requests
  ▼
React + Ant Design SPA ───► Express 5 API（TypeScript）
                              ├─ ProjectService
                              ├─ SystemSettingsService ─► data/system/settings.json
                              ├─ UploadService ─────────► Sharp
                              ├─ PromptService ─────────► MiniMaxProvider(M3，可选)
                              │      └─ ProposalConsentGate（必须显式接受）
                              ├─ StoryboardService ─────► RulePlanner（默认）
                              │      └──────────────────► MiniMaxProvider(H3，可选字幕/特效)
                              ├─ RenderQueue（单消费者）
                              │     └─ RenderPipeline ──► FFmpeg + ASS
                              ├─ ShareService
                              └─ FileProjectRepository
                                      └─ ./data/projects/<project_id>/
```

### 6.1 组件职责

| 组件 | 单一职责 | 主要依赖 |
|---|---|---|
| `FileProjectRepository` | 原子读写项目 manifest，列出和删除项目 | Node.js `fs/promises` |
| `SystemSettingsRepository` | 原子读写 `data/system/settings.json`，设置 0600 权限并屏蔽 Key | Node.js 文件系统 |
| `SystemSettingsService` | 提供不可序列化的内部凭据快照与公开脱敏DTO；校验、保存、清除和测试配置 | Zod、Repository |
| `InternalMiniMaxCredentials` | 仅供 MiniMaxProvider 在服务器进程内读取完整Key，禁止进入响应/日志/错误 | 私有服务类型 |
| `SafeMiniMaxHttpClient` | 解析并固定公网A/AAAA地址，禁用重定向，防DNS rebinding/SSRF | undici Agent、node:dns、IP分类库 |
| `PublicMiniMaxSettings` | 仅含 configured、mask、base、模型和version，供API/capabilities使用 | 可序列化DTO |
| `UploadService` | 流式接收、校验、规范化和保存图片 | Busboy、file-type、Sharp |
| `RulePlanner` | 默认、不依赖 AI 地生成确定性分镜、字幕、特效和 BGM 分类 | 风格模板、经审计的开源最小模块 |
| `MiniMaxProvider` | 共用一个 Key，按用途调用可替换的 M3/H3 模型并验证结构化响应 | HTTP 客户端 |
| `PromptService` | 让 M3 生成候选提示词，保存 proposed/accepted/rejected 状态 | Provider、Repository |
| `ProposalConsentGate` | 阻止未显式接受的 M3 文本进入 storyboard/render_input | PromptService |
| `StoryboardService` | 默认使用 RulePlanner；仅在开关开启时用 H3 替换字幕/特效字段 | Provider、Planner |
| `RenderPipeline` | 将已确认方案渲染为 MP4 | FFmpeg、Sharp、ASS |
| `RenderQueue` | 排队、取消、恢复和串行执行任务 | Node.js Promise/AbortController、Repository |
| `ShareService` | 生成/撤销分享令牌并解析只读资源 | secrets、Repository |
| React API client | 封装 HTTP、错误和轮询 | fetch、TanStack Query |
| UI shell | 统一导航、表单、反馈、主题和响应式布局 | Ant Design `ConfigProvider`、`App`、`Layout`、`Menu` |
| Editor state | 管理页面草稿，不复制后端持久化职责 | TanStack Query/React 本地状态 |

### 6.2 目录结构

```text
video-developer/
├── package.json                    # npm workspaces + root scripts
├── package-lock.json
├── tsconfig.base.json
├── README.md
├── THIRD_PARTY_NOTICES.md
├── docs/oss-adoption.md
├── .gitignore
├── docker-compose.yml
├── data/
│   ├── .gitkeep
│   └── system/settings.json        # 运行时生成，0600，不提交 Git
├── assets/
│   ├── fonts/NotoSansSC-Regular.otf
│   └── music/
│       ├── LICENSES.md
│       ├── humorous/
│       ├── romantic/
│       └── tech/
├── frontend/
│   ├── package.json
│   ├── vite.config.ts
│   ├── src/
│   │   ├── app/router.tsx
│   │   ├── app/theme.ts
│   │   ├── app/AppShell.tsx
│   │   ├── api/client.ts
│   │   ├── api/types.ts
│   │   ├── components/
│   │   ├── features/projects/
│   │   ├── features/editor/
│   │   ├── features/render/
│   │   ├── features/settings/
│   │   ├── features/share/
│   │   └── styles/
│   └── tests/
├── backend/
│   ├── package.json
│   ├── tsconfig.json
│   ├── src/
│   │   ├── server.ts
│   │   ├── app.ts
│   │   ├── config.ts
│   │   ├── api/
│   │   ├── domain/schemas.ts
│   │   ├── repositories/file-projects.ts
│   │   ├── repositories/system-settings.ts
│   │   ├── services/uploads.ts
│   │   ├── services/system-settings.ts
│   │   ├── services/prompts.ts
│   │   ├── services/storyboards.ts
│   │   ├── services/shares.ts
│   │   ├── integrations/minimax.ts
│   │   ├── integrations/oss/
│   │   ├── render/plan.ts
│   │   ├── render/subtitles.ts
│   │   ├── render/pipeline.ts
│   │   └── jobs/queue.ts
│   └── tests/
└── scripts/
    ├── check-ffmpeg.ts
    └── smoke-test.ts
```

## 7. 数据设计（文件系统）

### 7.1 磁盘布局

#### 7.1.1 系统设置文件

`data/system/settings.json` 是网站运行时配置，不使用数据库和 `MINIMAX_API_KEY` 环境变量：

```json
{
  "schema_version": 1,
  "version": 3,
  "minimax": {
    "api_key": "用户在网页填写的完整Key",
    "api_base": "https://api.minimax.io/v1",
    "prompt_model": "MiniMax-M3",
    "content_model": "MiniMax-H3"
  },
  "updated_at": "ISO-8601 UTC"
}
```

- 首次启动若文件不存在，创建不含 Key 的默认文件；父目录权限 0700、文件权限 0600，容器内由非 root 服务用户拥有。
- Key 按用户要求保存在网站配置文件中，因此属于磁盘明文敏感信息。系统通过文件权限、HTTPS、Basic Auth、非 Web 根目录、备份访问控制和日志脱敏降低风险；没有另一个独立密钥时不伪装成“已加密存储”。
- 写入使用 Zod 校验、进程内设置锁、“同目录临时文件 + `fsync` + 原子替换”；`version` 做乐观并发控制，防止两个设置页面互相覆盖。
- `SystemSettingsService.getInternalCredentials()` 返回 `InternalMiniMaxCredentials`，只允许 MiniMaxProvider 调用，完整Key字段不可枚举、不可JSON序列化且禁止传给logger。`getPublicSettings()` 返回独立构造的 `PublicMiniMaxSettings`，供GET/capabilities/审计使用；两种类型不得互相强制转换或复用。
- `GET` 响应只包含 `configured`、固定掩码、规范化API Base、两个模型名和version；任何接口、日志、错误、备份清单和capabilities都不得返回完整Key。
- `PUT` 中 Key 为空或缺省表示保持原值；提供新 Key 表示替换。Key 去除首尾空白后限制为 16–512 字符。API Base 必须是 HTTPS、端口只能省略或为443、主机属于代码内MiniMax官方allowlist、路径匹配预定义前缀，并且必须拒绝URL username/password、query、fragment、IP、localhost、私网DNS和跨主机重定向；持久化前执行URL规范化，避免测试连接成为SSRF或凭据回显通道。模型名只允许1–128位字母、数字、点、下划线和连字符。
- 保存和每次连接前都用 `dns.promises.lookup(host,{all:true,verbatim:true})` 解析全部A/AAAA；拒绝任何非全球单播地址，包括unspecified、loopback、RFC1918、CGNAT、link-local、ULA、multicast、documentation/reserved及IPv4-mapped私网地址。使用undici自定义Agent/lookup把本次请求固定到已验证地址，TLS SNI/Host仍使用官方域名，防止校验后再次解析造成DNS rebinding。
- HTTP客户端设置 `redirect:'manual'` 并拒绝所有3xx，不跟随重定向；连接/读取超时和响应体上限固定。DNS在保存时通过但调用时变私网必须拒绝并返回 `MINIMAX_CONNECTION_FAILED`。
- `DELETE /minimax-key` 二次确认后清除。测试连接只在服务器内读取 Key 调用供应商，不把 Key传给浏览器。
- 备份默认排除该文件；若管理员明确选择包含系统设置，备份必须标记含密钥并限制为 0600。

#### 7.1.2 项目数据

```text
data/projects/<project_id>/
├── manifest.json
├── uploads/<asset_id>/original.<ext>
├── uploads/<asset_id>/normalized.jpg
├── prompts/<proposal_id>.json         # M3 提案及 proposed/accepted/rejected 审计状态
├── storyboard/draft.json             # 当前可编辑草稿，含 version/updated_at
├── revisions/<revision_id>/storyboard.json  # 供审核展示的分镜副本
├── revisions/<revision_id>/render_input.json # 渲染唯一权威输入：分镜+设置+不可变素材摘要
├── revisions/<revision_id>/subtitles.ass
├── revisions/<revision_id>/render.log
├── revisions/<revision_id>/output.mp4
└── tmp/                         # 成功或失败后清理
```

- `project_id`、`asset_id`、`revision_id` 使用 UUIDv4，由服务端生成，不接受客户端路径。
- `storyboard/draft.json` 是唯一当前草稿；`storyboard:generate` 与 `PUT /storyboard` 都必须提交 `expected_version`（项目尚无草稿时为 `null`），在同一项目锁内做乐观并发检查，成功后版本加 1、计算 storyboard digest，并清空 `confirmed_version/confirmed_input_digest/confirmed_at`。版本不匹配返回 `409 STORYBOARD_VERSION_CONFLICT`。
- “待渲染确认载荷”由实际有效提示词文本/来源/proposal ID、完整 storyboard、风格及字幕/BGM/动效/H3开关、按顺序排列的素材 ID/文案/normalized SHA-256 组成；标题、分享状态等不影响视频的字段不纳入。canonical input digest 使用该载荷的 UTF-8、键排序、数组保序、无多余空白 JSON 计算 SHA-256。
- `POST /storyboard:confirm` 必须提交当前 storyboard version 和 canonical input digest；后端在项目锁内从当前项目状态重新构造载荷并匹配后，才保存 `confirmed_version`、`confirmed_input_digest` 和时间。任何会改变确认载荷的操作——修改/接受/恢复提示词，风格、字幕/BGM/动效/H3开关变化，上传/删除/排序素材，修改素材文案，重新生成或编辑分镜——都必须由服务端原子清空确认；只改标题或分享状态不清空。
- `POST /renders` 必须重新构造当前确认载荷，并校验 storyboard version 和 input digest 与服务端确认记录完全一致，否则返回 `409 STORYBOARD_NOT_CONFIRMED` 或 `STORYBOARD_CONFIRMATION_STALE`；该门禁适用于规则与 H3 分镜，不能由客户端绕过。
- 通过确认门禁后，`POST /renders` 在同一项目锁内读取已确认草稿，拒绝任何未 accepted 的 M3 提案引用，创建 revision，并原子写入 `render_input.json`：内嵌实际采用的提示词文本/来源/proposal ID/模型摘要、storyboard（含字幕/特效的 `rule` 或 `minimax_h3` 来源）、完整 `ProjectSettings`、每个被引用图片的 `asset_id`/固定 `normalized_relpath`/SHA-256/宽高/展示文案，以及冻结的 `font` 和 `music`。`font` 包含相对路径、SHA-256 和许可证条目摘要；启用 BGM 时先按 9.2 规则选定唯一曲目，`music` 包含 category、相对路径、SHA-256 和许可证条目摘要，禁用时为 `null`。所有字段均计入 `render_input_digest`；同时写 `storyboard.json` 供审核。`render_input.json` 是 worker 和 retry 的唯一权威输入，后续编辑草稿、设置、素材元数据、字体或曲库目录不改变选择。
- normalized 素材文件创建后不可原地覆盖；从项目草稿删除素材时，若任一 revision 的 `render_input.json` 仍引用它则保留物理文件，只从当前 assets 列表移除，项目整体删除时才回收。worker 读取前验证 SHA-256，不一致则以 `ASSET_INTEGRITY_ERROR` 失败。
- JSON 写入采用“同目录临时文件 + `fsync` + 原子替换”，避免进程中断产生半个文件。
- 每个项目使用进程内异步锁串行化 manifest、草稿、进度、取消和分享更新；manifest 的 `version` 作为 CAS 版本，防止并发更新丢失。
- `manifest.json` 是项目元数据和 revision 状态的唯一事实来源；派生文件可重新生成。
- 服务端启动时扫描 manifests 恢复索引，不维护额外全局数据库文件。

### 7.2 核心模型

```json
{
  "schema_version": 1,
  "version": 3,
  "id": "uuid",
  "title": "旅行回忆",
  "created_at": "ISO-8601 UTC",
  "updated_at": "ISO-8601 UTC",
  "assets": [
    {"id": "uuid", "filename": "a.jpg", "mime_type": "image/jpeg", "width": 1440, "height": 1920, "caption": "第一天到达海边", "order": 0}
  ],
  "settings": {"style": "romantic", "subtitles_enabled": true, "music_enabled": true, "effects_enabled": true, "minimax_prompt_optimization_enabled": false, "minimax_caption_effects_enabled": false},
  "prompt": {"original_text": "记录一次温暖的海边旅行", "active_source": "original", "accepted_proposal_id": null},
  "latest_revision_id": "uuid",
  "draft_storyboard": {"version": 2, "updated_at": "ISO-8601 UTC", "path": "storyboard/draft.json", "digest": "sha256:<hex>", "confirmed_version": 2, "confirmed_input_digest": "sha256:<hex>", "confirmed_at": "ISO-8601 UTC"},
  "share": {"enabled": true, "revision_id": "uuid", "token_digest": "sha256:<hex>", "created_at": "ISO-8601 UTC"},
  "revisions": [
    {"id": "uuid", "status": "completed", "progress": 100, "stage": "completed", "render_input_digest": "sha256:<hex>", "warning_codes": [], "error_code": null, "created_at": "ISO-8601 UTC"}
  ]
}
```

`prompts/<proposal_id>.json`：

```json
{
  "id": "uuid",
  "model": "MiniMax-M3",
  "original_text": "记录一次温暖的海边旅行",
  "proposed_text": "以温暖细腻的叙事记录海边旅行……",
  "proposed_digest": "sha256:<hex>",
  "status": "proposed",
  "accepted_text": null,
  "created_at": "ISO-8601 UTC",
  "decided_at": null
}
```

- 状态只允许 `proposed → accepted` 或 `proposed → rejected`，不可逆；再次优化创建新 proposal。
- `accepted` 必须来自独立确认 API，并提交 `proposed_digest` 防止确认错版本；“编辑后接受”把用户最终确认文本写入 `accepted_text`。
- `active_source=optimized` 只在 proposal 已 accepted 时合法；生成 storyboard 和 `render_input.json` 必须保存实际提示词文本、来源、proposal ID、模型名和摘要。未接受提案只能留作预览审计，不能被系统自动选用。
- 用户修改原始提示词后，后端必须清除旧 `accepted_proposal_id` 并把 `active_source` 切回 `original`；旧 proposal 保留审计但不能复用。关闭 M3 开关只阻止新请求，不撤销用户已经接受并拥有的文本；用户可显式“恢复原文”。

`storyboard.json`：

```json
{
  "version": 1,
  "duration_seconds": 18.0,
  "music_category": "acoustic/warm",
  "scenes": [
    {"asset_id": "uuid", "start": 0.0, "duration": 4.5, "subtitle": "第一天，我们抵达海边", "motion": "zoom_in", "transition": "fade"}
  ]
}
```

### 7.3 状态机

`Revision.status` 只取终态/调度态，`Revision.stage` 只表示运行中的进度阶段：

| 当前 status | 允许操作/事件 | 下一 status | stage 规则 |
|---|---|---|---|
| `queued` | worker 领取 | `running` | `planning` |
| `queued` | 用户取消 | `cancelled` | `cancelled` |
| `running` | 阶段推进 | `running` | `planning → preparing → rendering → finalizing` |
| `running` | 用户取消 | `cancelling` | 保持当前阶段 |
| `cancelling` | 子进程退出/重启恢复 | `cancelled` | `cancelled` |
| `running` | probe 成功 | `completed` | `completed` |
| `running` | 任一阶段异常 | `failed` | 失败时所在阶段 |

- `draft` 是项目编辑状态，不是 revision status；revision 创建后初始即为 `queued`。
- `completed`、`failed`、`cancelled` 为不可逆终态。重试永远创建新 revision，旧 revision 不改回 `queued`。
- 服务重启时：遗留 `queued` 保持不变；`running` 回到 `queued` 并清零瞬时进度；`cancelling` 变为 `cancelled`；终态不变。
- 状态只能由后端按表迁移。每次迁移都在项目锁内原子更新 manifest；前端不得直接提交 `status`、`stage`、输出路径或分享令牌。

### 7.4 数据保留

- 首版默认项目永久保留，直到用户主动删除。
- 删除项目时先将目录原子移动到 `data/trash/`，再异步删除，以减少半删除状态。
- 提供磁盘可用空间指标和管理员清理命令；自动过期策略待引入用户体系后再决定。

## 8. API 设计

统一前缀 `/api/v1`，成功返回 JSON；错误格式固定为：

```json
{"error":{"code":"INVALID_IMAGE","message":"图片格式不受支持","details":{}}}
```

| 方法与路径 | 用途 | 关键响应 |
|---|---|---|
| `GET /health` | 存活检查 | `{"status":"ok"}` |
| `GET /api/v1/capabilities` | FFmpeg、开源组件、M3提示词和H3字幕/特效能力 | 读取系统设置后返回分项能力/模型，不返回密钥；可用不代表启用 |
| `GET /api/v1/system-settings/minimax` | 读取系统设置展示值 | configured、固定掩码、base、两个模型、version；无完整Key |
| `PUT /api/v1/system-settings/minimax` | 管理员保存/替换 Key、base 和模型 | 原子写入 `data/system/settings.json`；返回脱敏设置 |
| `DELETE /api/v1/system-settings/minimax-key` | 二次确认后清除 Key | `204`，项目AI开关随后显示不可用 |
| `POST /api/v1/system-settings/minimax:test` | 用服务器文件中的 Key 测试 M3/H3 | 两个模型的脱敏结果和耗时 |
| `GET /api/v1/projects` | 项目列表 | 项目摘要数组 |
| `POST /api/v1/projects` | 创建项目 | `201` + project |
| `GET /api/v1/projects/{id}` | 项目详情 | project |
| `PATCH /api/v1/projects/{id}` | 改标题、设置、素材文案/顺序 | 更新后的 project |
| `DELETE /api/v1/projects/{id}` | 删除项目 | `204` |
| `POST /api/v1/projects/{id}/assets` | multipart 上传图片 | `201` + asset |
| `DELETE /api/v1/projects/{id}/assets/{assetId}` | 删除素材 | `204` |
| `POST /api/v1/projects/{id}/prompt-proposals` | 用户开启功能后调用 M3 生成候选，不改变有效提示词 | `201` + proposed proposal |
| `POST /api/v1/projects/{id}/prompt-proposals/{pid}:accept` | 提交 proposal digest 和最终确认文本，显式接受 | accepted proposal + active prompt |
| `POST /api/v1/projects/{id}/prompt-proposals/{pid}:reject` | 明确拒绝提案，继续使用原提示词 | rejected proposal |
| `POST /api/v1/projects/{id}/storyboard:generate` | 携带 `expected_version`；默认规则生成，H3开关开启时只替换字幕/特效 | 新 version + warnings + content source；冲突返回 409 |
| `PUT /api/v1/projects/{id}/storyboard` | 携带当前 version 保存草稿并清除旧确认 | 新 version/digest；版本冲突返回 409 |
| `POST /api/v1/projects/{id}/storyboard:confirm` | 提交当前 version/input digest，服务端从提示词+设置+有序素材+分镜复算后确认 | confirmed version/input digest/time |
| `POST /api/v1/projects/{id}/renders` | 校验当前分镜已确认，原子冻结草稿、创建 revision 并排队 | `202` + revision；未确认/已过期返回 409 |
| `GET /api/v1/projects/{id}/revisions/{rid}` | 查询进度或结果 | revision |
| `POST /api/v1/projects/{id}/revisions/{rid}:cancel` | 取消任务 | revision |
| `POST /api/v1/projects/{id}/revisions/{rid}:retry` | 在锁内逐字节复制源 `render_input.json` 并创建新任务，不读取当前项目草稿/设置 | `202` + new revision |
| `GET /api/v1/projects/{id}/revisions/{rid}/video` | 播放/下载，支持 Range | `video/mp4` |
| `POST /api/v1/projects/{id}/revisions/{rid}/share` | 为指定成功 revision 开启/轮换分享 | 一次性返回 URL |
| `DELETE /api/v1/projects/{id}/share` | 撤销项目当前分享 | `204` |
| `GET /api/v1/share/{token}` | 分享页元数据 | 令牌绑定 revision 的只读标题和视频 URL |
| `GET /api/v1/share/{token}/video` | 分享令牌所绑定的视频，支持 Range | `video/mp4` |

一个项目同一时刻只有一个活动分享链接，且链接永远固定到创建时指定的 `completed` revision；生成新 revision 不会改变旧链接内容。再次分享同一或其他 revision 会轮换 token 并使旧链接立即失效。token 由 32 字节随机值构成，manifest 保存 `SHA-256(token)`；随机 token 自身具有 256 位熵，无需另存盐，比较使用常量时间函数。

关键错误码：`PROJECT_NOT_FOUND`、`ASSET_NOT_FOUND`、`ASSET_INTEGRITY_ERROR`、`INVALID_IMAGE`、`FILE_TOO_LARGE`、`INVALID_SYSTEM_SETTINGS`、`SYSTEM_SETTINGS_VERSION_CONFLICT`、`MINIMAX_KEY_NOT_CONFIGURED`、`MINIMAX_CONNECTION_FAILED`、`INVALID_STORYBOARD`、`STORYBOARD_VERSION_CONFLICT`、`STORYBOARD_NOT_CONFIRMED`、`STORYBOARD_CONFIRMATION_STALE`、`PROMPT_PROPOSAL_NOT_ACCEPTED`、`PROMPT_PROPOSAL_VERSION_CONFLICT`、`MINIMAX_CAPABILITY_UNAVAILABLE`、`RENDER_ALREADY_ACTIVE`、`RENDER_FAILED`、`INSUFFICIENT_STORAGE`、`SHARE_DISABLED`。

## 9. 分镜、字幕、特效与 BGM 规则

### 9.1 确定性规则方案

1. 每张图片至少一个镜头，默认时长 `clamp(文字字符数 / 6, 2.5, 6.0)` 秒。
2. 总时长超过 60 秒时按比例缩短，但每镜不低于 2 秒；仍超限则拒绝并提示减少素材。
3. 总文案按标点切句，未填写逐图文案时按图片顺序均匀分配。
4. 相邻镜头避免使用相同 motion；首尾使用淡入/淡出。
5. 字幕每行最多约 18 个中文字符，最多两行；以渲染后像素宽度为最终准则。

### 9.2 风格模板

| 风格 | 色彩/字幕 | 动效与转场 | BGM 分类 |
|---|---|---|---|
| 轻松幽默 | 暖黄强调、较粗字幕 | 快速轻缩放、滑动；避免频闪 | upbeat/light |
| 浪漫温馨 | 暖粉/米白、柔和阴影 | 慢推近、交叉淡化 | acoustic/warm |
| 科技感 | 青蓝、细描边 | 平移、轻微故障切换；控制强度 | electronic/minimal |

时间线契约：`Scene.duration` 表示该镜头从开始到结束的完整屏幕时间；除最后一镜外，结尾与下一镜重叠转场。轻松幽默、浪漫温馨、科技感的默认转场时长分别为 0.4、0.8、0.5 秒，且不得超过相邻任一镜头时长的 25%。第 1 镜只做 0.4 秒画面淡入，最后一镜 `transition=none` 并做 0.6 秒淡出。镜头开始偏移按 `next_start = current_start + current_duration - transition_duration` 计算，`Storyboard.duration_seconds = 最后一镜 start + duration`；规则规划器以该公式反算并保证最终 15–60 秒，RenderPlan 单测逐帧验证偏移，ffprobe 允许误差不超过 0.5 秒。

BGM 契约：领域分类固定映射为 `upbeat/light → assets/music/humorous`、`acoustic/warm → assets/music/romantic`、`electronic/minimal → assets/music/tech`。只纳入 `LICENSES.md` 中记录且文件存在的曲目；按规范化相对路径排序，再用 `SHA-256(project_id + style)` 对曲目数取模。该选择仅在 `POST /renders` 创建 revision 时执行一次并冻结到 `render_input.json`；worker 不重新枚举曲库。新增或重排曲库不影响已创建 revision，冻结文件丢失或摘要不一致时返回 `ASSET_INTEGRITY_ERROR`。

### 9.3 MiniMax 辅助能力边界

#### 9.3.1 M3 提示词优化（默认关闭）

- 默认模型 `MiniMax-M3`，管理员可在“系统设置”中修改提示词模型；实际模型名必须展示在提案预览和审计文件中。
- 只有用户开启项目开关并点击“优化提示词”才发起请求；输入仅为当前提示词、风格和必要的文字上下文，不上传图片二进制。
- M3 只返回候选优化文本和简短修改说明，不生成字幕、特效、BGM、路径或 FFmpeg 参数。
- API 成功只产生 `proposed` 提案。页面必须展示原文/提案差异；只有用户点击“接受”或“编辑后接受”，后端确认 proposal digest 后才更新 active prompt。
- 拒绝、关闭弹窗、离开页面、网络超时或关闭开关均保持原提示词；系统不得自动接受、默认勾选接受或用提案覆盖原文。

#### 9.3.2 H3 字幕/特效内容（默认关闭）

- 默认模型 `MiniMax-H3`，管理员可在“系统设置”中修改字幕/特效模型；与 M3 开关完全独立。
- 只有用户开启“H3 字幕/特效”并生成方案时调用；输入采用当前有效提示词（原文或已接受的 M3 文本）、素材顺序和风格。
- H3 输出只允许填写每镜字幕及白名单 `motion`/`transition` 建议，不改变图片、时长上限、BGM 选择、文件路径或命令。
- H3 结果必须经过 JSON Schema、白名单和时间线校验，再进入 StoryboardEditor 标记来源供用户预览/编辑；用户确认分镜后才能渲染。

#### 9.3.3 共用配置、降级与隐私

- MiniMax 配置全部来自 `data/system/settings.json`，不读取任何 `MINIMAX_*` 环境变量。MiniMaxProvider每次调用 `getInternalCredentials()` 获取仅服务器可见的完整Key和模型；GET、capabilities、日志与审计只能调用 `getPublicSettings()` 获取独立脱敏DTO。管理员保存后内存快照原子更新，无需重启。
- `/capabilities` 分别报告 `prompt_optimization` 和 `caption_effects` 的可用状态及当前模型；Key 未配置时禁用两个项目开关，不支持某模型时只禁用对应开关，不静默换模型。
- 两项调用均使用固定 system prompt、严格 JSON Schema、15 秒超时和最多 1 次重试；授权失败不重试。
- M3 失败时保留原提示词；H3 失败时使用 RulePlanner 字幕/特效并返回非阻断 warning。两个功能关闭时不得向 MiniMax 发送请求。
- 日志不记录 Key、完整提示词、完整字幕或提案正文；只记录模型、proposal/revision ID、耗时、结果状态和脱敏错误码。
- Task 7 依据实施时官方文档锁定请求 URL、认证头和 schema，以 mock 契约测试为代码完成门槛；真实连接测试由管理员在系统设置页面主动触发，不阻塞默认无 AI 流程。

### 9.4 FFmpeg 渲染步骤

1. 用 Sharp 自动纠正 EXIF 方向并转为 sRGB JPEG。
2. 生成 1080×1920 背景：原图等比填充并模糊；前景图等比包含，避免主体裁切。
3. 每个镜头用 `zoompan`/`scale`/`crop` 生成中间无音频片段。
4. 按 storyboard 转场合并镜头；转场不可用时降级为硬切，不执行任意外部脚本。
5. 生成 ASS 字幕，使用项目内置中文字体并限制在平台安全区。
6. 使用 `render_input.json` 中冻结的 BGM 和字体，验证文件及许可证条目摘要；BGM 裁剪/循环、响度归一化并设置 1 秒淡入淡出，禁止运行时重新选曲。
7. 合成音视频和字幕，输出 H.264/AAC MP4；运行 `ffprobe` 验证编码、尺寸、时长和可播放性。
8. 原子移动为 `output.mp4`，清理 revision 临时文件，状态变为 `completed`。

## 10. 非功能需求

### 10.1 性能与容量

- 目标环境：4 vCPU、8 GB RAM、可用磁盘不少于 20 GB。
- 30 秒、10 张 1080p 图片的视频，在目标服务器上 3 分钟内完成作为首版性能目标，不作为跨硬件绝对承诺。
- 上传使用流式写盘，不把整个文件读入内存。
- 队列默认并发为 1；可通过服务端环境变量调到 2，但上线前必须压测。
- 视频响应支持 HTTP Range，以便浏览器拖动播放。

### 10.2 安全与隐私

- 根据文件魔数和 Sharp 解码结果验证类型，不信任扩展名和客户端 MIME。
- 对像素总数设上限，防止解压炸弹；移除 EXIF 元数据。
- 文件名只用于展示，磁盘路径全部使用服务端 UUID；拒绝路径穿越和符号链接。
- FFmpeg 使用参数数组启动，不拼接 shell 字符串；AI 输出只能映射白名单枚举。
- 限制请求体、上传数量、文字长度和同时任务数。
- 生产环境限定 CORS 来源并使用 HTTPS；反向代理以 Basic Auth 保护编辑 SPA 和项目 API，仅 `/share/*`、`/api/v1/share/*` 允许匿名持 token 访问，另匿名 `/health` 只返回固定存活状态且不泄露版本、路径或能力。首版不允许把未保护的项目 API 暴露到公网。
- Basic Auth 凭据只以部署 secret/htpasswd 文件注入，不进入仓库、镜像、浏览器持久存储或项目文件；MiniMax Key 按用户要求只存 `data/system/settings.json`，该文件权限必须为 0600、禁止提交 Git和默认备份。前后端不记录凭据、Key、完整提示词或字幕正文。
- 系统设置路由与页面必须受 Basic Auth 保护并启用 CSRF/Origin 校验；生产只允许 HTTPS。GET/错误/capabilities 永不返回完整 Key，PUT 请求体不得进入访问日志或错误上报。
- MiniMax 两开关默认关闭；关闭时后端不得发起供应商请求。M3 proposal 必须以服务端状态和摘要校验显式同意，不能仅依赖前端按钮状态。
- 分享 token 使用 32 字节安全随机数，manifest 仅保存 SHA-256 摘要并以常量时间比较；撤销或轮换后立即失效。
- 明示用户必须拥有上传素材及音乐的使用权。

### 10.3 可观测性

- JSON 结构化日志字段：`request_id`、`project_id`、`revision_id`、`stage`、`duration_ms`、`error_code`。
- `/health` 只判断进程；`/api/v1/capabilities` 从系统设置的脱敏快照报告 FFmpeg、字体、曲库、已采用开源组件及 M3/H3 两项能力。
- 系统设置变更日志只记录操作者、时间、配置版本和“Key已设置/替换/清除”，不记录 Key、API Base 查询参数或请求体。
- 每个 revision 保存 FFmpeg stderr 到 `render.log`，API 只返回安全摘要。
- 记录上传数、队列长度、各阶段耗时、成功率和磁盘余量；首版先输出日志，后续接监控系统。

## 11. 测试与验收策略

### 11.1 测试层级

- 后端单元测试：Zod 模型校验、系统设置原子写入/权限/脱敏、路径安全、manifest、规则分镜、M3同意门禁、H3降级、状态机、ASS 转义。
- 后端集成测试：Vitest + Supertest 覆盖 Express 路由；用 2–3 张测试图片调用真实 FFmpeg 验证 MP4；MiniMax 使用 mock 验证系统设置读取、两个模型路由和零调用默认行为。
- API 测试：系统设置保存/替换/清除/测试连接、Key永不回显、创建、上传、提示词提案/决定、分镜来源、排队、取消、下载、分享与错误结构。
- 前端组件测试：Ant Design 菜单/表单/上传/两个 Switch、系统设置 Password 字段、M3提案预览确认、H3来源、进度和错误展示。
- E2E：默认关闭 MiniMax 完成全流程；另测 M3 接受/拒绝及 H3 开关的可选路径，再覆盖下载、修改、分享与撤销。
- 人工视觉验收：三种风格各一条样片，检查主体裁切、字幕安全区、转场、音量和移动端播放。

### 11.2 MVP 完成定义

以下条件全部满足才算首版完成：

1. 新用户可在一个页面流程中上传 1–20 张合法图片和文字，选择三种风格之一。
2. 前端以 Ant Design 为唯一主要 UI 库；M3/H3 使用两个可点击选中/取消的独立 `Switch`，默认未选中，键盘和屏幕阅读器可操作。
3. 管理员可在“系统设置”网页填写、替换、清除并测试一个 MiniMax API Key；Key 原子保存到 `data/system/settings.json` 且权限 0600，任何读取接口都不回显明文，也不使用 `MINIMAX_API_KEY` 环境变量。
4. MiniMax 两个项目开关互相独立；Key 未配置时仍可完整生成，开关关闭时测试确认零 MiniMax 请求。
5. 开启 M3 时，提案必须先显示原文/优化文；拒绝或未确认不得改变有效提示词，只有显式接受的 proposal 才能进入 storyboard 和 `render_input.json`。
6. 开启 H3 时只生成字幕和白名单特效，结果经过分镜预览；服务端确认门禁不可绕过，H3失败时降级为规则内容。
7. 管理员可在系统设置分别替换 `MiniMax-M3` 和 `MiniMax-H3` 模型名，实际模型记录在提案/分镜来源中。
8. 输出为 1080×1920 H.264/AAC MP4，含用户最终确认的字幕、动效、转场和已授权 BGM。
9. 页面持续显示真实任务状态，刷新页面后状态不丢失，服务重启可恢复未完成任务。
10. 用户可修改方案并生成新 revision，能预览和下载任一成功 revision。
11. 指定成功 revision 的分享链接可开启、访问、下载和撤销；生成新 revision 不改变旧链接内容，撤销/轮换后旧链接返回 404。
12. 非法文件、超限、磁盘不足、FFmpeg/MiniMax 失败均返回稳定错误码，无路径、Key、完整提示词或命令泄漏。
13. `docs/oss-adoption.md` 对每个 GitHubStar 候选记录最小功能、采用/拒绝理由、许可证与版本；实际引入代码均出现在 `THIRD_PARTY_NOTICES.md`，没有为单一功能整体引入大型项目。
14. 后端 Vitest/Supertest/API/FFmpeg 集成测试、前端组件测试和关键 Playwright E2E 全部通过。
15. README 可让新开发者在 10 分钟内启动 Express 与 React 开发环境并在 MiniMax 关闭状态生成示例视频。
16. 参考抖音样片由产品方人工确认；若差异仅为模板参数，通过调整风格模板完成，不引入新架构。

---

# 实施计划

> **For agentic workers:** 实施前使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`，逐任务执行；每个步骤使用 `- [ ]` 跟踪。

**目标：** 交付一个无数据库、可在单服务器运行的图文竖屏视频生成网站。  
**架构：** React + Ant Design SPA 调用 Express 5 TypeScript API；后端以 Zod 校验和文件仓库管理系统设置/项目状态，通过单消费者任务队列调用 FFmpeg；默认流程由经审计的开源最小组件和规则引擎完成。MiniMax-M3 仅生成需用户接受的提示词提案，MiniMax-H3 仅生成可选字幕/特效，Key 由网页系统设置保存。  
**技术栈：** Node.js 22、Express 5、TypeScript、Zod、React、Vite、Ant Design、TanStack Query、Sharp、FFmpeg/ffprobe、ASS、Vitest、Supertest、Testing Library、Playwright、Docker Compose。

## 12. 实施任务

### Task 1：初始化 npm 工作区、Express 和 Ant Design 基线

**文件：** 创建 `.gitignore`、`package.json`、`package-lock.json`、`tsconfig.base.json`、`eslint.config.js`、`README.md`、`THIRD_PARTY_NOTICES.md`、`docs/oss-adoption.md`、`frontend/package.json`、`frontend/vitest.config.ts`、`frontend/src/test/setup.ts`、`backend/package.json`、`backend/tsconfig.json`、`backend/vitest.config.ts`、`backend/src/app.ts`、`backend/src/server.ts`、`backend/tests/`、`data/.gitkeep`、`assets/fonts/LICENSES.md`、`assets/music/LICENSES.md` 及三个风格目录的 `.gitkeep`。

- [ ] 确认本机安装 Node.js 22、npm、FFmpeg 7 和 Git；运行 `node -v && npm -v && ffmpeg -version && ffprobe -version`，四项均成功。
- [ ] 执行 `git init`；`.gitignore` 忽略 `node_modules`、构建产物、缓存、`data/projects`、`data/trash` 和 `data/system/settings.json`，保留 `data/.gitkeep`；项目不创建 MiniMax Key 环境变量示例。
- [ ] 创建npm workspaces和共享TypeScript配置；所有依赖使用 `--save-exact`，提交唯一根lockfile。根开发依赖精确安装TypeScript、tsx、Vitest、ESLint及TypeScript插件、Prettier；后端安装Supertest及类型、Express类型；前端安装Testing Library、user-event、jest-dom和jsdom。
- [ ] 前端精确安装React、Vite、Ant Design、icons、TanStack Query；后端精确安装Express 5、Zod、Sharp、Busboy、file-type、undici及安全IP分类库。创建前后端Vitest配置、测试setup、ESLint配置和最小健康检查测试，禁止npm exec临时下载未锁定包。
- [ ] 前端在 `ConfigProvider` 中设置中文locale和主题token；后端创建最小 `app.ts`/`server.ts`，实现 `GET /health`。配置根脚本 `dev/build/typecheck/lint/test:backend/test:frontend`，每个命令只使用lockfile依赖。
- [ ] 创建 `assets/music/{humorous,romantic,tech}`、`assets/fonts` 和许可证记录模板；建立 OSS 审计模板与第三方通知文件。
- [ ] README写明准确命令；运行 `npm ls --all` 确认无缺失/外部临时依赖，CI使用 `npm ci` 可从lockfile离线复现已缓存安装。
- [ ] 运行 `npm run typecheck && npm run build && npm run test:backend -- --run && npm run test:frontend -- --run`，预期全部退出码 0。
- [ ] 提交：`chore: initialize express and ant design workspace`。

### Task 2：领域模型、服务配置与开源组件审计

**文件：** 创建 `backend/src/config.ts`、`backend/src/domain/schemas.ts`、`scripts/check-ffmpeg.ts`、`scripts/audit-oss.ts`；修改 `docs/oss-adoption.md`、`THIRD_PARTY_NOTICES.md`；测试 `backend/tests/config.test.ts`、`backend/tests/domain/schemas.test.ts`。

- [ ] 先写 Vitest，覆盖风格、图片数、文字长度、scene、状态迁移、两个项目 MiniMax 开关默认 `false`、系统设置默认模型和 proposal 状态机。
- [ ] 用 Zod 定义 `Style`、`ProjectSettings`、`SystemSettings`、`PromptProposal`、`Asset`、`Scene`、`Storyboard`、`Revision`、`Project`；日期统一 ISO-8601 UTC。
- [ ] `config.ts` 只配置数据目录、上传限制、磁盘下限、任务并发和 FFmpeg 路径；明确禁止读取任何 `MINIMAX_*` 环境变量。
- [ ] 实现 `scripts/check-ffmpeg.ts` 检查 ffmpeg/ffprobe/H.264/字体/曲库；用 `scripts/audit-oss.ts` 审计 FFmpeg、Sharp、Ant Design、flycut-caption、videoflow、MoneyPrinterTurbo、Pixelle-Video、hyperframes 和 Subtitle Edit。
- [ ] 对审计通过的候选只做隔离最小模块 spike，经 `integrations/oss/` 适配；实际代码同步第三方通知，大型项目不得整体启动。
- [ ] 运行 `npm exec -- tsx scripts/audit-oss.ts --check docs/oss-adoption.md THIRD_PARTY_NOTICES.md && npm run test:backend -- --run backend/tests/config.test.ts backend/tests/domain/schemas.test.ts`，预期审计完整、测试全通过。
- [ ] 提交：`feat: add zod schemas and audited OSS selections`。

### Task 3：项目仓库、系统设置文件与能力 API

**文件：** 创建 `backend/src/repositories/file-projects.ts`、`backend/src/repositories/system-settings.ts`、`backend/src/services/system-settings.ts`、`backend/src/integrations/safe-http-client.ts`、`backend/src/api/system-settings.ts`、`backend/src/api/capabilities.ts`；测试 `backend/tests/repositories/file-projects.test.ts`、`backend/tests/repositories/system-settings.test.ts`、`backend/tests/integrations/safe-http-client.test.ts`、`backend/tests/api/system-settings.test.ts`、`backend/tests/api/capabilities.test.ts`。

- [ ] 先写项目仓库测试：CRUD、列表、非UUID、损坏隔离、CAS冲突、并发更新不丢字段和中断不覆盖旧文件。
- [ ] 用 `fs/promises`、每资源 Promise mutex、临时文件、文件句柄 `sync()` 和同目录 `rename()` 实现项目/系统设置原子仓库。
- [ ] 首次启动创建 `data/system/settings.json` 默认值；断言目录0700、文件0600。设置文件默认模型为 M3/H3且无Key，不读取环境变量。
- [ ] 实现受保护的设置GET/PUT/DELETE/test路由和两种不可混用的返回类型：Provider专用 `InternalMiniMaxCredentials`（完整Key、不可JSON序列化/记录）与API专用 `PublicMiniMaxSettings`（仅configured/mask/base/models/version）。
- [ ] 对设置路由启用JSON体限制、Origin/CSRF和请求体日志屏蔽；API Base字符串校验拒绝userinfo/query/fragment、非443端口、IP/localhost、非allowlist host和非法path；错误堆栈不得含Key。
- [ ] 实现 `SafeMiniMaxHttpClient`：解析全部A/AAAA并拒绝所有非全球单播/IPv4-mapped私网地址；undici自定义lookup固定本次已验证IP且保留TLS SNI；`redirect:'manual'` 拒绝全部3xx；设置连接/读取超时和响应上限。
- [ ] DNS mock测试覆盖公网A/AAAA、RFC1918、CGNAT、loopback、link-local、ULA、multicast、reserved、IPv4-mapped地址、首次公网后第二次私网的rebinding和30x重定向；任一解析结果不安全都不得建立socket。
- [ ] 实现 capabilities，从脱敏设置快照返回FFmpeg、OSS、M3/H3配置/模型状态；保存或清除Key后无需重启即可生效。
- [ ] 运行 `npm run test:backend -- --run backend/tests/repositories backend/tests/integrations/safe-http-client.test.ts backend/tests/api/system-settings.test.ts backend/tests/api/capabilities.test.ts`，预期权限、原子写、并发、热更新、DTO隔离、URL/DNS/重定向拒绝和Key不回显全部通过。
- [ ] 提交：`feat: add file settings and capability APIs`。

### Task 4：实现项目 CRUD API

**文件：** 创建 `backend/src/api/projects.ts`、`backend/src/api/errors.ts`；修改 `backend/src/app.ts`；测试 `backend/tests/api/test_projects.ts`。

- [ ] 先写 API 测试，覆盖创建、列表、读取、更新、删除、404、非法标题、统一错误结构；修改风格、字幕/BGM/动效/H3开关、原始提示词、素材文案/顺序会清空 storyboard 确认，只改标题不清空。
- [ ] 使用 Express `Router` 实现 `/api/v1/projects`，所有 params/body/query 先经 Zod 校验；Supertest 响应不暴露服务器路径、token摘要和内部日志。
- [ ] `PATCH` 只接受标题、风格/字幕/BGM/动效设置、两个默认关闭的 MiniMax 开关、原始提示词、素材文案和素材顺序白名单字段；客户端不能直接设置 accepted proposal、active source 或模型来源。
- [ ] 删除采用移入 trash；存在活动渲染时返回 `409 RENDER_ALREADY_ACTIVE`。
- [ ] 运行 `npm run test:backend -- --run backend/tests/api/test_projects.ts`，预期全通过。
- [ ] 提交：`feat: add project management API`。

### Task 5：实现安全图片上传与预处理

**文件：** 创建 `backend/src/services/uploads.ts`、`backend/src/api/assets.ts`；测试 `backend/tests/services/test_uploads.ts`、`backend/tests/api/test_assets.ts`，素材 `backend/tests/fixtures/images/`。

- [ ] 先写测试：JPG/PNG/WebP 成功；伪扩展名、损坏文件、超 15 MB、超像素、SVG、路径穿越文件名和第 21 张图片失败。
- [ ] 使用 Busboy 流式写入随机临时文件并计算大小，达到上限立即销毁流；用 file-type 魔数和 Sharp 完整解码双重校验。
- [ ] 用 Sharp 完整解码、应用 EXIF 方向、转 sRGB、移除元数据并输出 `normalized.jpg`。
- [ ] 保存原始展示文件名但磁盘只使用 UUID；失败时清理临时文件且不修改 manifest。
- [ ] 实现上传和删除 API；上传、删除或排序素材均清空 storyboard 确认。被当前 draft 引用时删除返回冲突；仅被历史 revision 引用时从当前 assets 列表移除但保留不可变 normalized 文件，直到项目整体删除。
- [ ] 运行 `npm run test:backend -- --run backend/tests/services/test_uploads.ts backend/tests/api/test_assets.ts`，预期全通过。
- [ ] 提交：`feat: add validated image uploads`。

### Task 6：实现规则分镜与风格模板

**文件：** 创建 `backend/src/services/storyboards.ts`、`backend/src/render/templates.ts`、`backend/src/api/storyboards.ts`；测试 `backend/tests/services/test_storyboards.ts`、`backend/tests/api/test_storyboards.ts`。

- [ ] 先写参数化测试，覆盖三种风格、逐图文案、总文案切句、无文案、2 秒下限、60 秒上限、转场重叠时长公式和 motion 不连续重复。
- [ ] 实现确定性文本切句、字幕换行、时长/转场 offset 计算、motion/transition 轮换和 BGM 分类到曲库目录映射；优先调用 Task 2 审计通过的最小模块，所有外部模块经 `integrations/oss/` 适配，不让其领域模型泄漏到 API。
- [ ] 校验 storyboard 的 asset 引用属于当前项目；允许且仅允许相邻镜头按各自 transition 时长重叠，按 9.2 公式校验 offset、无间隙、总时长和白名单枚举。
- [ ] 实现生成、保存和确认 storyboard API；生成/保存提交 `expected_version`，写入后计算 storyboard digest 并清空旧确认；确认 API 在项目锁内从有效提示词、设置、有序素材和分镜构造 canonical input digest 后记录 confirmed 字段。无图片返回 `INVALID_STORYBOARD`，旧版本返回 `STORYBOARD_VERSION_CONFLICT`，摘要不匹配返回 `STORYBOARD_CONFIRMATION_STALE`。
- [ ] API 测试覆盖生成/保存后未确认、确认 version/input digest 成功、错误摘要失败，以及编辑分镜后确认字段被清空；本任务不调用尚未实现的 render API。
- [ ] 运行 `npm run test:backend -- --run backend/tests/services/test_storyboards.ts backend/tests/api/test_storyboards.ts`，预期全通过且同一输入输出完全一致。
- [ ] 提交：`feat: generate deterministic storyboards`。

### Task 7：接入可选 MiniMax-M3 提示词优化与 MiniMax-H3 字幕/特效

**文件：** 创建 `backend/src/integrations/minimax.ts`、`backend/src/services/prompts.ts`、`backend/src/api/prompts.ts`；修改 `backend/src/services/storyboards.ts`、`backend/src/api/storyboards.ts`；测试 `backend/tests/integrations/test_minimax.ts`、`backend/tests/services/test_prompts.ts`、`backend/tests/api/test_prompt_proposals.ts`、`backend/tests/services/test_storyboard_ai.ts`。

- [ ] 先写 HTTP mock 测试，断言提示词请求使用系统设置中的默认 `MiniMax-M3`、字幕/特效请求使用默认 `MiniMax-H3`，网页修改模型后立即使用新值；覆盖超时、401、429、500、非 JSON、非法枚举、无 Key和Key清除。
- [ ] 写零调用测试：两个开关默认关闭；关闭时生成 storyboard 和视频全程不调用 MiniMax；两个开关可独立组合。
- [ ] MiniMaxProvider只能调用 `getInternalCredentials()` 并通过 `SafeMiniMaxHttpClient` 发请求，不得直接调用fetch/axios或接收Public DTO；M3/H3响应用Zod严格校验。类型/运行时测试断言内部凭据不能JSON序列化、记录或进入Express response。
- [ ] 实现 proposal API 和文件持久化：创建时状态只能为 `proposed` 且不修改 active prompt；accept 必须校验 proposal digest，并支持“原样接受/编辑后接受”；reject 保持原提示词；重复决定或旧 digest 返回冲突；修改原始提示词会使旧接受结果失效并切回 original。
- [ ] 在 storyboard 服务中，只有 `minimax_caption_effects_enabled=true` 才调用 H3；采用原提示词或已 accepted 的 M3 文本，只替换字幕/特效并标记 `source=minimax_h3`，时长、BGM 和文件均由规则流程控制。接受新提示词、切换H3/风格或重新生成后必须清除旧 storyboard 确认。
- [ ] M3 失败时保留原提示词；H3 失败时返回 RulePlanner 字幕/特效和 `AI_CONTENT_FALLBACK_USED`；任何 API 成功都不能隐式接受 M3 提案。
- [ ] 客户端超时 15 秒、最多重试一次，授权失败不重试；日志只保留模型、ID、耗时和脱敏错误码。
- [ ] 运行 `npm run test:backend -- --run backend/tests/integrations/test_minimax.ts backend/tests/services/test_prompts.ts backend/tests/api/test_prompt_proposals.ts backend/tests/services/test_storyboard_ai.ts`，预期模型路由、确认门禁、独立开关、零调用默认与降级测试全部通过。
- [ ] 提交：`feat: add consent-gated M3 prompts and optional H3 content`。

### Task 8：实现字幕与渲染计划

**文件：** 创建 `backend/src/render/plan.ts`、`backend/src/render/subtitles.ts`；测试 `backend/tests/render/test_plan.ts`、`backend/tests/render/test_subtitles.ts`。

- [ ] 先写测试，覆盖中文、emoji、ASS 特殊字符、换行、负时间、镜头间隙、字幕关闭、规则/H3 来源和三种模板；render_input 引用 proposed/rejected M3 提案时必须失败。
- [ ] 把 revision 的 `render_input.json` 编译成内部 `RenderPlan`，验证输入摘要以及每张图片、冻结 BGM、字体和对应许可证条目的 SHA-256；所有文件路径由 repository 解析，所有动效映射到固定 FFmpeg 参数片段。
- [ ] 生成 ASS：内置字体、1080×1920 PlayRes、安全区、描边和风格色；正确转义 `{}`、反斜杠与换行。
- [ ] 禁止 AI/用户文本进入滤镜表达式；字幕只写入独立 ASS 文件。
- [ ] 运行 `npm run test:backend -- --run backend/tests/render/test_plan.ts backend/tests/render/test_subtitles.ts`，预期全通过。
- [ ] 提交：`feat: compile storyboards into safe render plans`。

### Task 9：实现 FFmpeg 渲染管线

**文件：** 创建 `backend/src/render/pipeline.ts`、`backend/src/render/probe.ts`、`scripts/check-assets.ts`；测试 `backend/tests/render/test_pipeline.ts`；测试资产放 `backend/tests/fixtures/media/`，生产字体放 `assets/fonts/NotoSansSC-Regular.otf`，生产 BGM 分别放三个 `assets/music/<style>/` 目录。

- [ ] 先写集成测试：2 张横竖图分别渲染启用/禁用字幕与音乐的视频；取消时子进程终止；错误时无伪造 output；revision 创建后新增/重排曲库不换曲，替换/删除冻结 BGM 或字体则以 `ASSET_INTEGRITY_ERROR` 失败。
- [ ] 用 `node:child_process.spawn(command, args, {shell:false})` 参数数组调用 ffmpeg/ffprobe，以 `AbortController` 取消并限制 cwd/临时目录/输出路径；禁止拼接 shell 命令。
- [ ] 实现图片背景模糊 + 前景 contain、镜头 motion、转场、ASS 字幕、BGM 循环/裁剪/淡化和最终编码。
- [ ] 测试 fixture 使用脚本生成的纯色图片和正弦波音频，不冒充生产曲库；生产环境引入 Noto Sans SC（SIL OFL）及每种风格至少 1 首经负责人确认可商用/再分发的 BGM，并按 Task 1 字段登记许可证和 SHA-256。
- [ ] 实现 `scripts/check-assets.ts`，运行 `npm exec -- tsx scripts/check-assets.ts --font assets/fonts/NotoSansSC-Regular.otf --music-root assets/music --min-tracks-per-style 1 --verify-digests`；预期三个风格均通过且无未登记文件。
- [ ] 音乐选择只在创建 revision 时按 9.2 的分类→目录映射、规范化路径排序和 SHA-256 取模执行，并把曲目/字体路径、文件摘要和许可证条目摘要冻结到 `render_input.json`；worker 禁止重新选曲，没有许可证记录或摘要不匹配时拒绝渲染。
- [ ] 解析 FFmpeg `-progress` 输出，将阶段进度限制在 0–99；只有 probe 验证成功后写 100。
- [ ] 用 ffprobe 断言输出：1080×1920、30 fps、H.264、有音频时 AAC、时长误差不超过 0.5 秒、`yuv420p`。
- [ ] 运行 `npm run test:backend -- --run backend/tests/render/test_pipeline.ts`，预期全通过。
- [ ] 提交：`feat: render vertical videos with ffmpeg`。

### Task 10：实现任务队列、恢复、取消与重试

**文件：** 创建 `backend/src/jobs/queue.ts`、`backend/src/api/renders.ts`；修改 `backend/src/app.ts`；测试 `backend/tests/jobs/test_queue.ts`、`backend/tests/api/test_renders.ts`。

- [ ] 先写测试：FIFO 串行、重复提交冲突、状态顺序、进度持久化、取消 queued/running、失败、启动恢复；直接调用 render 时未确认/确认摘要过期/引用 proposed 或 rejected M3 提案均返回 409，确认后成功；确认后修改字幕/BGM/动效/H3/风格、素材上传/删除/排序/文案或有效提示词均使确认失效；只改标题不失效；排队后的修改不改变已冻结输入；retry 与源输入逐字节相同。
- [ ] 实现 Node.js 单消费者 Promise 队列；Express 服务启动时启动 worker，收到 `SIGTERM/SIGINT` 后停止接单，用 `AbortController` 终止当前 ffmpeg 并等待状态持久化。
- [ ] `POST /renders` 在项目锁内重构完整待渲染确认载荷，复算 `confirmed_input_digest` 并校验 storyboard version，再验证实际提示词为原文或 accepted proposal；未确认或过期返回稳定 409。通过后冻结全部输入到 `render_input.json`，记录摘要并创建 revision 后入队。worker 不读取当前状态、不调用 MiniMax或创建第二个 revision。
- [ ] retry 在项目锁内逐字节复制源 revision 的 `render_input.json` 到新 revision，记录相同输入摘要后入队；当前草稿、设置和素材元数据不参与重试。
- [ ] 服务启动时按状态表恢复：`queued` 保持、`running` 回到 `queued`、`cancelling` 变 `cancelled`、终态不变。
- [ ] 提交前按 `输入规范化文件总大小 × 2 + 预估未压缩帧缓存上限 2 GB + 目标输出 500 MB` 计算所需空间；`max(该估值, 5 GB)` 加配置磁盘保留量后仍不足则返回 `INSUFFICIENT_STORAGE`。
- [ ] 实现提交、状态、取消和重试 API；保证同项目最多一个活动任务。
- [ ] 运行 `npm run test:backend -- --run backend/tests/jobs/test_queue.ts backend/tests/api/test_renders.ts`，预期全通过。
- [ ] 提交：`feat: add recoverable render queue`。

### Task 11：实现视频播放、下载和分享

**文件：** 创建 `backend/src/services/shares.ts`、`backend/src/api/media.ts`、`backend/src/api/shares.ts`；测试 `backend/tests/api/test_media.ts`、`backend/tests/api/test_shares.ts`。

- [ ] 先写测试：完整下载、Range `206`、无效范围 `416`、未完成 revision `409`、为指定 completed revision 开启/轮换/撤销分享、新 revision 不改变旧链接内容、旧 token 失效。
- [ ] 视频端点只解析项目/revision 元数据，不接受任意磁盘路径；正确返回 `Content-Type`、`Content-Length`、`Accept-Ranges` 和安全文件名。
- [ ] 创建 32 字节安全随机 token，只保存 `SHA-256` 摘要并用 `crypto.timingSafeEqual` 比较；manifest 同时记录绑定的 `revision_id` 与创建时间。
- [ ] 分享 API 只返回标题、视频 URL 和下载能力，不返回项目编辑信息。
- [ ] 运行 `npm run test:backend -- --run backend/tests/api/test_media.ts backend/tests/api/test_shares.ts`，预期全通过。
- [ ] 提交：`feat: add video delivery and revocable sharing`。

### Task 12：实现 Ant Design 应用壳、系统设置和素材管理

**文件：** 创建 `frontend/src/app/theme.ts`、`frontend/src/app/AppShell.tsx`、`frontend/src/app/router.tsx`、`frontend/src/api/client.ts`、`frontend/src/api/types.ts`、`frontend/src/features/settings/SystemSettingsPage.tsx`、`frontend/src/features/projects/ProjectListPage.tsx`、`frontend/src/features/editor/AssetStep.tsx`；测试对应 `SystemSettingsPage.test.tsx`、`ProjectListPage.test.tsx`、`AssetStep.test.tsx`。

- [ ] 先写 Testing Library 测试：Ant Design Menu导航、空项目列表、创建、API错误、上传进度/限制/排序，以及系统设置读取掩码、空Key保持、替换、清除二次确认、M3/H3测试连接和Key不进入DOM缓存/URL。
- [ ] 用 `ConfigProvider`、`App`、`Layout`、`Menu`、`Grid` 建立统一中文应用壳和响应式主题；主菜单包含“视频项目”和“系统设置”，不引入MUI/Bootstrap等第二套UI库。
- [ ] API client 统一解析 Express 错误结构、附加 request ID；共享Zod/OpenAPI契约生成或校验前端类型。
- [ ] 系统设置页使用 `Form`、`Input.Password`、`Input`、`Button`、`Alert`、`Popconfirm`：Key初始为空且不回填掩码，留空保持，清除需确认；API Base前端也校验HTTPS、无userinfo/query/fragment，但以后端校验为准；保存后立即清空Key输入并刷新公开脱敏DTO。
- [ ] 项目列表使用 `Card`/`List`/`Empty`/`Modal`；素材编辑使用 Ant Design `Upload.Dragger`、`Image`、`Input.TextArea`、`Progress`，并提供键盘可操作排序。
- [ ] 保存失败保留非敏感业务输入并可重试；API Key失败只保留在当前受控输入直到用户修改/离开，不进入TanStack Query缓存。
- [ ] 运行 `npm run test:frontend -- --run frontend/src/features/settings frontend/src/features/projects frontend/src/features/editor/AssetStep.test.tsx && npm --prefix frontend run typecheck && npm --prefix frontend run build`，预期全通过。
- [ ] 提交：`feat: add ant design shell settings and asset UI`。

### Task 13：实现风格、分镜和生成进度界面

**文件：** 创建 `frontend/src/features/editor/PromptOptimizer.tsx`、`frontend/src/features/editor/StyleStep.tsx`、`frontend/src/features/editor/StoryboardEditor.tsx`、`frontend/src/features/render/RenderStep.tsx`；测试 `frontend/src/features/editor/PromptOptimizer.test.tsx`、`frontend/src/features/editor/StyleStep.test.tsx`、`frontend/src/features/editor/StoryboardEditor.test.tsx`、`frontend/src/features/render/RenderStep.test.tsx`。

- [ ] 先写测试：两个 Ant Design `Switch` 默认未选中、点击/键盘可独立切换且显示已启用/未启用；未配置Key时禁用并链接系统设置；M3预览/决定、H3来源/降级、分镜确认失效、提交/轮询/取消/重试。
- [ ] PromptOptimizer 使用 `Switch`、`Button`、`Modal`、`Typography`、`Input.TextArea`；只有开关选中并点击按钮才请求M3，并排展示原文/提案/说明/模型，不预选接受。
- [ ] StyleStep 使用 `Card`、`Radio.Group` 和独立 M3/H3 `Switch`；capabilities 未配置时禁用对应开关但规则生成可用，没有曲库或FFmpeg才禁用视频生成。
- [ ] StoryboardEditor 使用 `Form.List`、`List`/`Table`、`Tag`、`InputNumber` 展示镜头和来源；“确认分镜”调用API。影响载荷的变化清除确认，标题变化不清除。
- [ ] RenderStep 使用 `Descriptions`、`Progress`、`Alert`、`Button`；仅服务端确认有效时启用提交，轮询不可见时降频，终态停止。
- [ ] 展示阶段文字和真实百分比；失败显示安全错误信息及重试，不展示 render.log 原文。
- [ ] 运行 `npm --prefix frontend test -- --run src/features/editor src/features/render && npm --prefix frontend run typecheck && npm --prefix frontend run build`，预期全部退出码 0。
- [ ] 提交：`feat: add consent-gated prompt and storyboard workflow UI`。

### Task 14：实现审核、修订、下载和分享界面

**文件：** 创建 `frontend/src/features/render/ResultPage.tsx`、`frontend/src/features/share/SharePage.tsx`、`frontend/src/features/share/ShareActions.tsx`；测试 `frontend/src/features/render/ResultPage.test.tsx`、`frontend/src/features/share/SharePage.test.tsx`、`frontend/src/features/share/ShareActions.test.tsx`。

- [ ] 先写测试：播放器 URL、下载、返回修改、revision 切换、开启/撤销分享、复制、Web Share 支持/不支持、无效 token。
- [ ] 结果页使用 Ant Design `Result`、`Card`、`Tabs`、`Alert`、`Button` 展示9:16播放器、revision、警告、下载和重新生成。
- [ ] 分享操作使用 `Space`、`Button`、`Modal`、`message`；优先 `navigator.share`，否则复制，失败时展示可选URL。
- [ ] 分享页只读，token 无效/撤销后显示资源不可用，不泄露项目 ID 或服务端路径。
- [ ] 运行 `npm --prefix frontend test -- --run src/features/render/ResultPage.test.tsx src/features/share && npm --prefix frontend run typecheck && npm --prefix frontend run build`，预期全部退出码 0。
- [ ] 提交：`feat: add review download and share UI`。

### Task 15：端到端测试与无障碍/兼容性检查

**文件：** 创建 `frontend/e2e/video-flow.spec.ts`、`frontend/playwright.config.ts`、`frontend/vitest.config.ts`、`frontend/src/test/setup.ts`、`scripts/smoke-test.ts`；修改 `frontend/package.json` 测试脚本。

- [ ] 用固定素材编写默认 E2E：Ant Design菜单→项目→上传→两个Switch保持未选中→规则方案→确认→渲染→播放/下载/分享，并断言MiniMax零请求。
- [ ] 增加系统设置E2E：输入Key并保存后页面只显示掩码；刷新不回填明文；替换/清除/测试连接生效；请求日志、DOM快照和浏览器存储均不含Key。
- [ ] 增加可选AI E2E：通过Switch独立选择M3/H3；M3拒绝不生效、接受才生效；H3只生成字幕/特效；确认门禁与失效规则正确。
- [ ] 增加异常 E2E：非法图片、M3/H3 未配置、M3 提案未接受、storyboard 未确认/确认过期、H3 降级、渲染失败、刷新恢复和磁盘不足模拟。
- [ ] `scripts/smoke-test.ts` 通过 API 完成最短流程，并用 ffprobe 校验下载文件。
- [ ] 在 Chrome、Safari 和移动视口验证上传、播放、下载；运行 axe 检查关键页面无严重无障碍错误。
- [ ] 运行 `npm run test:backend -- --run && npm run test:frontend -- --run && npm run typecheck && npm run build && npm --prefix frontend run e2e`，预期全部退出码 0。
- [ ] 提交：`test: cover complete video generation workflow`。

### Task 16：容器化、运维和数据保护

**文件：** 创建 `backend/Dockerfile`、`frontend/Dockerfile`、`deploy/nginx.conf`、`deploy/entrypoint.sh`、`docker-compose.yml`、`scripts/backup_data.sh`、`scripts/proxy-smoke-test.sh`；完善 `README.md`。

- [ ] 后端 Node 22 镜像安装固定主版本FFmpeg和中文字体，以非root用户运行；挂载可写 `/app/data` 和只读 `/app/assets`，启动后创建 `data/system/settings.json` 为0600。
- [ ] 前端生产镜像使用固定版本Nginx托管Ant Design SPA并代理Express；`deploy/nginx.conf` 默认对 `/`、编辑路由和除share外的API启用Basic Auth，`auth_basic_user_file /run/secrets/editor_htpasswd`，仅 `/health`、`/share/*`、`/api/v1/share/*` 匿名；视频代理保留Range/206头。
- [ ] compose明确定义frontend-proxy、backend服务、只读Nginx配置和`editor_htpasswd` Docker secret；`deploy/entrypoint.sh` 在secret缺失/空文件时拒绝启动，不在镜像生成默认密码。Express设置路由继续做Origin/CSRF校验，不能仅依赖代理。
- [ ] 设置上传请求体320MB、普通API超时30秒、上传/视频10分钟；异步渲染不占HTTP长连接。compose声明healthcheck、持久卷和日志轮转，不声明/注入任何`MINIMAX_*`环境变量，MiniMax只能由系统设置页写入数据卷。
- [ ] 备份脚本默认归档项目manifest/uploads/proposals/outputs并明确排除 `data/system/settings.json`；只有显式 `--include-secrets` 才包含且输出文件0600并警告含Key。
- [ ] README说明Express/Ant Design开发、系统设置页面单Key配置、配置文件权限、模型修改、开源审计、备份恢复和故障处理。
- [ ] 运行 `bash scripts/proxy-smoke-test.sh http://localhost`：未认证 `/` 和系统设置返回401，带凭据后200；`/health`匿名200；匿名分享API返回业务404而非401；生成视频后Range请求返回206；伪造Origin访问设置PUT被Express拒绝。
- [ ] 运行 `docker compose config --quiet`，并以 `grep -R "MINIMAX_" docker-compose.yml backend/Dockerfile frontend/Dockerfile` 验证无匹配；启动后通过设置API写入测试Key，运行 `npm exec -- tsx scripts/smoke-test.ts --base-url http://localhost --basic-auth`，预期生成有效MP4且GET设置不回显Key。
- [ ] 提交：`chore: add production deployment and operations guide`。

### Task 17：发布验收

**文件：** 创建 `docs/release-checklist.md`、`docs/samples/README.md`、`scripts/release-check.ts`、`scripts/restart-recovery-test.ts`、`artifacts/.gitkeep`。

- [ ] 使用 `npm exec -- tsx scripts/release-check.ts --styles humorous romantic tech --output artifacts/release` 以同一组固定素材生成三种风格样片；预期输出每条 MP4、ffprobe JSON、渲染耗时和文件大小清单。
- [ ] 产品方对照可访问的参考视频人工确认画幅、图片运动、字幕位置/节奏、转场强度和 BGM 氛围；若参考仍不可访问，则以 2.2 表格、9.2 时间线契约和三种样片作为明确替代验收基线，不宣称像素级复刻。
- [ ] 运行 `npm exec -- tsx scripts/release-check.ts --verify-licenses assets/music/LICENSES.md assets/fonts`，预期所有音乐、字体和测试素材均有来源、作者、许可、下载日期及文件摘要。
- [ ] 在目标规格服务器运行 `npm exec -- tsx scripts/release-check.ts --repeat 10 --assert-success-rate 1.0 --assert-no-active-jobs`，预期 10 次全部成功、无崩溃和无遗留活动状态，证据写入 `artifacts/release/summary.json`。
- [ ] 运行 `npm exec -- tsx scripts/restart-recovery-test.ts`，预期 queued/running/cancelling 按状态表恢复；运行 `bash scripts/backup_data.sh create artifacts/backup.tar.zst && bash scripts/backup_data.sh restore-test artifacts/backup.tar.zst`，预期恢复副本校验和一致。
- [ ] 运行 `npm exec -- tsx scripts/audit-oss.ts --check docs/oss-adoption.md THIRD_PARTY_NOTICES.md`，预期每个候选有采用/拒绝结论，所有实际第三方代码/资产均有版本、许可证、复用范围和修改记录。
- [ ] 通过受保护的系统设置 API 写入合规测试Key和M3/H3模型，运行 `npm exec -- tsx scripts/smoke-test.ts --base-url http://localhost --minimax --redact-output`：验证设置GET只返回掩码、M3同意门禁、H3范围和分镜确认；随后清除Key并确认规则流程仍通过。证据不得包含Key。
- [ ] 运行 `npm run test:backend -- --run && npm run test:frontend -- --run && npm run typecheck && npm run build && npm --prefix frontend run e2e`，预期全部退出码0；验证系统设置脱敏/权限、磁盘不足、分享撤销、项目删除和日志脱敏证据均写入release checklist。
- [ ] 对照“11.2 MVP 完成定义”逐项签字并提交：`git add docs/release-checklist.md docs/samples artifacts/release/summary.json artifacts/release/*.probe.json && git commit -m "docs: add v0.1.0 release evidence"`；生成的 MP4 样片保留在服务器但不提交 Git。
- [ ] 全部通过后执行 `git tag -a v0.1.0 -m "video generator v0.1.0"`；运行 `git status --short` 预期无输出，`git tag --points-at HEAD` 预期包含 `v0.1.0`。

## 13. 推荐实施顺序与里程碑

| 里程碑 | 包含任务 | 可演示结果 |
|---|---|---|
| M1：基础与素材 | Task 1–5 | 创建项目、上传并安全保存图片 |
| M2：方案生成 | Task 6–8 | 开源/规则可编辑分镜；可选 M3 提案确认与 H3 字幕/特效 |
| M3：视频产出 | Task 9–11 | 异步生成、恢复、播放、下载和分享 MP4 |
| M4：完整网站 | Task 12–14 | 用户可在网页走完整闭环 |
| M5：可发布 | Task 15–17 | 测试、容器部署、授权和发布证据齐全 |

依赖关系：M1 → M2 → M3 → M4 → M5。Task 12 的项目/素材页面可在 Task 4–5 API 稳定后开始；Task 13 的分镜与渲染界面必须等待 Task 6–10 的草稿版本、MiniMax warning、revision 和状态契约稳定。任何 API 类型变更必须先更新 OpenAPI 契约和前端类型。

## 14. 后续演进（不进入 v0.1.0）

1. **v0.2：** TTS 旁白、字幕按音频强制对齐、更多模板、项目复制。
2. **v0.3：** PostgreSQL + 对象存储 + Redis/BullMQ，多用户、登录和配额。
3. **v0.4：** 接入经审核的 AI 作曲/文生视频 API；采用 provider adapter，不改变项目/修订领域模型。
4. **v0.5：** 可视化时间线；字幕编辑继续复用 v0.1 审计通过的最小组件，若当时未采用 flycut-caption 则重新审计其新版本。
5. **更后期：** 社媒 OAuth/发布；需单独评估平台协议、审核机制、账号安全和合规，不复用当前公开分享 token 作为平台凭证。

## 15. 可选的早期视觉校准

文档中的 2.2 默认参数、9.2 风格/时间线契约和 Task 17 三种样片构成参考链接不可访问时的完整验收基线，因此不阻塞 Task 1，也不承诺对原作品做像素级复刻。若产品方能合法提供参考视频文件或录屏，应在 M1 结束前校准以下模板参数，以减少发布阶段返工：

- 单镜平均时长和字幕出现节奏；
- 图片是全屏裁切还是模糊背景 + 完整前景；
- 字体、字号、字幕位置和强调色；
- 转场种类和强度；
- 是否需要旁白（若需要则进入 v0.2，除非产品方批准调整首版范围）。
